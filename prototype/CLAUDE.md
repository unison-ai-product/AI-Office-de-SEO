# プロジェクト常設ルール

0. **正本参照ルール（最優先）**: 本ファイルと `Design.md` 追補は**要約**であり、仕様の正本は同梱リポジトリの `../docs/`（本プロトはv3.7.50から要求パッケージ `prototype/` に同梱。**現行版: v3.7.49相当・2026-07-06統合**。manifest.jsonのversionが正）。
   - 画面台帳（ユーザー面 S1〜S7 / **W1〜W10**（W10=サポート、2026-07追加）/ A0〜A8・**§0共通規約=操作明記・5類型・グローバル要素（＋サポートパネル起動）**）: `docs/design/ai-office-de-seo/L3-ui-prototype/ai-office-de-seo-screen-inventory_v3.7.md`
   - 管理コンソール台帳（**ADM-S1〜S12**（S12=サポートデスク、2026-07追加）・**§1.5 編集可能オブジェクト台帳**）: 同 `ai-office-de-seo-admin-screen-inventory_v3.7.md`
   - 受入観点・ビルド順（**PT-0＋PT-A〜Z**。PT-M/N/O/P/Q/R/S/T/U/V/W/X/Y/Zは2026-07更新で追加。W10⇄ADM-S12往復=PT-Y、アクセシビリティ=PT-Z、fixture必須セット=PT-X）: 同 `ai-office-de-seo-prototype-plan_v3.7.md`
   - 画面・UI要求: `L1-requirements/…navigation-ui-requirements_v3.7.md`（**REQ-NAV-01〜09**。06=レスポンシブ・07=UI文言外部化・**08=アクセシビリティ品質床・09=UIテキストレジストリ（版activateで差し替え）**）/ `…agent-office-ui-requirements_v3.7.md`（REQ-AOUI-01〜07。ペルソナ⇄工程は04、config駆動は07）
   - プロダクト要求: `…product-requirements_v3.7.md`（**REQ-PRODUCT-01〜23**。11=通知カタログ・12=カスタマイズ階梯・13=ネットワーク学習/オプトアウト・14=CSVエクスポート・15=グローバル検索・16=運営お知らせ/ステータスページ・**17=月次目標/配分/予測・18=資源/変更ガバナンス（変更予算・振動検知）・19=導出事実ストア/施策台帳・20=サマリー契約/意味索引・21=メール配信基盤・22=サポート運用（W10/ADM-S12）・23=マスターテナント/事例許諾**）
   - キーワード系: `…keyword-gsc-article-map-requirements_v3.7.md`（**REQ-KGA-01〜21**。13=属性/ギャップ・14=アサイン台帳・15=マッチカスケード・16=ロングテール昇格・17=価値スコア/AIO・18=起点3類型・**19=サイトトポロジー戦略（幹枝葉/リンク再調整）・20=ウォッチリスト/急変検知/季節・21=インデックス状況/技術ヘルス**）
   - 課金系: `…billing-credit-provider-requirements_v3.7.md`（REQ-BILL-01〜11。11=実行レーン/Batch）、生成フロー: `…agent-runtime-requirements_v3.7.md`（**REQ-AGENT-01〜11**。09=13状態・10=中断再開・**11=全体整合/Cohesion QA**）、リライト: `…rewrite-runtime-requirements_v3.7.md`（**REQ-RWR-08=好調保護/波及・09=フラッシュリライトTDH**）、WP自動化: `…wp-automation-…_v3.7.md`（**REQ-WPA-11=滞在/スクロール計測・12=部分パッチ・13=CVポイント台帳/管理CRO**）、外部情報源: `…dataforseo-competitor-batch-requirements_v3.7.md`（REQ-SRC-01〜10。**10=負荷平準化/静穏窓**）、運用継続: `…security-observability-…`（**SEC-14/15/16**）・`…development-unit-roadmap_v3.7.md`（**DUR-06〜10=キャパシティ/実行基盤/DR/コンテナ/自動復旧**）、管理面要求: `…admin-console-requirements_v3.7.md`（**REQ-ADM-01〜11**。11=表示ラベルレジストリ）
   - **運用**: 画面や工程に触れる前に該当原本を読む。工程名・データ項目・候補源は原本から引用し、推測で埋めない。完成判定は見た目ではなくPT観点で行う。実装で決めたことはその場で本ファイル・`Design.md`追補に書き戻す。
1. **UI表示言語規約を徹底**（詳細は `Design.md` §6.5）: ユーザー向け画面に開発用語・仕様ID（REQ-*, S1〜S7, W1〜W9, Stage, Pack, Ticket, hard gate, fail-close, lane, full_auto, Batch 等）を出さない。対訳表に従う。表にない用語は先に対訳を追加してから使う。管理コンソールは対象外（ただしTERMS辞書＋日本語二段表記の運用）。
2. 通常SaaSビューはアプリシェル型（サイドメニュー左端固定・流体幅・1024px未満はドロワー）。Webサイト風の中央寄せにしない。グローバル要素（通知・ジョブインジケータ・**グローバル検索**・残高/チャージ）はヘッダー等の常設領域に置き、第一階層7項目に数えない（REQ-NAV-02）。**ヘルプ・サポートはヘッダーに置かず右下フロート（チャット形式のFAB→AI一次応答/ツアー/ヘルプセンター/稼働状況、`userViewOn`でadmin以外に表示）。表示言語（日本語/English）はヘッダーではなく設定「表示言語」で選ぶ（2026-07改訂・Design.md §6.4準拠）。**
3. Agent Officeビューは `Design.md` 追補3（§6.6）準拠: 1画面完結・フロア1面表示＋エレベーター・俯瞰1部屋1キャラ・カットシーンズーム・状態連動ホロ小物・WORK LOG・吹き出し・ビジュアライザー。テキスト・数値・表・ボタンは必ずHTML/CSS描画（REQ-NAV-05/REQ-AOUI-03）。デスクトップ最適・モバイルは通常ビューへ誘導（REQ-NAV-06）。
4. デザイントークンは `config/design-tokens.css` が正。オフィスレイアウトは `config/office_layout.json` から描画（ハードコード禁止、REQ-AOUI-07）。**現行config v2.1.0 は7フロア構成（1部屋=1フロア・ハブ=ダッシュボード。1F=01〜7F=07）＝ユーザー指示でゆとり重視に変更。正本（office_layout.initial.json v2.1.0 / Gate A-3 v1.3）・ルート Design.md §6.6 とも7フロアで一致済み（v3.7.49で書き戻し完了）**。追従はconfig変更のみで行う（PT-G。エレベーター表示はfloors配列から生成）。
5. **閲覧専用に倒さない（AC-NAV-05 / PT-P）**: 画面台帳に明記された操作（追加・編集・削除・実行・選択・一括処理）はプロトでも実際に操作できる形で作る。表示だけのモックにしない。
6. **表示状態は5類型**: 通常/読込中/計算中（＋再試行）/空/エラーを全一覧・全詳細が持つ（REQ-SEC-06、PT-C。既存の`state.disp`検証トグルで一括確認）。
7. アセット運用: `scene_05` / `scene_08` / `scene_ref_*` は数値焼き込みのため使用禁止。動的差し込み画像は `&lt;img src&gt;` でなく background-image。全演出は `prefers-reduced-motion: reduce` で停止。
8. 通常SaaSビューのテーマは `Design.md` 追補4（§6.7）準拠: ブルー×シルバー・メタリック、濃色は左上、ゴールドはチャージ専用、ステータス色は減彩度版、アイコン全SVG、サイドメニュー両ビュー200px。
9. **UI文言はハードコードから外部化する方針**（REQ-NAV-07・PT-Q）: 新規実装分はラベル定数（辞書オブジェクト）経由で書く。既存分の一括外部化はバックログ12。
10. **1画面（1タブ）に目的を積まない**（2026-07-08 ユーザー制定・screen-inventory §0 が正本）: 1タブ=1目的。目的（ユーザーの用向き）が変わるところでタブに切る。新しいセクションを足すときは、まず「どの目的か」を決め、既存タブの目的と一致しないなら既存タブへの縦積みではなく新タブ／タブ分割で対応する。各タブには役割キャプション（S2の`kwTabDesc`方式）を推奨。第一階層7項目は増やさない（REQ-NAV-02の範囲内でタブを増減する）。
11. **破壊的操作は「直後の取り消し」で守る（思いやりUI・2026-07-08）**: 一覧行の削除系はワンクリックで実行してよいが、必ず `showUndoToast(msg, undoFn)`（6秒・元に戻すボタン付きトースト）を使う。確認ダイアログは不可逆・広範囲な操作（解約・全端末ログアウト・許諾撤回・緊急停止）に限る。Escキーは全モーダル/ドロップダウンを閉じる（`componentDidMount`の`onEsc`に状態キーを追加登録）。

---

# 進捗メモ

## 完了（〜2026-07-03 PT最終監査まで）
- **単一DC** `AI Office de SEO.dc.html` に2モード（通常SaaS ⇄ Agent Office、localStorage永続）。S1〜S7・W2〜W9（W1=共有ワークベンチ枠、W8緊急停止はS4内、W9同意書はS7内）・S2タブ再編・13工程正本準拠・オンボーディングツアー（通常7/Office6）。
- **PT-A〜H・L・M 充足を監査済み**: シェル共有化（`shellOn`/`WB_MAP`/`openWb()`）、エージェント状態導出（`derivePersonaStates()`＝office_layout.jsonのmapping.stages駆動）、採用/あとで/却下の共通UI（`recFbSet`/`recVals`）、4状態展開、暫定プロファイル/主張軸Validate差し戻し、Gate A-1凍結エンベロープ（`mkEnv()`・envLog）、一時停止/再開（checkpoint・再ウォーム費・job.suspended/resumed同形）、S3レーン選択の見積連動（ctLanes）、S5順位列/CV KPI/AI検索の影響パネル、W5再開Preflightモーダル。
- **`Admin Console.dc.html`**（ユーザー面と分離、REQ-ADM-01）: ADM-S1〜S11全画面実装済み。PT-I/J/K充足を監査済み（理由必須＝ceReason/ffReason/rpReason、テナント別コスト内訳、mask表示、draft→Preview→Validate→Approve→Publish、公開済みversion不変）。日本語二段表記＋EASY説明＋TERMS辞書。なりすましはlocalStorage `aos_imp` でユーザー面連動（バナー＋`roGuard()`）。

- **バックログ1〜3 完了（2026-07-03）**: ①グローバル検索（両モードヘッダー常設。`gsVals()`/`gsIndex()`/`gsGo()`。検索対象＝KW_ROWS・記事（_anBase）・ジョブ（_whBase＋稼働中）・通知・設定項目。Officeモードは選択→wbワークベンチで開く。本文対象外の注記あり）、②CSVエクスポート（S2 `kwCsv`（フィルタ適用後）・S5 `anCsv`・W6 `whCsv`、共通`exportCsv()`＝BOM付きBlobダウンロード）、③運営お知らせ（W7に種別チップ付き表示、ADM-S1に作成・対象選択・配信＋監査記録。localStorage `aos_announce` でユーザー面へ連動）＋ヘルプメニュー（ツアー/ヘルプセンター/稼働状況を両ヘッダーの「?」に集約）。新規文言は`LABELS`辞書経由（ロジック駆動分のみ。テンプレート静的文言はストリーミング制約でインラインのまま）。

- **ADM-S2 価格改定フロー（2026-07-03・PT-P充足）**: カタログ表に操作列を追加。①改定＝5ステップモーダル（新売価＋effective_fromプリセット→影響確認（freeze不変・売上影響・Stripe新Price発行）→自動点検（粗利下限）→承認（理由必須）→公開。`state.pr`/`prSched`で改定予約チップ表示）、②Stripe Price対応付け張り替え（`rm*`・理由必須・次回請求から適用注記）、③Credit Pack新規作成（`pn*`＝draft行追加→改定フローで公開）。全操作pushAudit記録・ACT_JPに対訳追加済み。

- **ADM-S8の編集はノーJSON（2026-07-03）**: 本文テンプレート＝textareaで文章を直接編集（`PACK_BODY`・「差分へ反映」でdraftのdChangesに追記）、**Prompt Packはセクション別チューニング**（役割/制約/トーン/出力形式＝`PROMPT_SECTIONS`/`packSec`）、few-shot＝文章入力＋正例/反例選択のみで追加/削除（`FEWSHOTS`/`fsExtra`/`fsRemoved`）、office_layout＝部屋名入力＋1F/2F選択＋追加/削除のフォーム（`OL_DEFAULT`/`olRooms`。JSONは公開時に自動生成という建付け）。さらに**チューニングのお試し実行**（draft内で現行版 vs draft版のサンプル出力比較＝`TEST_SAMPLES`・サンドボックス・課金なし注記）を同梱。**全Pack型にフォーム編集を完備（2026-07-03）**: 品質ゲート＝行編集（名称＋必須/推奨切替・追加/削除。コア4項目中2項目は安全不変条件＝🔒ロック表示、`QG_GATES`）、Workflow＝停止ガードのトグル（fail-closeは常時ONロック、`WF_GUARDS`）、類語辞書＝行追加/削除（`DICT`・集合知候補チップ）、新規作成・複製（`packNew`/`dDup`＝`state.packExtra`でALLPにマージ）。ユーザーに生JSONを書かせない方針をADM全編集UIの規約とする。

## バックログ（2026-07-03 要件最新化の突合。着手順・grep監査済み）

新設PT観点（PT-N/O/P/Q）と画面台帳の追記分に対する未実装。**着手前に必ず該当REQ原本を読む。**

1. ✅完了 **PT-Q グローバル検索**（REQ-PRODUCT-15）: ヘッダー常設。テナント内のキーワード・記事（タイトル/URL/メタ）・ジョブ・通知・設定項目を横断検索→対象画面へ遷移。本文全文は対象外。両モード対応（Officeはヘッダー相当へ）。
2. ✅完了 **PT-Q CSVエクスポート**（REQ-PRODUCT-14）: S2キーワード表・S5分析表・W6ジョブ履歴に導線（プロトはモック生成でよい）。本文・プロンプト・シークレットは対象外の注記。
3. ✅完了 **PT-Q 運営お知らせ＋導線**（REQ-PRODUCT-16）: W7に「運営お知らせ」種別を追加、ADM-S1に作成・対象選択（全体/プラン/テナント）・配信の操作（配信は監査記録）。ヘッダー/ヘルプ近傍に公開ステータスページ・ヘルプ専用サイトへの導線を常設。
4. ✅完了 **PT-P S2手動追加**（REQ-KGA-03）: 「+ キーワードを追加」→モーダル（単発/一括貼付/CSVインポート（実ファイル読込）/シード語展開（SEED_MODS・チェック選択→一括登録））。登録分は`state.kwExtra`に「判定中/取得中」のpending行として一覧先頭に入る（`kwAddRegister()`、重複はスキップ通知）。
5. ✅完了 **PT-P S3起点の選択**（REQ-KGA-18）: 起点セグメント（`ctOrigin`＝keyword/news/video、候補＝`CT_NEWS_ORG`/`CT_VIDEO_ORG`）。news候補は鮮度期限チップ、freshHigh×おまかせ選択で鮮度注意表示（`ctFreshWarnOn`）。S2昇格候補（PROMOS 5件）に起点タグ・採用で該当起点のS3へ遷移（originKey）。
6. ✅完了 **S5マッチ品質ビュー**（REQ-KGA-15/16）: 「クエリ一致の内訳」セクション（`anMatch`＝完全一致/類語一致/包含一致/同一ページ着地/検索結果で確認済み/未マッチの積み上げバー＋凡例）・匿名化上限の別掲（推定12%・100%不可到達の明示）・未マッチ昇格導線（単体＋クラスタ集計を行単位で「昇格候補へ」登録、`state.anProm`）。
7. ✅完了 **PT-N W3構成案の型表示**（REQ-PACK-11/16）: 生成プレビューに「記事タイプ（解説ガイド）・見出し構成（課題→解決→手順型）」をカタログ由来ラベル＋「構成カタログ v3.2 から決定」チップで表示、「サンプル記事の型（文体学習）とは別物」を同一帯で明記。品質チェック各行に定義対応ツールチップ（`q.def`・ユーザー語彙）。
8. ✅完了 **W4/W6/S4操作の補完**: S4承認キューに選択チェック＋一括承認・一括差し戻し（理由必須・Role注記。`apSel`/`apBulkApprove`/`apBulkReject`）、W6に種別・状態・期間フィルタ（`whFilter`/`whfOk()`、CSVもフィルタ適用後）、S4予約の変更（プリセットモーダル）・取消（復元可。`schedOv`/`schedCan`）。
9. ✅完了 **PT-O 管理コンソール補完**: 未登録キーの「ラベル未登録」印（ADM-S4の`rewrite_longtail_batch`行でfail-visible。S8のラベルレジストリ追補で解消される連動付き）。ADM-S4のレーン別・バッチ失敗/SLA超過/フォールバック率は既存laneRowsで充足済みを確認。
10. ✅完了 **管理コンソール操作の補完**（台帳§1.5）: ADM-S1インシデント起票（incCreate）/経過記録/クローズ・アラートack、ADM-S8に表示ラベルレジストリ（lrAdd）・通知テンプレート編集（ntSave=draft）・office_layout JSON編集＋Validate＋公開（olValidate/olPublish）・旧版ロールバック（dRollback=新版として再公開draft）、ADM-S3手動rollback（理由必須）・モデル有効/無効切替（状態チップクリック→理由モーダル）。全操作がpushAuditで監査記録。
11. ✅完了 **S7補完**（REQ-BILL-07/08・REQ-SEC-10）: クレジット消費履歴（seLedger＝予約/確定/戻し/チャージ）・Stripe Customer Portal導線（sePortal）・解約/退会＝オフボーディング申請（2段確認・取り下げ可。seOffb*）・セキュリティカード（認証方式表示・SSO/2FAは拡張点注記）。
12. △運用で充足 **PT-Q UI文言外部化＋閲覧系モバイル**（REQ-NAV-06/07）: 新規・ロジック駆動文言は`LABELS`辞書経由で運用中（テンプレート静的文言の一括外部化はストリーミング制約とのトレードオフで見送り＝本実装時のi18n基盤で対応）。閲覧系モバイルは既存レスポンシブ（ドロワー・auto-fit grid・表の横スクロール・Office狭幅時の「通常ビューへ」誘導）で充足を確認。
13. ✅完了 **office_layout正本追従**: config v2.0.0＝2フロア・部屋7＋ハブ（1F=01〜04、2F=05〜07）。描画・エレベーターはconfig駆動のためコード変更なしで追従（PT-G実地検証）。**〔更新: 現行は v2.1.0＝7フロア（1部屋=1フロア＋ハブ）。正本 office_layout.initial.json も7フロアへ改訂済み＝v3.7.49。上記2フロア記述は当時の履歴〕**

上記は **v3.7.16〜22 時点のバックログを消化した状態（2026-07-03）**。その後 docs が v3.7.24〜48（2026-07-05）まで大幅に先行し、新画面・新REQの大きなギャップが発生。下記新バックログで管理する。

---

## バックログ（2026-07-05、v3.7.48突合。現行DCはv3.7〜22相当で、以下は未実装）

**着手前に必ず該当REQ原本（docs/）とPT観点を読む。grepで現行DCに未実装を確認済み。**

### ユーザー面
1. ✅完了 **W10 サポート（REQ-PRODUCT-22・PT-Y）**: AI一次応答パネル（根拠付回答・低確信時は捏造せず担当へ引き継ぎ提案）・問い合わせ作成/追跡・受付ID・回答の目安・ステータス自動案内。両モードのヘルプメニューに起動導線。`state.screen='support'`・supportVals()。
2. ✅完了 **S1 新タブ（REQ-PRODUCT-17・KGA-20）**: ダッシュボードを3タブ化（今日／プランニング／変動）。プランニング=月間目標と進捗（プログレスバー）・配分計画（カテゴリ別 新規/リライト推奨・トポロジー/価値スコア由来・取り込みボタン）・参考予測レンジ（保証しない誠実表記）・実績乖離＋要因。変動=急変検知（注意/好調/情報の3類型）・SERP変動/アルゴ更新＋リライト保留ガード・季節変動（シーズン前リフレッシュ候補）・ウォッチリスト変化。`state.s1Tab`・`s1Vals()`。表示状態トグル（検証用）は今日タブに残置。
3. ✅完了 **S2 新タブ（REQ-KGA-19/20）**: トポロジープランナー（幹枝葉ツリー・オーファン検知・リンク再調整キュー→承認へ）/ ウォッチリスト（ピン留め・順位/クリック/SERP変化・しきい値・継続下落でリライト導線）。`kwTab='topo'/'watch'`・`kwTopoWatchVals()`。
4. ✅完了 **S5 新タブ（RWR-08/09・WPA-11/13・KGA-21）**: 既存を「概要」タブ化し6タブ構成。リライトブリーフ / 好調記事分析（保護フラグ・滞在スクロール・波及リンク）/ フラッシュリライト（負残差・AIO切り分け・TDH案適用）/ CVポイント台帳（下記5）/ インデックス・技術。`anTab`・`s5Vals()`。
5. ✅完了 **CVポイント台帳（WPA-13）**: ★正本ではS6=学習ナレッジ管理でありCVポイント台帳ではない（旧CLAUDE.md誤記）。CROが属するS5にタブとして実装（カタログ登録・記事割当・CRO提案・健全性）。
6. ✅完了 **S7 補完（SRC-10・PRODUCT-21・SEC-16）**: 稼働時間帯/静穏窓カード・メール補助チャネルのバウンス自動停止/再有効化・全端末ログアウト（step-up確認）/招待失効・再送。`s7Vals()`。
7. ✅完了 **W4 承認対象拡張（KGA-19/RWR-08/09・WPA-12/13）**: `state.approvals`にリンク再調整・TDHフラッシュ・CVポイント差替の部分パッチ3種を追加（差分・理由つき、既存モーダル・一括承認で処理）。
8. ✅完了 **PT-Z アクセシビリティ（REQ-NAV-08）**: `:focus-visible`の可視リング（両テーマ・両DC）追加。reduced-motion停止・native buttonのキーボード到達は既存。

### 管理コンソール
9. ✅完了 **ADM-S12 サポートデスク（REQ-PRODUCT-22・PT-Y）**: チケットキュー（severity×プラン・SLA監視・担当）・AI会話要約・定型返信・なりすまし調査導線・FAQ還流・deflection/エスカレーション/SLA計測。`scr='support'`・`supportDeskVals()`。
10. ✅完了 **ADM-S1/S4/S9 拡張**: S4=負荷平準化ヒートマップ・キャパシティ/配賦原価・導出事実/サマリー省略効果、S1=メール送達性/DMARC・自動復旧/保守イベント、S9=オーナー回復・マスターテナント・事例許諾台帳。`admExpandVals()`。
11. ✅完了 **ADM-S8/S10 拡張（NAV-09・ADM-10）**: UIテキストレジストリ（ui.text.*・activate）・ゴールデン評価/回帰デルタ・段階ロールアウト（master→canary→一般・進行/ロールバック）。

### 方針・基盤
12. ◑ **UIテキスト外部化の本格化（NAV-09・PT-Q）**: ①管理側の**UIテキストレジストリ（ui.text.*・版activateで差し替え）はADM-S8/S10で実装済み**。②ユーザーDCの**ロジック側確定文言はLABELS辞書へ全面集約**（2026-07-05: 純粋リテラルのtoast＝`LABELS.msg.*`（計45件）、連結/分岐の動的文言＝`LABELS.fn.*`（関数14件）へ移行完了。ロジック側の`showToast`直書きリテラルはゼロ。以後ロジック新規文言は必ず`LABELS`経由）。③**テンプレート静的文言の外部化を全体化**（2026-07-05）＝`{{ }}`ホール化は初回ペイントとインライン編集を壊すため採らず、**ソース文字列キーのテキストノード走査**方式に格上げ（`TXT`レジストリ＝日本語源文字列→各版/言語訳。`applyI18n()`が`document.body`のテキストノードを走査し、該当ラベルのみ差し替え／`componentDidUpdate`で画面遷移後も再適用／JA復帰は`__i18nOrig`キャッシュから原文復元）。**テンプレート個別タグ不要でアプリ全体に効く**（既定=日本語はインラインのまま＝描画・編集無傷。エントリ追加だけで対象拡大）。レジストリが正本で版・言語切替可能（NAV-09「版activateで差し替え」）。初期シードは中核〜65語＋**全画面の実在ラベルを機械抽出して一括翻訳・登録（計約497語＝ナビ/タブ/ボタン/見出し/チップ/空・エラー状態/補助文）。テンプレートから`>静的JP</`を抽出→固有名詞・数値データ・断片を除外→英訳をレジストリに合流**。EN切替でダッシュボード〜各画面のラベル・見出し・本文補助が英語化、JA復帰で全復元・混入なし・エラーなし。長文補助文51件も機械抽出→翻訳→登録済み（計約541語＝短ラベル＋長文helper）。さらに属性テキスト（placeholder/title＝走査機構を属性対応に拡張）19件、正本ボキャブラリ（13工程ラベル・マッチ方法6＋説明・アサイン状態・ステータス・必須/推奨・起点/通知設定）65件を追加（計約625語）。固有名詞・数値・記事本文・キーワード語そのもの・都市名/月/順位などのデータは対象外（UIクロムではなくコンテンツのため）。Agent Officeモードの演出テキストも登録（部屋名/ペルソナ/統計/状態/おすすめ/WORK LOG/セリフ計105件＝約730語）。あ/ENトグルは通常＋オフィス両ヘッダーに設置（オフィスは1フロア表示のため現在の部屋のみDOM描画→フロア移動で順次英語化）。さらにツアー全ステップ・システム/通知メッセージ・品質チェック定義・ジョブ進行文・AIサポート応答など残りのUI表示文51件も登録（計約780語）。以降の未登録日本語は記事タイトル・キーワード語・URL・日付・人名・SERP構成/読者セグメントなどの**コンテンツ/データ**で、UI文言レジストリの対象外（機械翻訳しない）＝externalize可能範囲は完了。**英訳の対訳規約**（2026-07-05突合）: 同一日本語概念＝同一英語（承認=Approve/Approval、リライト=Rewrite、おまかせ=Auto、今すぐ=Now、クレジット=Credits等）、§6.5正本の軟化語と整合（参照する情報源=Reference sources・おまかせ運用=Auto operation・緊急停止=Emergency stop・孤立ページ=Orphan page）、内部英語ジャーゴン（QA/TDH/AIO/Pack/lane/Stage）は使わず製品語彙で訳す。以後の追加もこの規約に従う。**実証**: 通常ヘッダー あ/EN トグルで、ドロワーナビ7＋ダッシュボードタブ＋分析タブが ja⇄en、EN中に別画面へ遷移→その画面のタブも英語化、JA復帰で全復元・英語混入なし・コンソールエラーなし。**残りは機械作業**＝`TXT`にエントリを足すだけ（初回ペイント無影響）。以後の新規文言もこの方式。**MTフォールバック（2026-07-05）**: レジストリ未登録かつ「UI文らしい」文字列のみ `window.claude.complete` で翻訳→`localStorage['aos_mt_<loc>']`にキャッシュ→スワップ（`_mtFlush`／24件バッチ／glossary付きプロンプトで用語統一）。データは`_mtGuardOk`で除外（URL・パス・ドメイン・日付・件数・順位・%・空白区切り名詞列＝キーワード語・短い裸ラベル）。オンライン時のみ（`window.claude.complete`不在時は無害なno-op＝レジストリのみ）。レジストリ優先で既訳はブレず未登録UIだけ補完。実証: モックAPIで未登録UI文→翻訳/キャッシュ/スワップ、データ5種（キーワード/URL/件数/日付/都市名）はMT呼び出しゼロでJA維持・エラーなし。

優先: 全ユーザー面（1-8）＋管理面（9-11）＝✅完了（2026-07-05）。残: 12（UIテキスト全面外部化＝本実装のi18n基盤で対応）。ADM-S10のKill Switch本体・S6/W4のさらなる細目は必要時に追補。各着手前にREQ原本とPT観点を読む。

---

## セッション追記（2026-07-05 後半）— バックログ消化後の追加対応

- **S2 キーワードからの起動**（画面台帳S2・遷移図§3 `選択→生成起動→S3`）: キーワード詳細に「この語で記事を作成（今すぐ）」「おまかせで予約（夜間割安レーン）」を追加＝未割当時、割当済みは「リライトへ」。`kwMakeNew`/`kwReserveNew`/`kwGoRewrite`（state `kwSel*`）。**一覧の複数選択→一括おまかせ投入**（Preflight合算モーダル）: 行チェック（`kwPick`）＋「すべて選択」（`kwSelectAll`・常時表示ツールバー）＋合算見積モーダル（`kwBulkModal`。※state key `kwBulk`は既存の一括貼付文字列と衝突するため`kwBulkModal`に）。見積は新規72cr/リライト40crのキーワード別合算。
- **§6.5 全画面用語点検・是正**（ユーザー面）: 私の追加分に内部用語漏れがあり是正 → Preflight→「まとめて見積もり」、TDH→「タイトル・説明・見出し」、AIO→「AI検索の影響」（対訳表準拠）、部分パッチ→「一部だけ更新」、オーファン→「孤立ページ」。残存はコメント・変数名（`f.aio`等）・エンベロープ`et:`のみ＝§6.5許容。**以後、画面/文言追加のたびに§6.5対訳表突合を締め作業に含める**。CTR/SERP/intent/YMYLは業界用語として許可。
- **気配り**: 「残差」→「期待との差」、一括見積を固定係数から可変合算へ、キーワード一覧に「すべて選択」。
- **点検実施記録**: 全ユーザー画面（S1〜W10）・管理コンソール（ADM-S1〜S12）・Agent Office（A0〜A8）を各台帳で突合し必須能力充足を確認。大きな欠落なし。意図的逸脱はオフィス7フロア（ユーザー指示・config駆動）のみ、他は規約制約として文書化。モバイルOffice→通常ビュー誘導はREQ-NAV-06どおり、デモトーストはプロト範囲規定どおり（＝欠陥ではない）。

---

## セッション追記（2026-07-05 導線総見直し＝行き止まり/準備中スタブの一掃）

通常ビュー全画面を「気づき→行動」で踏破し、宣言だけで遷移しない導線・準備中トースト・表示のみのリストを実操作に置換（AC-NAV-05 / PT-P）。**以前の『デモトーストはプロト範囲＝欠陥ではない』の扱いは本セッションで上書き**＝ユーザー操作の主要スタブは実機能化。各追加は既存モーダル様式・`roGuard()`（なりすまし/閲覧ガード）・`LABELS.msg`辞書に準拠。

- **S1**: 「目標を設定」→実モーダル（月間目標）、「利用明細」→設定の請求へ。
- **S2**: トポロジー「コア記事・強化カテゴリ指定」→チェック選択モーダル（生成方針ノートに`優先:`反映）、ウォッチ「+ ピン留めを追加」→キーワード＋しきい値モーダルで監視行追加、マップのクラスタchip→一覧タブへ絞り込みジャンプ、ギャップのセル→空白象限は新規作成/既存象限は一覧、網羅率の未カバー例→「この主題で記事を作成」CTA。
- **S3**: 「承認待ちへ」の遷移先をダッシュボード→**承認キュー（オートメーション）**へ是正。
- **S4**: 唯一の欠落だった**おまかせ運用の同意ゲート**を追加（初回ON時に同意モーダル）。他は既存実装。
- **S5**: 「気づき→行動」6遷移を実着地（慎重リライト→コンテンツ作成、リンク強化/CRO承認へ送る/修正候補/差替/TDH適用→承認キュー）、CVポイント登録/編集・割当編集の準備中スタブ→実フォームモーダル（カタログ即時増減）。
- **S6**: NG表現の追加/削除、ユーザー要望・追加主張軸の削除（基本3件は固定）、「サンプル記事を差し替える」→貼付モーダル（サンプル数更新＝再推定の建て付け）、「サポートに相談」→W10。
- **S7**: 稼働時間帯／バッチ静穏窓の「変更」→プリセット選択モーダル（共通1つ・種別で内容切替）。他は既存実装。
- **W7**: 種別フィルタ（すべて/要対応/運営お知らせ）＋空表示、計画メンテナンス通知に「稼働状況」ページ導線。
- **W6**: 履歴行クリック→結果詳細モーダル→分析/リライトへ。
- **W10**: 自由入力コンポーザー（知識ベース照合・低確信時は担当引き継ぎ誘導）。
- **クレジット購入**: 支払い方法変更をお支払い管理ポータル導線に統一、完了ステップに領収書・お支払い管理リンク。

**通し確認**: サイドナビ7画面すべて到達・描画、コンソールエラーなし。**残る既知**＝Agent Officeビューのミニカード操作（通常ビューへ誘導する簡易表示。必要時に実操作化）。新規文言は全て`LABELS.msg`/`LABELS.fn`経由＝EN/MTフォールバックでも拾える。

---

# セッション引き継ぎメモ

## ファイル構成
- `AI Office de SEO.dc.html` — ユーザー面DC（全画面・全モーダル・ツアー内包）
- `Admin Console.dc.html` — 開発管理者コンソール（現行ADM-S1〜S12実装（S12サポートデスク=2026-07-05追加）。内部用語可＝ただしTERMS辞書に対訳を先に追加してから使う）
- **統合エントリ（2026-07-05）**: 派生・重複ファイル（offline src / .html / （オフライン版）/ standalone_src / screenshots）を削除。`Admin Console.dc.html` はユーザーDCのヘルプメニュー「開発コンソール（社内）」から `<dc-import name="Admin Console">` で子DCとして取り込む方式に統合（コード重複なし・`state.mode='admin'`は非永続＝リロードで通常面）。REQ-ADM-01の分離は維持（社内導線・戻るバー付き・admin側は内部用語のまま）。正本は引き続きこの2 DC
- `config/office_layout.json` — v2.1.0（7フロア・1部屋=1フロア＋ハブ＝ダッシュボード。ユーザー指示でゆとり重視に変更）。描画は完全にJSON駆動。フロア別背景＋色相はコード側FLOOR_BG/FLOOR_HUEで付与。personas[].mapping.stagesがキャラ状態導出の正
- `config/design-tokens.css` — Gate A-4凍結トークン（参照用正本。DCはインライン同値）
- `assets/` — webp素材（sign_01〜07・char_*・state_*・scene類）。`Design.md` — 追補1〜4。`reference/` — 参照モック（実装素材にしない）

## logicクラスの要点（Component）
- `state.mode` 'standard'|'office'（localStorage `aos_proto_mode`）、`state.screen`＋`state.wb`（固定オーバーレイ）
- モックイベント: `SCENARIO`（14ステップ＝全13工程周回＋WPエラー保留、`tick()` 4秒毎）。`job1Idx`=`stageIdxOf()`、キャラ状態=`derivePersonaStates()`。エンベロープは`mkEnv()`（aos.event.envelope.v1）→`state.envLog`
- renderVals分割: 本体＋`stdVals()`＋`buyVals()`＋`tourVals()`のスプレッド合成
- 部屋データ`ROOM_DATA`/`ROOM_LOGS`/`SPEECH`。残高`state.balance`。おすすめFB`state.recFb`（両モード共有）。`manualHold`中はtick停止
- 13工程は`STAGES`が正（REQ-AGENT-09準拠）。工程を参照する表示はすべて`STAGES`経由（文字列直書き禁止）

## ハマりどころ（再発防止）
- 動的画像srcはbackground-image必須（`&lt;img src="{{ }}"&gt;`は先読み404）
- officeフロア行は`min-height: min-content`（0だと低画面で重なる）
- 数値焼き込み素材（scene_05/08/ref_*）使用禁止。エレベーター焼き込み「2」はHTMLインジケータで被覆済み
- スタイルはインラインのみ・helmetは@keyframes/フォント/bodyリセットのみ
- 通常ビューは減彩度パレット、原色ネオンはOffice専用。WORK LOGにevent_typeタグを出さない（§6.5）

## 次セッションの入口
1. **全ユーザー面バックログ＋管理面＋i18n（外部化・約780語＋MTフォールバック）＝完了。全画面の導線見直し（準備中スタブ一掃）も完了**（2026-07-05）。次は必要に応じて Agent Officeミニカード操作の実化・管理コンソール細目（ADM-S10 Kill Switch本体等）・実データ永続化（本実装側）。各着手前に必ず原本（docs/）とPT観点を読む
2. 画面・タブ追加時は `state.screen`/`WB_MAP` に配線し、サイドメニュー両方（static＋drawer）とOffice部屋オプションにも足す
3. 新しい用語はまず `Design.md` §6.5 対訳表（ユーザー面）/ TERMS辞書（コンソール）へ追加してから使う


---

## バックログ（2026-07-06 正本v3.7.49照会の差し戻し。着手前に該当REQ原本を読む）

1. ~~**P1 event_type改名（契約適合・Gate A-1）**: `job.suspended` / `job.resumed` → `generation.job_suspended` / `generation.job_resumed`（mkEnv呼び出し2箇所。他11種はカタログ適合済み）。~~ **実装済み（2026-07 監査で確認）**: 現物は `generation.job_suspended` / `generation.job_resumed` を発行済み（旧名の残存なし）。
2. ~~**P2 PT-X②境界違反fixture**: 範囲外データ要求→fail-close拒否表示＋監査記録の再現デモ（REQ-SEC-07）。~~ **実装済み（2026-07）**: なりすまし（サポート閲覧）バナーに「範囲外データを要求（テスト）」を追加し、fail-close拒否モーダル（`boundary.access_denied` 監査記録つき）を再現。これでPT-X 7系すべて実装済み。
3. ~~**P3 CVポイント台帳の配置是正**: 正本はS6（戦略入力）に台帳（カタログ登録・記事割当・有効期間、REQ-WPA-13）、S5はCRO差し替え提案。S5タブの台帳機能をS6へ移設しS5には導線を残す（旧CLAUDE.mdの「S6=学習ナレッジ」は誤読）。~~ **実装済み（2026-07 監査で確認）**: S6「CVポイント台帳」タブに台帳、S5 CROタブは「CVポイントの管理へ→」導線のみ。
4. 正本v3.7.49の書き戻し済み事項（追加作業不要・参照のみ）: Design.md §6.4〜6.7取り込み・office_layout.initial.json=7フロア正本化（config v2.1.0と一致）・ASSET-MAPPING使用禁止注記・W8/W9実装形注記。

## セッション追記（2026-07-08）— 実装×書面の全量突合監査（S1〜S7・W1〜W10・A0〜A8）と是正

台帳（screen-inventory §0〜§5）と現物の全量突合を実施。是正内容:

- **書面→実装（操作欠落の実装。AC-NAV-05）**: S2 キーワード単体の分類変更（必須/推奨/オリジナル・詳細ヘッダーのチップで切替、`st.kwCls`）と削除（`st.kwRemoved`・割当非影響注記）／S2 除外候補の採用/却下（`DEMOTES`＋対策候補タブ「除外候補（価値低下）」、recFb系）／S5 内部リンク候補の個別採用/却下（`anLinkCands`・`st.linkFb`）／S7 タイムゾーン変更（`quietTzCycle`）／W5 ジョブ進捗モーダルにキャンセル（`wjCancel`・checkpoint注記）／W7 通知の個別既読（`n.onRead`・`st.ncReadIds`）／W9 登録時同意書（S7セキュリティタブ「利用同意書（登録時）」＋全文モーダル: 最終責任・YMYL・データ取り扱い。法務プレースホルダ注記）／新規UIラベル約65語のEN辞書追補（タブ名・W8ガバナンス・プラン・メンバー管理・事例許諾・ローカル順位・soft-disable等）。
- **実装→書面（台帳追従）**: S1 期間切替は「4週/12週/6ヶ月」で確定、S2 タブ名は実装ラベル（キーワード一覧/キーワードマップ/トピッククラスター/順位モニタリング/対策候補キーワード/キーワードマッピング）で確定（screen-inventory §1/§5）。
- **報告のみ（未対応の改善候補）**: S5概要タブの情報過密（9カード≒1.9画面分）／ヘッダー未読バッジが個別既読と非連動／§0の「探索軸」ラベル非明示（機能は分散実装済み）。
- **用語適正監査（同日追補）**: 内部語の軟化＝ツールチップの `checkpoint`/REQ-ID 除去・fail-close表記を「安全側で遮断/拒否」に（REQ-NAV-01）。GSC公式表記への統一＝GSC由来の表は「掲載順位/平均掲載順位」（順位モニタリング等のランクトラッカー系は「順位」のまま区別）。「月間Vol/Vol/検索量/検索ボリューム」→「月間検索数/検索数」に統一（CSV含む）。「Local Pack」→「ローカルパック」。おすすめカードは「CTR（クリック率）」表記。EN辞書8語追補。残置（任意）: オーガニック→自然検索・SERPの言い換え統一・noindex/canonicalの平易ツールチップ。
- **視覚品質（同日追補）**: 注記グレー `#7A8BA6`（コントラスト3.0〜3.5:1）→ `#64748B`（4.8:1）に全置換（221箇所。Gate A-4の品質床=WCAG AA目安に適合）。階層は維持（#16294B > #51637F > #64748B）。
- **視覚品質・第2弾（同日承認済み）**: アンバー`#B5893A`→`#8F6A25`(4.9:1)・グリーン`#339B7D`→`#1F7A5C`(5.3:1)・セカンダリ`#8496B0`/`#8A97AD`→`#64748B` を全置換。淡面rgba・濃色面用`#3ED598`は不変。用語第2弾＝「オーガニック」→「自然検索」・「SERP変動/構成」→「検索結果の変動/構成」・noindex/canonicalに平易補足。残置: `#D25B5B`(3.9)・`#7E6BD9`(4.2)はAA境界下（L4トークンで暗色変種を検討）。
- **UI表記「ジョブ」→「タスク」統一（同日・ユーザー指示）**: UI文字列64箇所とTXT辞書（キー＝JA文字列とEN値 Job→Task）を全置換。オフィスビューの「本日のタスク」と表記が揃った。コード識別子（jobPause/JOB_ROWS/job1Idx等）・イベント名（generation.job_*）・ID体系（J-####）・書面の「ジョブ」（REQ-AGENT-09の状態機械等）は不変＝内部用語として維持（screen-inventory §0に規約追記済み）。
- **ビジネス用語の平易化＋UI表記対応表の制定（同日・ユーザー指示）**: リライトブリーフ→リライトの指示書／アサイン→割当／バックログ→未割当（の一覧）／ガバナンス→自動変更の安全ルール／クールダウン→変更間隔／承認キュー→承認待ち／実行キュー→実行予定／レギュレーション→執筆ルール／エスカレーション→担当者への引き継ぎ／オプトアウト→参加を停止／ダイジェスト→まとめ通知。**正本は glossary（AOS-L2-GLOSSARY）末尾の「UI表記対応表」**（screen-inventory §0からも参照）。TXT辞書キーは同時置換済み・重複キー1件（承認待ち）を解消。
- **思いやりUI監査＋アンドゥ基盤（同日・ユーザー問い「他に見直すべき点は」）**: 最大の穴＝ワンクリック削除8箇所に確認もアンドゥもなし → `showUndoToast(msg, undoFn)`（6秒・トーストに元に戻すボタン）を新設し、NG表現・要望・執筆ルール・主張軸・メンバー・サイト・キーワード削除に配線（削除→元に戻す→「元に戻しました」まで実機確認）。Escキーで全モーダル/ドロップダウンを閉じる`onEsc`をグローバル登録（unmountで解除）。方針11として制定。
- **思いやりUI第2弾＝残提案3件も実装（同日・ユーザー指示「すべて対応で」）**: ①未保存変更ガード＝W8安全ルール・キーワード取得設定にダーティ検知（`_w8Dirty`/`_acqDirty`・基準値は保存時に`w8SavedVals`/`kwAcqSavedVals`へ記録）→保存ボタン横に「未保存の変更があります」チップ＋タブ離脱時に注意トースト（値は保持される旨を明記・ブロックしない）。②モーダルのフォーカス管理＝開いたら先頭ボタンへ自動フォーカス（`componentDidUpdate`+`_overlays()`）・Tabでモーダル内循環（フォーカストラップ、`onEsc`ハンドラに統合）。③一括承認の2段確認＝「選択中のn件を一括承認します。内容は確認済みですか？」ストリップ→承認を確定/やめる（Escでも解除）。すべて実機検証済み。
- **レスポンシブ是正（同日・ユーザー指摘「S2タブがはみ出る」）**: S1・S2のタブバーだけ `flex-wrap: wrap` が欠落していた（他画面のタブバーは適用済み。S2はシャドウ値 .07 の別文字列だったため置換漏れに注意）→ 2箇所修正し、`width: fit-content` の横並び行で flex-wrap 欠落ゼロを確認。768px/375px で全9画面の横はみ出しスキャン＝ゼロ（表は各自の overflow-x:auto 内スクロールで意図どおり）。タブは狭幅で複数行に折り返す。
- **REQ-WPA-14 第3弾＝本文プレビュー（同日・ユーザー指示「開いて本文をみれるように」）**: 各成果物行に「本文を見る」→ プレビューモーダル（`st.ctBody`・`mdOf(title)`のデモ本文＝実運用はVaultから復号表示・コピー/DL/閉じる同梱・保持中バッジ）。コピー/DLも`mdOf`に共通化。要求書§14「持ち出し」に表示（プレビュー）を追記・台帳S3行更新。
- **REQ-WPA-14 第2弾＝Output Vaultへ一般化（同日・ユーザー指示「失敗時だけでなく保持してコピペ/DL・停止/再開も」）**: 保持を全成果物に拡張（各行に「本文保持 〜M/D」表示・「本文をコピー」=clipboard・「ダウンロード（.md）」=Blob DL・期限切れ行はWP誘導note）。退避行に「自動再送を停止/再開」トグル（`st.wpVaultPaused`。停止中も保持期限は進行と注記）。要求書§14・REQ-PRODUCT-04例外注記・glossary・FEATURE-LIST・台帳S3行を同旨に更新。持ち出しは監査対象と要求に明記。
- **REQ-WPA-14 WP送信失敗のフェイルセーフ退避を新規要求化＋実装（同日・ユーザー指示「WPに送られなかったときのバックアップ。保持2週間」）**: 要求正本=wp-automation要求書§14（暗号化・既定14日=Config Registry・自動/手動再送・成功/期限で本文完全削除・監査対象）。REQ-PRODUCT-04に「唯一の例外」注記を追加。プロト=S3成果物一覧に「退避中（再送待ち）」行（失敗理由・退避期限7/21表示・「WordPressへ再送する」→`st.wpVaultRetried`でWP下書きに遷移＋退避削除トースト）＋フッター注記＋W7通知（要対応・「成果物へ」導線）。FEATURE-LIST・glossary UI表記対応表にも追記。
- **「1タブ=1目的」原則の制定とS7分割（同日・ユーザー制定）**: 原則の正本は screen-inventory §0＋本書方針10（メモリにも保存）。適用第1号として S7旧「サイト・取得」（メンバー管理＋取得設定＋稼働時間=3目的・1.7画面）を「サイト・メンバー」「データ取得・稼働」の2タブに分割（実務6タブ・各1画面前後に）。§5・既定タブ表記も更新済み。
- **S3「成果物・履歴」タブ新設（同日・ユーザー指摘「成果物や生成履歴が見れない」）**: `ctTabs` に output タブ追加。成果物一覧＝作成した記事4件（タイトル・URL・状態バッジ WP下書き/承認待ち/公開済み・QA結果・消費cr）＋操作（WordPressで開く=`wpOpen`トースト・品質チェックへ=progressタブ・リライトへ=createタブrewriteモード）。生成履歴＝直近4件の抜粋＋「タスク履歴をすべて見る」→W6。本文はWP正本のためメタのみ（REQ-PRODUCT-04）を画面上にも注記。screen-inventory §5 S3行を更新済み。
- **S2再設計・Office・レスポンシブ（同日・ユーザー指摘）**: ①S2タブ「キーワードマッピング」→「記事×キーワード」に改名し、全タブに役割キャプション（`kwTabDesc`: キーワード起点/記事起点/俯瞰/監視/受け入れ判断）を常設＝一覧・記事一覧・未割当の役割混同を解消。②記事一覧（旧・記事別）に**URL・クリック28日・平均掲載順位・最終更新**の記事単位サマリー列を追加（Article Summary契約準拠・min-width 940px横スクロール）。セクション名「記事一覧（記事×キーワード割当）」「未割当キーワード（記事化待ち）」に明確化。③Officeビュー: ヘッダーKPI 4枚にスパークライン追加（計算済み `k.spark` を流用・ダーク配色 #4A5F8C/#3ED598）、9px/8.5px文字を10〜10.5pxへ引き上げ（BGM装飾ラベルのみ9.5px）。KPIストリップは isNarrow で非表示＝仕様どおり。④レスポンシブ: 768px=全画面はみ出しゼロ✓・375px=ヘッダーが534px要求で全画面159pxはみ出し→ `@media (max-width:600px){ header { flex-wrap: wrap !important } }` で解消（全画面ゼロ・ドロワー動作✓）。
- **残課題の完了消化（同日・ユーザー指示「謎の余白/残課題を潰して」）**: ①S2の4カードグリッド（auto-fit 2列）で行高が揃い短カードの下に大きな空白が出る問題→ラッパーを `class="kw-masonry" style="columns: 2 340px"` の段組メーソンリーに変更（`<style>`に `.kw-masonry > section { break-inside: avoid; margin: 0 0 14px; }` 追加。S2の全タブに効く）。②AA境界下の赤 `#D25B5B`(3.9)→`#C2543F`(4.5)・紫 `#7E6BD9`(4.2)→`#6C59CE`(5.3) を全置換（94+49箇所。淡面rgba・ダーク用#9B7BFFは不変）。③2軸ラベル: `EXPLORE — じぶんで選ぶ` チップをS2キーワードマップ・S3新しく作る・S5検索実績に付与（RECOMMENDEDと対、§0規約充足）。残る未対応はタイポ5段凍結（L4）とID接頭辞T-化（要判断）のみ。
- **見せ方の是正（同日・情報設計監査）**: ①S5に「クエリ・マッチ品質」タブを新設し概要の過密を解消（§5台帳どおり。外枠sc-if=`anTabOvMatchOn`＋各セクションを`anSecOvOn`/`anSecMatchOn`でラップ＝セクション非移動）。②検索実績テーブルに列ソート（クリック/CTR/掲載順位、`st.anSort`）。③問題一覧に種別フィルタ（すべて/インデックス/技術、`st.idxFilter`）。④ヘッダー未読バッジを個別既読（ncReadIds）・全既読と連動化（ハードコード'4'を廃止。通知ポップオーバー開閉での自動全既読も廃止）。監査結果: 110字超の長文段落ゼロ＝折りたたみ不要、他テーブル（CVカタログ・配分計画・ローカル順位）は行数少でソート不要と判断。
- **ロジック側ハードコード文言の外部化（同日）**: インライン `showToast('…')` 残存4箇所（ctHold再実行/入力必須・作成キャンセル・マッピング保存/必須）と demotes/anLinkCands の fbText を `LABELS.msg` に集約（holdInstrReq/holdRerun/ctCancelled/kwPrimaryReq/kwMapSaved/demoteDone/demoteKept/linkFbDone/linkFbRejected）。インライン showToast はゼロ。テンプレート静的文言はストリーミング描画のためインライン維持（方針9どおり）。
- **L4ハンドオフ用の実装マップを新設**: `docs/design/ai-office-de-seo/L3-ui-prototype/ai-office-de-seo-prototype-implementation-map_v3.7.md`（AOS-L3-PROTO-IMPL-MAP。トークン実測⇔Gate A-4対応・vals×画面×stateマップ・頻出スタイル→コンポーネント候補10種・残課題）。プロト自体の共通化リファクタは行わない方針を明記。

## セッションノート（2026-07-08 実行継続の明示）
- ユーザー質問「停止できる？実行状況はどこ？画面遷移で中断されない？」→ 停止・状況表示は既存（ヘッダー進行ピル、S3進行中タブ、タスク詳細モーダルの一時停止/キャンセル、W1緊急停止）。欠けていた「画面を離れても実行は継続する」明示文言を2箇所に追加:
  - タスク詳細モーダル（wj）フッター左: 「この画面を閉じたり他の画面へ移動しても、実行は止まりません。…」
  - S3 進行中タブのヘッダー下: 「実行はAIオフィス側で進みます。画面を移動しても中断されません（進捗はヘッダーの進行バーから…）」
  - TXT.en 2件追加。実機検証済み（両文言表示・EN辞書あり）。
- 防御: wjVals の idx を STAGES 範囲にクランプ（不正な jobDetail 注入でも label 参照で落ちない）。

## セッションノート（2026-07-08 デモ省略の解消）
- 「デモのため省略」7箇所を棚卸し。画面内で再現できる3つを実装で解消:
  1. **step-up再認証**（全端末ログアウト）: トースト直行→「本人確認（再認証）」モーダル（Google再ログインのモック、アカウント表示＋「Googleで本人確認して実行」）。secReauth 状態。文言から「（デモでは省略）」を削除（TXT.enキーも同時リネーム）。
  2. **お支払い管理**: Stripe Portal 外部リンク（トースト）→ 画面内モーダル（VISA••••4242、請求書3件、領収書ボタンは実Blobダウンロード .txt＋LABELS.fn.receiptDl トースト）。sePortal / buyChangeCard の2導線とも接続。portalOpen 状態。_dlReceipt() メソッド追加。
  3. **稼働状況ページ**: トースト→ モーダル（4コンポーネントの稼働状態＋計画メンテ告知）。ヘルプの「稼働状況」ボタンと通知の導線の両方から開く。statusOpen 状態。
- 3状態とも onEsc のリセットリストに追加済み（Escで閉じる・フォーカストラップは既存機構が自動適用）。
- 不要になった LABELS.msg.portal / LABELS.helpStatus は削除。TXT.en 約25キー追加。
- 意図的に残した境界（外部サービスの実体）: WP編集画面を開く、ヘルプセンター専用サイト、サンプルCSV読込（デモ機能そのもの）、メール状態切替（デモスイッチ）、サイト切替時の共通サンプル数値（データ差し替えは大工事のため保留）。
- 検証済み: タグバランス全OK（sc-if 335/335, div 1412/1412）、3モーダルの開閉・導線・Esc・EN表示、再認証フロー（確認→再認証→実行トースト）、領収書ボタン3件描画。

## セッションノート（2026-07-08 S1「今日」タブの分割・改名）
- ユーザー指摘「『今日』が謎・詰みすぎ」→ 方針10（1タブ=1目的）でS1を4タブに再編:
  - **やること**（既定・key='today'のまま）: 今日のおすすめ施策＋承認待ち（＝ユーザーの行動）
  - **サマリー**（key='sum'）: KPIカード＋タスク一覧＋サイト成長サマリー＋クレジット残高（＝状態の把握）
  - プランニング / 変動 は従来どおり。Office Viewバナーは両タブ共通（ラップせず）。
- 実装: 外側sc-ifを s1TabHome（today||sum）に変更し、各セクションを per-section sc-if（s1TabToday / s1TabSum）でラップ（セクション移動なし・非破壊）。
- 追随修正: ツアーstepに s1 フィールド追加（tourGoがタブを自動切替。タスク/クレジットのstepでsum、おすすめ/承認でtoday）; 「タスク一覧で見る」「稼働中のタスクへ」→ 新val goDashboardTasks（s1Tab:'sum'）; 検索インデックスのJ-1024エントリ state に s1Tab:'sum'。
- TXT.en: 'やること': 'To do', 'サマリー': 'Summary' 追加（'今日'キーは温存）。
- screen-inventory §1 S1タブ行を更新。検証: 両タブの表示/非表示9項目、ツアーのタブ自動切替＋tourRect取得、EN表記、タスク導線ジャンプ、タグバランス（sc-if 340/340）すべてOK。

## セッションノート（2026-07-08 S2用語の是正: クラスター図・サイト構造ツリー）
- ユーザー指摘: 業界で「キーワードマップ」=記事×キーワード割当一覧のこと。俯瞰バブル図に使うのは誤解を招く。また「トピッククラスター」タブの実態はサイト構造（幹→枝→葉の階層）＝ツリーで、クラスタ（まとまり）とディレクトリ（階層）を混同していた。
- 改名: S2タブ「キーワードマップ」→**クラスター図**（EN: Cluster map）、「トピッククラスター」→**サイト構造ツリー**（EN: Site structure tree）。
- 用語の整理原則: **階層＝ツリー／キーワードのまとまり＝クラスタ**。「トピッククラスター」は設計概念（ピラー＋クラスタ記事）としてキャプション・トースト（topoCoreSaved等）でのみ使用。割当一覧は「記事×キーワード」タブが正。
- 連動修正: map内h2・ローディング文言・出典注記、Office部屋ホワイトボードの options と WB_MAP キー（'クラスター図'）、kwTabDesc 2件、TXT.en キー同時リネーム（旧 'キーワードマップ' 系エントリ削除）。
- 書面同期: screen-inventory S2行（旧称と改名理由を注記）、glossary UI表記対応表に2行追加。
- 検証: JA/EN両表記・旧名残ゼロ（コメント/概念用法を除く）・両タブ描画OK。

## セッションノート（2026-07-08 サイト構造ツリーをカテゴリ構造に是正）
- ユーザー指摘: サイト構造ツリーに入るべきはキーワードではなく**カテゴリ構造**。各カテゴリにプルダウンで主要キーワードを紐づける。反対にトピッククラスター（クラスター図）はキーワード間の関連性を見せるもの。
- topoTree を記事/キーワード行 → **カテゴリ行**に全面差し替え: 幹=トップカテゴリ（費用・相場 / 手続き・住所変更 / 荷造り・梱包 / 挨拶・マナー）、枝=サブカテゴリ（単身/家族）。各行 = カテゴリ名＋URLパス（mono）＋記事数（公開/作成予定）＋内部リンク状態。
- **「主要キーワード n語 ▼」プルダウン**: st.topoOpen（index→bool）でトグル。展開するとキーワードチップ（クリックで kwTab:'list' + kwSel にジャンプ＝一覧で選択表示）。kwCount は locale で「語」サフィックス切替。
- 凡例を 幹（トップカテゴリ）/ 枝（サブカテゴリ）に変更（葉チップ廃止→プルダウン説明文）。kwTabDesc: map=「キーワード起点 — キーワード同士の関連性で束ねたクラスタ…」/ topo=「サイト構造 — カテゴリのツリーに主要キーワードを紐づけ…」。
- バグ修正: status表示のsc-ifが n.orphan を両分岐で参照していた既存バグ → n.orphanOff を追加して負分岐を正しく制御（孤立チップと状態テキストの二重表示を解消）。
- TXT.en 約27キー追加（カテゴリ名・記事数・リンク状態・凡例・ボタン等）。書面: screen-inventory S2 / glossary UI表記対応表を更新。
- 検証: カテゴリツリー描画・プルダウン展開・チップ→一覧ジャンプ（kwSel設定）・孤立チップ単一表示・EN表示・タグバランス（sc-if 341/341）すべてOK。

## セッションノート（2026-07-08 S2: 表の切れ・記事一覧テーブル化・未割当の可視化）
- **順位モニタリングが途中で切れる件**: 原因は kw-masonry（columns:2）内にセクションがあり半幅（455px）＋min-width表の横スクロールで切れて見えていた。`kwMasonryCols` valを新設し、表が主役のタブ（watch/ledger）は columns:'1'、他は '2 340px' に動的切替（masonryのstyle属性にバインド）。watch=1040px幅で全列表示を確認。
- **記事一覧（記事×キーワード）をキーワード一覧ライクに**: クリック（28日）・掲載順位ヘッダーをソートボタン化（st.kwMapSort {key,dir}、▲▼マーク・アクティブ色）。数値パースは '2,140'/'3.2位' 対応。1カラム化で全幅表示。
- **未割当キーワードをキーワード一覧でわかるように**:
  1. S2上部KPIカード「未割当」をクリック可能に（→一覧タブ＋状態=未割当フィルタ。noteに誘導文言）。「対策候補」カードも対策候補タブへ。
  2. 状態フィルタチップに件数表示「未割当 3 / 割当済 7」（locale対応。onClickは元のステータス値で動作するようlabelのみ加工）。
  3. 記事×キーワードタブの未割当セクションに「キーワード一覧で絞り込む」ボタン（kwGoUnassigned）。
- 検証: watch/ledger全幅・ソート降順↔昇順・KPIカードクリック・チップ件数・絞り込み3/10件表示・タグバランス（button 410/410）OK。
- 注意: プレビューのレンダラーが2回無応答化（preview_eval/screenshot 30sタイムアウト）。サーバー再起動→数十秒待ちで自然復帰。コード起因ではなくハーネス問題。ポート8756が旧python.exeに掴まれることがある（preview_startのエラーメッセージ内PIDは既に死んでいることもある→再startで解決）。

## セッションノート（2026-07-08 S3タブ再編: 作成する/ライブ生成/カレンダー/生成履歴）
- ユーザーのIA提案どおりS3を4タブに再編: **作成する**（従来） / **ライブ生成**（旧: 進行中・品質チェック。key='progress'のまま） / **カレンダー**（新設 key='calendar'） / **生成履歴**（旧: 成果物・履歴。key='output'のまま — 通知onGo等の既存参照を壊さない）。
- カレンダータブ新設: 「公開・生成カレンダー」= 2026年7月の月グリッド（日曜始まり35セル、7/1=水でオフセット3、min-width 700pxで横スクロール対応）。イベントチップ色分け: 予約公開=青/おまかせ生成=紫/メンテ=アンバー/準備系=グレー。今日（7/8）バッジ。下に「直近の予定」リスト4件。「予約の変更はオートメーションへ」ボタン（scheduled_actionsがS4正である旨の導線）。
- 曜日ヘッダーは locale で JA/EN 切替（valsで生成、辞書に頼らない）。イベント・予定の文字列はTXT.enに約25キー追加。重複キー2件を検知して解消（"今日"は既存キー流用、"承認待ちへ"は既存 "To approvals" に統一）。
- 書面: screen-inventory §2 のS3行を4タブ構成に更新（S2行の旧タブ名も同時に是正）。
- 検証: 4タブ表示・35セル・今日バッジ・イベント・直近リスト・オートメーション遷移・生成履歴タブ（成果物+履歴）・EN表示・タグバランス（sc-if 343/343, section 90/90）すべてOK。

## 2026-07-08 クラスター図の実図化・サイト構造ツリー見出し是正
- 指摘: 「クラスター図とサイト構造ツリーは内容が合ってない」。クラスター図タブがチップ羅列で関連性の図になっておらず、topoタブの見出しが旧称「サイト構造プランナー」のままだった。
- クラスター図: ハブ&スポーク図に置換。vals計算部（kwFilteredの直前）に `kwNet` IIFE — 中心(380,225)に「引越し」、半径128に6クラスタハブ（60度刻み、クラスタ別カラー）、各ハブから半径118+26*(j%2)にKW葉（spread 46度、半径を交互にずらして重なり回避）。エッジは回転div（transform-origin: 0 50%、atan2で角度）。バインディングは kwNetEdges/kwNetHubs/kwNetLeaves。キャンバスは760x450固定＋overflow-x auto。
- 動作: ハブクリック=kwFilters.cluster設定+一覧へ / 葉クリック=kwSel+一覧へ / 未割当KWは破線アンバー（凡例あり）。旧チップ行は図の下のフッターに縮小して残置（絞り込み用）。kwMasonryColsに'map'を追加し1カラム化（図が半幅に潰れるため）。
- topo見出し「サイト構造プランナー」→「サイト構造ツリー」（TXT.enの旧キー削除。'サイト構造ツリー'は既存キーがあり重複注意）。データ文言の（サイト構造プランナーの…）も追随。
- TXT.en: 新サブタイトル・"引越し": "Moving" 追加。ハブの「N語」はvalsでlocale分岐（ENは数字のみ）。クラスタ名自体はデータ文字列なのでEN未訳（既存挙動と同じ）。
- 検証ノート: reload後は aos_proto_mode を再設定しないと Agent Office ビューで起動し図のDOMが見つからない。葉の重なりは getBoundingClientRect の矩形交差でゼロ確認。screen state が analytics 等へドリフトするので検証前に毎回 setState で戻すこと。
- 追記: サイト構造ツリー（topo）タブも2カラムだと窮屈との指摘で kwMasonryCols の1カラム対象に 'topo' を追加（1カラム対象: watch/ledger/map/topo）。

## 2026-07-08 IA再編: 検索流入分析を解体 → サイトページ管理を新設
- 動機（ユーザー指示）: リライトはキーワード由来だけでなく構造整理・CRO由来もある。キーワード管理=キーワード/クエリの2起点、サイトページ管理=ページ起点（本文は持たず圧縮サマリのみ=Article Summary契約）。「サチコ見ればええやん」回避のため検索流入分析という画面は解体。
- S2キーワード管理（5タブ）: キーワード一覧 / クラスター図 / **クエリ分析（新設・旧S5クエリ・マッチ品質を移設: クエリ一致内訳×2+AI検索の影響+未マッチ候補+クエリ変化）** / 順位モニタリング / 対策候補キーワード。topo/ledgerタブは削除（vals kwTabTopoOn/kwTabLedgerOnも削除、kwTabQueryOn追加）。
- S5サイトページ管理（screenキーは'analytics'のまま維持=参照安定の原則。8タブ・既定anTab='pages'）: ページ一覧（旧記事×キーワード: 記事一覧→見出し「ページ一覧（サマリー×キーワード割当）」+未割当+割当の健全性） / サイト構造ツリー（S2から移設） / 流入サマリー（旧: 概要） / リライトの指示書 / 好調記事分析 / フラッシュリライト / コンバージョン改善 / インデックス・技術。anTabDesc役割キャプション新設。
- DOM移設はawk 2パス（範囲抽出→削除+アンカー行で注入）。ラッパーsc-ifはインデント幅（14sp/12sp）で正規表現除去し、内側のsc-if（深いインデント）を巻き込まない。grid-column: 1 / -1 はmasonry脱出用だったので除去。
- 画面をまたぐようになった導線に screen/anTab を追加: kwGoUnassigned・topoのkwChips.onGo（screen:'keywords'を追加）、whDetailAnalytics/goAnalytics/volAlertGo（anTab:'overview'）、onRewrite（anTab:'brief'）、WB_MAPにanTab対応（if (m.anTab) next.anTab = m.anTab）、検索インデックスのarticle行はstate:{anTab:'overview'}。
- kwMasonryCols 1カラム対象は watch/map に縮小（topo/ledgerはS2から消えたため。S5側は元々1カラム縦積み）。
- TXT.en: 検索流入分析→サイトページ管理系のキーを一括更新（'Site Pages'）。キー改名は必ず旧キー削除とセット（「サイト構造ツリー」重複の前例）。anTabDesc/kwTabDescの新キャプション8+2件追加。
- 書面: screen-inventory §1 S2/S5行・§2タブ表・IA再編ノート（92行目）、glossary UI表記対応表に3行追加（検索流入分析→サイトページ管理 / クエリ・マッチ品質→クエリ分析 / 記事×キーワード→ページ一覧）。
- 追記: 「流入サマリー」タブは「ページ一覧」に統合（2026-07-08）。anTab 'overview' は廃止し全参照を 'pages' へ（anTabOvMatchOn/anSecOvOn は tab==='pages'）。S5は7タブ。

## 2026-07-08 S5リライト統合タブ（リンク調整/順位改善/コンバージョン改善/スモールリライト）
- S5を5タブに集約: ページ一覧 / サイト構造ツリー / **リライト** / 好調記事分析 / インデックス・技術。リライトタブ内に種別セレクタ（anRwKind: link/rank/cv/flash、既定rank）。
- 実装: brief/flash/cvのDOMは動かさず、sc-if条件だけ `tab==='rewrite' && rw==='...'` に変更（DOM移動は最小主義）。リンク調整のみ新規grid＝リンク再調整の候補（topoタブから移設）＋内部リンク・孤立ページ（pagesタブ右カラムから移設）。anTabDescはrewriteのとき種別でネスト切替。
- 導線: onRewrite→{anTab:'rewrite',anRwKind:'rank'}、WB_MAP リライト原因解析→rank / 内部リンク候補・孤立ページ検知→link（ハンドラに if (m.anRwKind) 追加）。
- 検証の教訓: setState後350msではanTabDescのテキストノード更新前に読むことがあり偽陽性の「キャプション固着」に見えた。600ms待ちで全種別正常を確認。vals関数は `window.__aos.s5Vals()` で直接呼んで検算できる。

## 2026-07-08 リライトタブの意味論修正（指示書=開始時の確認へ）
- 指摘: 「リライトの指示書」を常設タブで見せるのは変（指示書は開始時に生成される設定）。種別の正しい定義=順位改善（順位ギャップ）/CV改善（好調→下降で訴求軸調整）/スモールリライト（CTR＋不足の追加）。
- 順位改善: anRankRows候補リスト（現在順位→目標・落としたクエリ数・根拠note）を新設。「開始（指示書を確認）」→ st.anBriefOpen=true で既存の指示書DOMを「開始前の確認」ヘッダ（←候補一覧へ/この指示書でリライトを開始）付きで表示。anRankListOn/anBriefOpenOnで排他。種別・タブ切替とbriefStartでanBriefOpen:falseにリセット。
- CV改善: CVトレンドセクション新設（anCvTrendRows: CV 28日件数＋前期・下降率チップ・原因・調整案+訴求軸の調整を開始→goContent）。既存CRO提案はその下に維持。
- スモールリライト: フラッシュ（TDH/CTR）の下に「不足コンテンツの追加候補」新設（anGapAddRows: 見出し追加/FAQ追加/段落追記チップ＋根拠＋追加を開始）。
- anTabDescのrank/cv/flashキャプションを新定義に更新（EN辞書キーも改名）。旧キー3つは削除済み。
- 注意: 「開始前の確認」を部分文字列で検証すると候補リストのフッター（「開始前の確認画面で調整できます」）に誤ヒットする。検証マーカーは「開始前の確認 — リライトの指示書」を使う。

## 2026-07-08 S5第3ラウンド: 統合テーブル・上位ベンチマーク・移設2件
- ページ一覧: kwMappingテーブルにCTR/クエリ一致（高中低ピル）/AI検索の影響 列を追加し「サマリー×キーワード割当×検索実績」の統合テーブル化（min-width 1180px・10列）。旧「検索実績（ページ×クエリ）」セクションは削除し、表示状態5類型（dispLoading/Precalc/Empty/Error/Normal）を統合テーブル側に移植（REQ-SEC-06のデモを喪失させないため）。未割当キーワードのセクションは廃止（キーワード一覧で確認、フッターに案内文）。RECOMMENDED・改善前後比較はpagesグリッド末尾に残置。旧anTabOvMatchOnラッパー（2カラムflex）は削除済みだがvals anTabOvMatchOn/anSecOvOn/anTabOverviewOnは残存（未参照・無害）→次回掃除可。
- ローカル順位（地域別）→ S2順位モニタリング（watch）タブ末尾へ移設（anLocal valはグローバルなのでそのまま動く）。
- 好調記事分析 → S6学習ナレッジ管理へ移設（knTabsに winner 追加・sc-if binding anTabWinnerOn→knTabWinnerOn。winnerRows/winnerNote valsはグローバル）。
- 上位ベンチマーク（bench）タブ新設: anBenchRows=狙う記事×クエリ×現在順位×上位1〜3の共通条件×自ページ差分×リライトへ/記事を作成（未作成記事の単独記事化候補も1行）。週次更新・順位非保証の注記つき。
- インデックス・技術 → タブ名「テクニカル分析」（キーは'index'のまま）。
- kwMapping base に ctr/match/ai/clicksDelta を追加、matchStyle高中低で配色。

## 2026-07-08 配線監査・残骸掃除
- 残骸vals削除: anTabOverviewOn / anTabOvMatchOn / anSecOvOn（旧2カラムflexラッパー用、DOM参照なし）。
- クエリ分析タブ内の自画面遷移を修正: 「価値スコアを確認」→ kwGoList（kwTab:'list'へタブ切替、ラベル「価値スコアを確認（キーワード一覧）」）、「キーワード管理で見る」→ kwGoUnassigned（「未割当の一覧で見る」、未割当フィルタ付き）。footer文言の「キーワードマッピングの未割当の一覧」→「未割当としてキーワード一覧に」。
- 廃止コンテンツの辞書キー掃除: 'インデックス・技術'・"クエリ・マッチ品質"・旧footer文・"キーワード管理で見る"。重複コメント（S5 好調記事分析）削除。
- 検証: 全導線マーカー確認済み。注意: document.body.innerText はこのレンダラで~1400字程度にしかならない（overflow内は含まれない模様）。「文字数>閾値」での存在判定は偽陰性になる — 必ず includes(マーカー文字列) で判定する。

## 2026-07-08 ページ一覧のスリム化（3セクション整理）
- 指摘: ページ一覧の割当の健全性・RECOMMENDED・改善前後比較は妥当でない。
- 割当の健全性: 削除（未割当=S2一覧が正・二重割当=テーブル状態ピルで既出・孤立ページ=構造ツリーの責務）。kwHealth vals削除。
- RECOMMENDED — おすすめ: 削除（おすすめ軸はリライトタブの候補リスト群が担う）。anRecommend / recVals('anRec',...) 削除。
- 改善前後比較: S6好調記事分析タブへ移設（リライトの効果測定=勝ち筋の学習）。anBefore valはグローバルのままでOK。
- ページ一覧タブ＝統合テーブル1本＋フッター案内のみの純粋な管理表になった。

## 2026-07-08 スモールリライトの1リスト統合
- 指摘: 不足コンテンツの追加はそもそもフラッシュリライト（スモールリライト）の中身であり、別セクションはおかしい。
- 「フラッシュリライト候補」表＋「不足コンテンツの追加候補」の2セクションを、単一のカードリスト「スモールリライトの候補」に統合。種別チップ: タイトル差し替え（青）＝TDH、見出し追加/FAQ追加/段落追記（緑）＝不足追記。AIO行は「AI検索の影響 ・ 別扱い」チップでボタン非表示（actOn:false）。
- vals: flashRows/anGapAddRows を廃止し anSmallRows（flash配列とgap配列をspreadで結合、sigはlocale分岐で合成）。タイトル等の案ピッカー（anFlashPick/flashProposals）は従来どおり接続。
- 事故と教訓: sedの範囲削除で flashRows の閉じ `})),` が残り構文破壊しかけた／DOM置換時に anTabFlashOn の閉じ `</sc-if>` を1つ失いタグバランスで検出→復旧。複数行valsの削除はsed範囲でなくEditツールで正確にやること。
- 検証時の注意: ピッカー開閉直後にチップ数を数えると一時的に2倍に見える（アニメ中の残骸？）。安定後に再計測すると正しい。

## 2026-07-08 流入クエリ/流入ページタブ・カテゴリーツリー改名・KPIカード縮小
- S2に「流入クエリ」タブ（key 'inq'）新設: anRows（旧・検索実績のURL×クエリデータ）をクエリ起点の表として再利用（anSortClicks/Ctr/Rankのソートもそのまま接続）。kwMasonryCols 1カラム対象に'inq'追加。
- S5に「流入ページ」タブ（key 'traffic'）新設: anPageTraffic（5行デモ: クリック28日・前期比±・CTR・掲載順位・主要クエリ・CV）。フッターで「下降ページはリライトタブへ自動」「クエリ内訳は流入クエリへ」の相互導線を明記。
- サイト構造ツリー → カテゴリーツリー に改名（タブラベル・h2・辞書キー'カテゴリーツリー': 'Category tree'・topoキャプション「カテゴリ構造 —」・データ文言・glossary）。tabキーは'topo'のまま。
- 上部KPIカード縮小: minmax(215→148px)・gap 10→8・padding 12x15→9x12・数値23px→17px（S1サマリー/S5/テクニカルKPIの共通スタイル一括）。5枚が1行に収まるように。
- sedの教訓: パターン内に `||` があると `s|…|…|` の区切りと衝突して "unknown option to s" になる（kwMasonryColsで発生）→ Editツールで対応。辞書追加後は必ず重複キーgrep（"主要クエリ"・"カテゴリ構造 —"で重複発生→削除）。

## 2026-07-08 S1タブ改名: やること→ピックアップ / 変動→モニタリング
- ラベルのみ変更（keyは today/vol のまま＝TOUR_STEPSのs1指定・検索インデックス・goDashboardTasks等の参照は無傷）。TXT.enは 'ピックアップ': 'Picks' / 'モニタリング': 'Monitoring' にキー改名（旧キー削除済み）。

## 2026-07-08 S4再編: おまかせ/緊急停止の分離・夜間チェック廃止→予算管理/目標管理
- auTabs 6本: 承認 / 予約 / おまかせ運用 / 予算管理 / 目標管理 / 緊急停止。
- おまかせ運用（auto）: RECOMMENDED＋トグル＋接続と同期のみに。W8安全ルール（変更予算・クールダウン・振動検知・保留候補）は「予算管理」（budget）へ移動し、先頭にクレジット月間予算カード（480/1,200cr・80%通知・100%でおまかせ停止）を新設。w8Dirty未保存ガードの発火条件を auTab==='auto' → 'budget' に変更。
- 目標管理（goal）: auGoalRows（新規記事/リライト/クリック/CVの月間目標×進捗バー）＋「おまかせはこの目標と予算の範囲で動く・正本はS1プランニング」note＋goDashboardPlan導線（screen:'dashboard', s1Tab:'plan'）。
- 夜間チェックタブ廃止: auTabNightOn/auNight vals削除・辞書キー（夜間チェック/夜間チェック実行予定）削除。承認タブの「夜間チェックの完了後に…」文言と作業員ステータス文字列はプロセス表現として残置。
- 緊急停止（kill）: 旧autoタブ末尾のstopAllセクションを独立タブ化（binding auTabAutoOn→auTabKillOn）。
- 検証注意: タブバー自体に「緊急停止」ラベルがあるため、innerText全文でのタブ内容判定は誤検知する。セクションh2やボタン存在で判定すること。

## 2026-07-08 S6「好調記事分析」→「成功学習」（何を学ぶかの明示）
- タブ名改名（key 'winner'維持）。先頭に「学んだ勝ちパターン」セクション新設: knWinLearn=[what（構成/訴求/内部リンクの学び）, ev（根拠の実測値）, apply（反映先: 生成の型/タイトル案/リンク優先度）, state（反映済み/学習中）]。
- 好調記事セクションの見出しを「好調記事（保護対象・学習元）」に変更し、学習の元データであることを明示。フッターに「誤った学習は保護を外して再学習」の運用note。
- EN: 'Success learning' / 'Winning patterns learned' ほか。

## 2026-07-08 ナレッジ管理改名・同意文の上限連動・オートチャージ新設
- 学習ナレッジ管理 → ナレッジ管理（全置換: サイドメニュー×2・ドロワー・h1・data-screen-label・ツアーSCREENS・辞書キー・コメント。screenキー'knowledge'不変）。
- おまかせ同意モーダル（consentOpen）: 「クレジットの消費は月間の上限（1,200 cr）を超えません。」→「オートメーション「予算管理」で設定した月間上限に準じます（現在: 1,200 cr）。」（ハードコード値ではなく設定への参照に。辞書キーも改名）。settings→セキュリティタブ「内容を確認」から開く（state: consentOpen。automation側のconsentOnはこのstateのval）。
- S7お支払いタブに「オートチャージ」セクション新設: トグル（state autoCharge・roGuardつき）＋ON時詳細（しきい値300cr未満/購入500cr/月2回上限/メール通知・Owner限定）。valsは acDetailOn/acBg/acKnob/acToggle（autoToggleのパターン踏襲）。
- 教訓（再発）: TXT.enはダブルクォート区画とシングルクォート区画が混在。sedのアンカーは実際の引用符スタイルをgrepで確認してから書くこと（「しきい値」キーが一度サイレントに追加失敗→EN検証で検出）。

## 2026-07-08 自動公開（承認なし公開）オプション新設 — 方針変更
- 従来の「公開前承認: 必須（変更できません）」を撤回し、おまかせ運用タブに「自動公開（承認なしで公開）」トグルを新設（state autoPublish・既定OFF・roGuard）。ONで品質チェック合格記事を承認なしで公開。トグルはアンバー配色（通常の青と区別）＋ON中は警告ボックス常設（例外: 不合格/YMYL近接/保護記事の変更は承認必須、緊急停止で停止可）。
- 同意モーダル文言を改訂: 「どの設定でも承認なしには行われません」→「既定では承認後。自動公開ONの場合のみ承認なしで公開」。<b>で分割されたテキストノードのため辞書キーは3断片で登録。
- これはL1要件（公開前承認必須）からの意図的な方針変更（ユーザー決定）。screen-inventory S4行に記録済み。L1本文の改訂はL1更新時にまとめて行うこと。
- 検証ノート: preview_screenshot がpreview_evalと異なる画面状態を写すことがある（設定画面のタブが撮影ごとに変わる等・原因未特定）。機能検証はDOMのincludes判定を正とし、スクリーンショットは補助と割り切る。

## 2026-07-08 S7設定の再編（5タブ）＋1サイト1課金
- seTabs: 組織設定(site) / データ設定(acq) / お支払い・通知(billing) / セキュリティ(security) / その他(other・新キー)。link/notifキーは廃止（DOMはsc-if再ゲートで移動: 連携→acq、通知→billing、稼働時間帯・静穏窓→site）。
- securityブロックを分割: セキュリティタブ=認証・全端末ログアウトのみ。表示言語/おまかせ同意/利用同意書/データ利用/事例掲載→その他。解約・退会はお支払いから→その他の末尾へ（awkで範囲cut＆挿入、sc-ifの開閉を3対で組み替え）。
- 1サイト1課金: 「＋サイトを追加（プラン追加）」にボタン改名、対象サイト一覧に「1サイト＝1プラン（サイトごとに課金）」注記、サイト追加モーダルにアンバーの課金注記（プラン+1・次回請求から）。
- サイト追加モーダルのstateは siteAdd（オブジェクト）であり siteAddOn は派生val。モーダルを開く検証は setState({siteAdd:{name:'',url:''}}) で行うこと。

## 2026-07-08 プランとクレジットの分離（お支払い・通知タブ）
- 指摘: クレジットチャージとプランが混在。seBills（クレジット・請求のサマリー行）に契約プラン/次回更新が入り込み、チャージ履歴の「スタンダードパック +1,200cr」がプラン月間付与1,200crと同名で紛らわしかった。
- 契約プラン: プラン属性を集約（月間クレジット付与 1,200cr/月・次回更新 8/1・対象サイト=1サイト1プラン）。
- クレジット・請求: seBillsをクレジット専用に（今月の付与（プランから・7/1）/追加チャージ（クレジットパック）+0/今月の消費/先月の消費/不足見込み）。冒頭に「残高＝プランの毎月付与＋チャージの合算。プランの変更は上の契約プランへ」の説明文。
- 名称分離: 「スタンダードパック購入」→「クレジットパック購入（追加チャージ）」（消費履歴・アクティビティの2箇所）。プラン名を含むパック名は付与と購入の混同を招くため禁止。

## 2026-07-08 レイアウト/文言/データ整合の総点検（全画面巡回）
- 全S1〜S7+S3を preview で巡回し、書面（screen-inventory/glossary）と突合して一括修正。
- **URL統一**: `/moving/` → `/hikkoshi/`（23箇所）。`greeting-gift`→`aisatsu-gift`（同一ページの二重スラッグ）、`/hikkoshi-aisatsu-gift`（スラッシュ欠け）も修正。
- **REQ-ID非表示**: UI注記の「（`REQ-XXX-nn`）」を全削除（KGA-22/WPA-14/PRODUCT-18/08/23/SEC-07/08）。方針を glossary UI表記対応表に制定。
- **S4見出し**: 「公開前の承認は常に〜」→「既定で〜（「自動公開」ONの対象を除く）」（自動公開方針と整合）。
- **クレジット正本化**: 今月消費392cr/月間付与1,200cr（S1カード 2,760/4,000→392/1,200、S4予算 480→392・40%→33%）。S7請求と一致し残高1,240の算数が合う（432+1,200-392）。
- **CV正本化**: actCv 248→138 / gCv 320→150（S1プランニング＝S4目標管理）。
- **順位正規化**: 見積もり比較=11位（watch/vol/inq/kw一覧）、挨拶ギフト inq 15→14位、TDH行 6位→14位・基線2.6%。
- **キーワード2語分離**: 既存ページ tanshin-souba のプライマリを「引越し 単身 相場」に（pages/inq/traffic/rewrite/bench/briefTarget/single-cost承認行）。「単身 引越し 相場」は未割当・新規候補専用（S3/ピックアップ/対策候補/watch 15位）。急変検知-34%→リライトの物語と整合。
- **J-1024=リライト**に変更（旧「新規」は既存ページと同名タイトルで矛盾）。JOB_ROWS/jobDetail×2/ledger/activity/検索インデックスの6箇所。
- **タブ順序**: S2 流入クエリ→クエリ分析、S5 ページ一覧→流入ページ→カテゴリーツリー（キー不変・sc-if配線変更なし）。
- **レイアウト**: 緊急停止タブに影響範囲4行を追加（過疎解消）。watchのリライトボタン折返し→nowrap。S1予測レンジ 20px→17px+nowrap+単位分離。S2未割当KPI注記短縮。
- **S7組織設定**: サイトピル 主サイト/サブサイト→「Standard ・ 表示中」/「Standard」、削除→「解約して外す」（tooltip でプラン解約を明示）。
- **表記統一**: cl/週→クリック/週、「学習ナレッジへ」→「ナレッジ管理へ」、「おまかせ（夜間）」→「おまかせ（夜間割安）」。通知の順位表記 12位→8位圏 を 11位→5位圏 に（リライトタブと一致）。
- EN辞書: キー改名は sed 一括（JA本文と辞書キーが同時置換で同期）。新規13キー追加。重複キーは既知の11件のみ（増加なし）。
- 検証: JA/EN 全マーカー includes で確認・console エラーなし・タグバランス clean。preview_screenshot は間欠タイムアウトあり（DOM検証を正とする）。
- 教訓: python の replace スクリプトは**全 rep 成功後に書き込み**なので途中 assert 失敗なら無変更で安全（sed より安心）。件数指定 rep(old,new,cnt) が事故防止に有効。

## 2026-07-09 通常ビュー⇄Officeビューの役割切り分けと画面配線（/goal）
- ユーザー指示: 「画面遷移を全体で見直して通常ビューとOfficeビューを上手に行き来する画面配線を。通常ビュー=選択の基盤、Officeビュー=実行状況の確認基盤と切り分けると効果的」。
- **概念の正本化**: 通常ビュー=**選択の基盤**（選ぶ・決める・承認・設定＝全操作面）／Officeビュー=**実行状況の確認基盤**（動いているタスク＝チームの働きぶりを見守る監視面。決める操作は持たない）。従来の「同じ中身・別スキン」訴求を役割の対比に置換。
- **共有val新設** `goOfficeWatch`（main return、goStandardの隣）: `this.setState({jobDetail:null}); this.setMode('office')`。開いているタスクモーダルを閉じてからOfficeへ。setMode は screen を保持するので往復で元画面に戻る（`shellOn/stdChromeOn` 参照）。
- **通常→Office「見守る」導線（実行文脈の3点）**:
  1. タスク詳細モーダル（jobDetail）フッター: 「オフィスで見守る」ボタン（濃色=クロスビュー識別）→ `goOfficeWatch`。モーダルはグローバルオーバーレイ（`st.jobDetail`）なので必ずクリアしてから遷移すること。
  2. S3ライブ生成タブ 注記「実行はAIオフィス側で進みます…」を flexラップ span 化し「オフィスで見守る」ピルボタン→ `goOffice`。注記本文は単一 `<span>` のまま保持（applyI18n のテキストノード走査を壊さない）。
  3. S3作成完了バナー（ctCreatedOn）: 「タスク一覧で見る」の隣に「オフィスで見守る」→ `goOffice`。
- **Office→通常「選ぶ・決める」導線**: HUBカード（A0俯瞰の左下）を「同じ中身を通常ビューで見る」→「通常ビューで選ぶ・決める」に、キャプション「キーワード選定・承認・設定など、選ぶ・決める操作は通常ビューで。」を追加。既存の部屋→ワークベンチ（`openWb`/`WB_MAP`=部屋で「何を見ますか？」→通常画面をオーバーレイ）は「決めるために通常画面を開く」動作として維持。
- **S1 Office誘導カード** 再フレーミング: 見出し「実行状況をオフィスで見守る」（`<br>`除去して単一ノード化＝i18n容易化）、サブ「動いているタスクを、チームの働きぶりで確認。選ぶ・決めるはこの通常ビューで。」。
- **ツアー**（TOUR_STEPS office-btn ・ TOUR_STEPS_OFFICE 俯瞰/サイドメニュー）を同じ切り分けの言い回しに更新。office-btnステップは title も「Agent Office（実行状況の確認）」に。
- **EN辞書**: 新規9キー（見出し/サブ/HUBキャプション/HUBボタン/オフィスで見守る/office-btn title/ツアー3文）を `"同じ中身を、たのしい入り口で。"` の直後に追加。旧文言のキーは孤児化するが無害（重複キー増なし）。
- **検証（preview 実機）**: タスクモーダル→「オフィスで見守る」→mode=office・jobDetail=null・モーダル消滅・HUBに「通常ビューで選ぶ・決める」表示 → HUBボタンで mode=standard・screen=dashboard保持（往復ループ成立）。S3注記/バナーの導線・EN（S1カード/モーダル/HUB）を includes 判定で確認。console エラーなし。タグバランス clean（sc-if 343/343・button 418/418・div 1486/1486）。
- 書面同期: screen-inventory §3冒頭に役割注記＋§5末尾に2026-07-09追記、glossary UI表記対応表に2行（通常ビュー/Officeビューの役割）。
- 検証の再現メモ: preview がディレクトリ一覧（`http://localhost:8756/`）に居ることがある→ `location.href` を `/AI%20Office%20de%20SEO.dc.html` に明示ナビゲート。`#dc-root` の `__reactContainer*` から fiber をスタック探索して `stateNode.logic`→`window.__aos`。ロケール切替は `setState({locale})` 後 `applyI18n()` を明示呼び。

## 2026-07-09 Officeビューを深堀り・詳細化の基盤へ格上げ（/goal 続き・方針転換）
- ユーザーの補足指示（AskUserQuestionへの回答）: 「対称性は"Officeの操作を削る"のではなく、Officeに通常ビューには無い"深さ"を持たせて差別化」。**通常ビューは再編しない・既存導線は残す**。Officeで「より細かく・詳しく見れる」ようにする。例＝キーワードの詳細データ／記事生成のライブ編集状況。
- **背景**: 前ラウンドの指摘「両ビューで単純に同じ操作の配線」に対し、真因は Office が通常画面を `openWb`/`WB_MAP` でそのまま開くリスキン入口だったこと。今回は Office 部屋詳細（`roomView`）に **Office固有の深堀りビュー**を追加して差別化。
- **新メソッド `deepVals()`**（wjValsの直後・main returnに `...this.deepVals()` をspread）: `st.roomView` で出し分け。
  1. **ライブ編集ビュー**（`deepLiveOn = roomView==='content_creation'`）: 生成中の記事を5セクションで表示し、**`job1Idx` 連動**で `Math.floor(((idx+1)/13)*5)` セクションが完了→現在節が「執筆中」＋点滅カーソル→残りは待機（ゴーストバー）。文字数は 420→2,240 に増加、品質チェック4項目は idx>=8/9/10/11 で ✓ に。SCENARIO tick（4秒）で自動進行。検算: `window.__aos.deepVals().docSections.map(s=>s.stateLabel)` で idx別の状態を確認済み（idx1=執筆中/待機×4、idx6=完了×2/執筆中、idx11=完了×4/執筆中、idx12=完了×5・2,240字・QA全✓）。
  2. **キーワード詳細インスペクタ**（`deepKwOn = roomView==='keyword_research'`）: 引越し 相場の 月間検索数/価値スコア/掲載順位/割当ページ＋価値スコア内訳バー（需要/競合の緩さ/AI検索の余地/CV貢献）＋掲載順位の推移スパークライン（polyline・22→11位）＋検索結果の構成＋関連クエリchips。
- DOM は部屋詳細右カラム（`flex 1 1 300px; max-width 400px`・overflow-y auto）の先頭2ブロック（stats/WORK LOG/オプション/RECOMMENDED の上）。max-height 232px の内部スクロールでライブ本文を収める。
- **深堀り導線 val**: `goOfficeLive`（jobDetailクリア→setMode('office')→officeNav('content_creation')）／`goOfficeKwDetail`（setMode→officeNav('keyword_research')）。標準側の入口: S3ライブ生成注記のボタンを「オフィスで見守る」→「**オフィスでライブ編集を見る**」に変更し `goOfficeLive` へ／S2キーワード詳細ヘッダーに「**オフィスで詳しく見る**」（濃色ピル）→ `goOfficeKwDetail`。タスクモーダル/S3バナーの「オフィスで見守る」（overview=`goOfficeWatch`/`goOffice`）は据え置き。
- **EN辞書**: chrome 13キー追加（ライブ編集見出し/現在の工程:/文字数:/品質チェック（自動）/価値スコアの内訳/掲載順位の推移/検索結果の構成/関連クエリ/価値スコア/現在の掲載順位/割当ページ/オフィスでライブ編集を見る/オフィスで詳しく見る）。**記事本文・セクション文・キーワードデータ（dkTerm/dkSerp/dkRelated/dkTrendNote/stateLabel）は規約どおりEN対象外**（コンテンツ/データ）。`applyI18n` はテキストノードを `trim()` してキー照合＋前後空白を再付与するため「現在の工程:」等は末尾スペース無しで登録（`_i18nSwapAll` L6776/6784確認）。
- **検証**: 両部屋の深堀りビュー描画（JA/EN）、`deepVals()` の idx別セクション状態、標準→Office導線（S3ボタン→content_creation・S2ボタン→keyword_research）、スクショ2枚（ライブ編集＝節が埋まる様子＋QA／キーワード詳細＝スコアバー・順位推移・SERP構成・関連クエリ）。console エラーなし。タグバランス clean（sc-if 347/347・button 419/419・div 1522/1522・polyline 3/3）。
- 書面: screen-inventory §3 役割注記＋A1/A2行に深堀りビュー、glossary Officeビュー行を「見守る・深堀り」に更新。
- 残（任意）: 他部屋（レポート分析・自動化・ナレッジ・設定・テクニカル）の深堀りビューは未実装＝順次拡張可。ライブ編集の文字数/カーソルは job1Idx 連動＋CSS のみ（専用タイマー無し＝低リスク）。
- **⚠️ 本節のデータパネル実装は同日ユーザーに却下され、次節「Office詳細ページ」形式に置き換えた（パネル2枚は撤去済み）。本節は経緯記録として残す。**

## 2026-07-09 Office詳細ページへ改修（深堀りパネル却下→内部メニュー＋ページ形式）
- **ユーザー指摘**: ①深堀りの狙いは「人間には面倒・難しい作業をエージェントがこなしている様に見せる」こと。②前節実装（部屋詳細に細かいデータを1画面詰め込み・max-height 232px 内部スクロール）は Design.md §6.6 の原則（1画面完結・スクロールさせない）に違反。③指示: 元に戻した上で「内部にメニュー構造を作ってページを追加する形式」に。**Officeビューはスライダー（スクロール）禁止・詳細ページは1コンテンツ=1ページ・ワンメッセージ**。部屋詳細は詳細ページへの導線＋エージェントが働く雰囲気のアニメ提示に徹する。
- **撤去**: 部屋詳細右カラム先頭の「ライブ編集」「キーワード詳細インスペクタ」パネル2枚を削除。EN辞書の孤児キー「ライブ編集 — 生成中の記事」「品質チェック（自動）」も削除。
- **新構造**:
  - state `officePage: null|'kw_overview'|'kw_score'|'kw_rank'|'kw_serp'|'kw_related'|'cc_live'|'cc_qa'`。setMode/openRoom/closeRoom/ツアーリセットで null クリア。
  - `deepVals()` → **`detailVals()`** に改名・全面書き換え（main return の spread も `...this.detailVals()` に）。部屋ごとのページ定義 `PAGES`（id/label/msg=吹き出しワンメッセージ）、実演エージェント（部屋で最も動きのあるペルソナ＝俯瞰代表と同じ prio 選出・状態色/状態ラベル連動）、`dPagesOn/dPages`（部屋詳細の導線メニュー）、`dpOpen/dpTitle/dpMsg/dpBack/dpMenu/dpAgent*`、ページ別フラグ `dpKwStatsOn` 等。データ（SEC/docSections/docQa/dkStats/dkScore/dkTrendPts/dkSerp/dkRelated）は前節を流用。
  - **部屋詳細=導線ハブ**: 右カラム先頭に「エージェントの作業を詳しく見る」カード（`dPages` ボタン列→`officePage` セット）。キャラ・吹き出し・WORK LOG・ホロ小物は従来どおり。詳細ページを持たない部屋はカード非表示。
  - **Office詳細ページ**（`data-screen-label="Office詳細ページ"`・z-index 85＝部屋詳細80と「何を見ますか？」90の間）: 100vh flex・**overflowなし（内部スクロールゼロ）**。ヘッダー=「部屋へ戻る」(`dpBack`)＋SECTION表記＋ページ名。左=内部メニュー(`dpMenu`・アクティブ強調)。中央=担当エージェント（aosFloat・状態リング・吹き出しに `dpMsg` 常時表示）＋コンテンツカード1枚（光スイープ）。
  - ページ: キーワードリサーチ=概要(4統計+割当)/価値スコアの内訳/掲載順位の推移(スパークライン大)/検索結果の構成/関連クエリ の5枚。コンテンツ制作=ライブ執筆(job1Idx連動・カーソル・工程/文字数)/品質チェック の2枚。cc_live 本文行は ellipsis 1行化であふれ防止。
  - 深堀り導線の着地変更: `goOfficeLive`→部屋ズーム＋`officePage:'cc_live'`、`goOfficeKwDetail`→`'kw_overview'`（openRoom が officePage を null にするため officeNav の後に setState する順序依存あり）。
- **EN**: ページ名（キーワード概要/ライブ執筆）・部屋へ戻る/詳細ページ/エージェントの作業を詳しく見る・吹き出し7文・QA項目4件・執筆中・フッター断片（対象:/・ 総合 0.82/・ 毎週自動チェック/・ 洗い出しは自動で継続中）を追加。記事本文・キーワード語はデータ扱いで未翻訳。docTitle は既存キーで翻訳される（既存挙動どおり許容）。
- **検証**(preview実機): 部屋メニュー5+2項目表示・旧パネル消滅／全7ページの描画とメニュー切替／内部スクロール要素0・ページoverflowなし（DOM計測）／「部屋へ戻る」→部屋詳細復帰・officePage=null／goOfficeLive経路／EN切替（chrome翻訳・データJA維持）／詳細ページ無し部屋（レポート分析）でカード非表示／consoleエラー0／スクショ（kw_rank: 左メニュー+エージェント吹き出し+グラフ1枚）。※レンダリング非同期のため検証は1ステップ=1eval（クリック直後の同一eval内DOM検査は古い状態を読む）。
- **⚠️ 同日ユーザーレビューで2点差し戻し → 次節で修正済み。**

## 2026-07-09 Office詳細ページの粒度修正＋部屋カラム溢れ解消（Sonnetワーカー委任）
- **ユーザー指摘**: ①部屋詳細の右カラムにスクロールバーが出ている（Officeビューはスライダー禁止＝カラム溢れも違反）。②「1ページ=1コンテンツ」の単位は**1キーワード（or 1記事）**であり、1グラフ単位は粒度が細かすぎて深堀りにならない。ページには対象の全詳細データ＋分析をぎっしり。エージェントは左下に小さく、中央はデータで埋める。③以後は**オーケストラ方式**（メインが判断・品質、実装はSonnetワーカー=Agentツール model:sonnet に委任）で作業する（グローバル方針・memoryに保存済み）。
- **実装（Sonnetワーカー fe-component に仕様書で委任・検収はメイン）**:
  - 部屋詳細の導線カードをピル型にコンパクト化（mono 10pxラベル＋flex-wrap小型ピル）→ 右カラム scrollHeight==clientHeight（1170x946でover:0）。
  - `detailVals()` 再構成: PAGES を対象単位に。keyword_research=3ページ（kw_souba/kw_gift/kw_danball、各 label=キーワード語・msg・stats4・score4・trend・serp・related・analysis）。content_creation=2ページ（cc_rewrite=既存job1Idx連動ロジック流用／cc_danball=全節完了・文字数1,860・工程「送信待ち（接続回復後に自動再送）」・QA全✓＝送信失敗通知のデモと整合）。返却: dpKwOn/dpStats/dpScore/dpTrendPts/dpTrendNote/dpSerp/dpRelated/dpAnalysis、dpCcOn/dpDocSections/dpDocWords/dpDocStageLabel/dpDocStageText/dpDocQa。旧7フラグ・dk*系は全廃（grep残存0）。
  - 詳細ページテンプレート: エージェントを `position:absolute; left:26px; bottom:20px` に縮小配置（吹き出し=dpMsg常時・状態ピル）、左メニューは padding-bottom:250px で回避。中央は kw=2列6カード（基本データ/価値スコアの内訳/掲載順位の推移(svg)/検索結果の構成/関連クエリ/エージェントの分析=紫枠）、cc=3fr:2fr（本文（節ごとの進行）／工程＋品質チェック）。overflow-y/max-height 不使用。工程カードはラベルと値を別span（コロン断片キー回避）。
  - 導線着地: goOfficeLive→'cc_rewrite'、goOfficeKwDetail→'kw_souba'。
  - EN: 旧ページ名・旧吹き出し7文・断片キー（対象:等）削除、追加=基本データ/エージェントの分析/本文（節ごとの進行）/工程/進捗/現在の工程/文字数/送信待ち…/新吹き出し5文。キーワード語・記事名・節本文・serp/related/analysis はデータ扱い未翻訳。
- **検収**(preview実機・メイン実施): 右カラムover:0・可視スクローラ0／kw 3ページ・cc 2ページの全データ描画／innerScrollers 0・pageOverflow false（全ページ）／部屋へ戻る復帰／S2「オフィスで詳しく見る」実クリック→kw_souba着地／EN切替（chrome英訳・データJA維持）／consoleエラー0／スクショ2枚（kw_souba 6カード＋左下エージェント、cc_rewrite 本文進行＋工程/品質チェック）。タグバランス: sc-if 349/349・sc-for 168/168・button 422/422・div 1544/1544・svg 88/88。

## 2026-07-09 詳細ページ第3ラウンド: 縦ストレッチ＋正本データ整合＋NEXT ACTION（Sonnetワーカー委任）
- **ユーザーレビュー3点**: ①デザイン品質も見ているか→全ページ目視パスで「下半分の空洞」を検出し差し戻し。②見た後の予約/作成など次アクション導線はあるか→未整備だった。③ロジック要求と合致しているか→**REQ-AOUI-01違反を検出**（詳細ページの数値が発明値で通常ビュー正本と矛盾。特に「引越し 挨拶 ギフト」は正本8位→14位下落なのに詳細ページは30→18位改善と真逆）。
- **修正1（縦ストレッチ）**: kwグリッド `grid-auto-rows: minmax(min-content,1fr); height:100%`、ccグリッド `grid-template-rows: minmax(min-content,1fr)×2`＋本文カード `grid-row: span 2`。各カード flex column 化＋本文ラッパー `justify-content: space-evenly`（cc本文の節リストのみ上寄せ維持）。順位推移svgは `height:100%` で伸長。
- **修正2（正本整合）**: kwページを KW_ROWS/クエリ分析/KW_CLUSTER_DETAIL 準拠に置換 — kw_mitsumori「引越し 見積もり 比較」(9,900/0.82/11位/割当済・SERPは見積もりクラスタ準拠)、kw_gift「引越し 挨拶 ギフト」(月間クリック1,320・CTR1.4%（基線2.6%）・14位・8→14位下落トレンド・スコアカード見出しを dpScoreTitle='クエリ一致と機会' に切替・承認待ちストーリー)、kw_danball「引越し ダンボール 無料」(5,400/0.61/—（未計測）・trend=null→ `dpTrendOn` false でプレースホルダ「未計測 — 記事公開後に順位計測を開始します」)。cc_danball の工程は `wpVaultRetried/wpVaultPaused` と連動（WP下書き作成済み/退避中/送信待ち）。kw_souba は消滅（grep残存0）。
- **修正3（NEXT ACTION）**: 各ページに `dpCta` {label,onClick}。kw=分析カード下部・cc=工程カード下部に青グラデボタン＋mono 'NEXT ACTION' 小見出し。着地: kwを開く（S2 kwSel セット）／差分を承認（automation）／タスクを管理（content＋jobDetail J-1024）／成果物を確認（content ctTab:'output'）。goOfficeKwDetail は kwSel→ページのマッピング（見積もり比較/ダンボール無料、他は kw_mitsumori）。
- **EN**: 追加=クエリ一致と機会/月間クリック/CTAラベル4種/未計測プレースホルダ/—（未計測）/（記事生成済み・未公開）/WP下書き作成済み（7/8 再送成功）/送信退避中（自動再送 停止中）/新吹き出し文。旧吹き出し・旧ページ名キーは削除。
- **検収**(メイン実施): 正本値の全描画・下落トレンド・未計測プレースホルダ／CTA4種の実クリック着地（kwSel/automation/jobDetailモーダル/ctTab output）／S2からの kwSel マッピング着地／wpVaultRetried 連動／**レスポンシブ**: 1920x1080・1280x800・1170x946・1024x700 で hScroll 0・オーバーレイ全面フィット・clip 0・グリッド下端ギャップ0（余白なし）。800x500の極小窓のみ内容クリップ（デスクトップ最適=REQ-NAV-06の想定内・スクロールバーは出ない）。consoleエラー0。タグバランス: sc-if 351/351・sc-for 168/168・button 424/424・div 1550/1550・svg 88/88。※preview_screenshot が30sタイムアウトを繰り返す事象（ハーネス起因・evalは正常）のため、本ラウンドの視覚検証はDOM計測＋前ラウンドのスクショで代替。
- **⚠️ preview_resize の残留事故**: レスポンシブ検証後にビューポートエミュレーションを解除し忘れ、ユーザー側で「アプリが左上の小箱＋余白だらけ」に見えた。エミュレーションはユーザーが見ている同じパネルに乗る。**検証後は必ず preset:'desktop'（native復帰）→ reload までやってからターンを終える**（memoryにも記録）。スクショ30sタイムアウトも同根（native復帰で解消）。

## 2026-07-09 詳細ページ第4ラウンド: 導線視認性＋配置監査（Sonnetワーカー委任）
- **「詳細への導線が分からん」**: コンパクト化しすぎた導線カードを再強化（白太字14px見出し＋キャプション「1キーワード・1記事ごとの詳細ページへ」＋青アクセントピル＋›）。相殺として WORK LOG を slice(-7)→slice(-5)・min-height 92→72px（右カラム溢れ0を946/800px高で実測）。**「何を見ますか？」ペルソナメニューに第2グループ「DETAIL — 詳しく見る」を追加**（PAGES をクラスフィールド `DETAIL_PAGES` に昇格し menuPages val で共有。俯瞰キャラクリック→項目クリックで openRoom＋officePage を一気にセット）。ENキー2件はワーカー停止のためメインが直接追加（"1キーワード・1記事ごとの詳細ページへ"/"DETAIL — 詳しく見る"）。
- **「背景を意識して配置をおろそかにしてない？」**: 全ビューをバウンディングボックス交差判定で監査。俯瞰=衝突0。**部屋詳細=ホロ小物カード×代表ペルソナピルが162×79px重なり**→ホロのアンカーを左下→左上（left:6px; top:6px）へ。**詳細ページ=エージェント吹き出しがグリッド左下カードへ24px食い込み**→吹き出し max-width 250→200px（エージェント右端252 < グリッド左258 実測）。再監査で衝突0。絶対配置要素を追加する際はこの交差監査をレビュー項目に含めること。
- Sonnetワーカー運用の学び: 長い複数項目の発注では途中で停止することがある（報告が「Now 修正B:…」等で切れる）。**完了通知後は報告を鵜呑みにせず、grep/実測でファイル実態を監査してから検収する**（今回もEN追加漏れ・停止を2回検出）。

## 🔖 セッション引き継ぎメモ（2026-07-09 セッション終了時点）

**このセッションでやったこと（/goal「通常⇄Officeの画面配線見直し」から発展）**: Officeビューを「実行状況の確認・深堀り基盤」として全面強化した。最終状態は全て preview 実機検収済み・consoleエラー0・タグバランス一致。

### 最終アーキテクチャ（Office深堀り）
- **役割分担**: 通常ビュー=選択の基盤（選ぶ・決める・承認）／Officeビュー=実行状況の確認基盤（見守る・深堀り）。Officeは決定を持たず NEXT ACTION で通常ビューへ渡す。
- **部屋詳細（室内ズーム）**: 中央壁面ホロパネル（状態連動ホロ小物＋詳細ページ導線ボタン＋統計＋WORK LOG 5行）＋下にキャラ列。右カラムは「何を見ますか？」+RECOMMENDED のみ。ステージは縦flexフロー配置（絶対配置の衝突なし）。
- **Office詳細ページ**（`officePage` state / `detailVals()` / `DETAIL_PAGES` クラスフィールド）: 1ページ=1対象（キーワード3件: kw_mitsumori/kw_gift/kw_danball、記事2件: cc_rewrite/cc_danball）。左=内部メニュー、中央=データグリッド（縦ストレッチ・スクロール禁止）、左下=担当エージェント（吹き出しワンメッセージ）。データは正本（KW_ROWS/クエリ分析/成果物一覧）と一致、cc_danball 工程は wpVaultRetried/Paused 連動。各ページに NEXT ACTION ボタン。
- **導線3系統**: ①部屋パネルの立体ボタン ②「何を見ますか？」の「DETAIL — 詳しく見る」グループ（俯瞰キャラ→一気遷移） ③S2「オフィスで詳しく見る」（kwSelマッピング）・S3「オフィスでライブ編集を見る」。

### このセッションで確立した設計原則（Design.md §6.6 に反映済み・memoryにも保存）
1. **1ページ=1対象**（1キーワード/1記事の全データ＋分析。グラフ単位の細切れ禁止）
2. **スクロール絶対禁止**（overflow-y/max-height不使用。カラム溢れのスクロールバーも違反）
3. **背景アートは温存より情報配置優先**（中央壁面にホロパネルとして被せる。サイド詰め込み禁止）
4. **押せるもの=立体ボタン／飾り=フラットタグ**で形状差別化
5. **REQ-AOUI-01**: Office側の数値・状態・ストーリーは通常ビュー正本と必ず一致させる
6. **オーケストラ方式**: 実装はSonnetワーカー（Agentツール）に仕様書で委任、メインは判断・検収。ワーカーは長い発注で途中停止することがある→完了報告を鵜呑みにせず grep/実測でファイル監査してから合格判定

### 検証プロトコル（重要）
- memory `preview-verify-dc-prototype` 参照。特に: **preview_resize 後は必ず preset:'desktop'→reload で復帰させてからターンを終える**（怠るとユーザー画面が縮小表示になる実害）。レンダリング非同期のため検証は1ステップ=1eval。配置変更時はバウンディングボックス交差監査を必須とする。
- 書面同期先: screen-inventory §3・glossary UI表記対応表・Design.md §6.6（ルートと prototype/Design.md 追補3の両方）・本ファイル。

### 未了・次セッション候補
- 他5部屋（分析/自動化/ナレッジ/設定/テクニカル）の詳細ページは未実装（DETAIL_PAGES に追加すれば導線・メニューは自動追従）。
- Office詳細ページの EN は chrome のみ翻訳（データ非翻訳の規約どおり）。新ページ追加時は同様に。
- ツアー（TOUR_STEPS_OFFICE）に詳細ページの案内ステップは未追加。

## 2026-07-09 部屋詳細を中央壁面ホロパネル構成へ再配置（Sonnetワーカー委任）
- **ユーザー指摘（設計原則）**: 「背景を見せないといけないという意識から配置を避けていないか。サイド配置を減らして中央の背景にかぶせた方が分かりやすい」→ **情報配置がアート温存より優先**。中央壁面にはホロ投影パネルとして被せてよい（memory: office-overlay-on-background-art）。
- **再配置**: ステージを縦flex（column・space-between）にし、上=**中央壁面ホロパネル**（width min(640px,94%)・半透明+スキャンライン+光スイープ）／下=キャラ列のフロー配置。パネルに ①状態連動ホロ小物（旧・左上absoluteカードの中身を移設） ②「エージェントの作業を詳しく見る」導線ピル（dPagesOnゲート） ③統計タイル（150px列に縦積み）＋WORK LOG（1fr列）を集約。右カラムは「何を見ますか？」+RECOMMENDED の2カードのみに削減。絶対配置を排したので衝突は構造的に発生しない。バインディング名・JSロジック・EN辞書は不変。
- **検収**: パネル中央配置・キャラ列との交差0（1237x946・1280x800）・右カラム/ステージともoverflow 0・settings部屋（トグルholo・導線なし部屋でピル非表示）・タグバランス sc-if 352/352 他一致・consoleエラー0・スクショ目視（壁面にKEYWORD CLOUD+導線+486/32+WORK LOGが載り、SEO AGENTロゴは下に透ける）。
- **同日追補1（ボタン可視性）**: 「ボタンはわかりやすくして」→パネル内の詳細ページ導線を角丸長方形の立体ボタン（青グラデ+影+白文字+›・hover明化）に変更。KEYWORD CLOUDのフラットタグと形状で差別化。クリック着地検証済み。
- **同日追補2（詳細データのリッチ化）**: 「チープじゃないか？」→statsを{k,v,sub,subFg}拡張（差分・状態のサブ行: ↑8週で11ランク改善=緑/前月比−22%=赤/リライト承認待ち=琥珀 等・正本整合）、順位推移svgに面グラデ(#dpTrendGrad)+グリッド線3本+始点終点ラベル+改善緑/下落赤の線色（dpTrendColor/dpTrendArea/dpTrendStart/dpTrendEnd）、スコアバーに25/50/75目盛+%ピル、関連クエリに検索アイコン、分析カードに担当エージェント丸アイコン、工程カードに進捗バー（dpDocProgPct・job1Idx連動を実測一致確認）、節番号を状態色丸バッジ化。EN追加なし（全てデータ扱い）。検収: kw_mitsumori/kw_gift（赤トレンド）/cc_rewrite（進捗バー8%=idx0一致）で描画・スクロール0・overflow0・consoleエラー0。タグバランス sc-if 352/352・sc-for 169/169・button 425/425・div 1563/1563・svg 89/89。
- **同日書面反映**: Design.md §6.6 を両ファイル（ルート正本 `Design.md` と `prototype/Design.md` 追補3）で改訂 — 室内詳細を中央壁面パネル構成に書き換え（右パネル2カード・WORK LOG 5行・配置原則・ボタン可視性）＋「Office詳細ページ（深堀り・1ページ=1対象）」節を新設（目的/レイアウト/スクロール禁止/正本一致/NEXT ACTION/導線3系統）。

## 2026-07-09 通常⇄Office 部屋パネルのタブ着地バグ修正（ユーザー指摘「設定が合致しない」・Sonnetワーカー委任）

- **ユーザー指摘**: 「通常ビューとOfficeビューで設定が合致しないのがあると思うんだけど、通常ビューを大改修した影響だからOfficeビューのほうを修正できるか？画面マップとかがあれば先にドキュメントに残してほしい」。
- **調査**（メイン実施・grep/Readで直接特定。原因即判明のためWorkflow不使用）: `openWb(roomId, opt)`（部屋パネルの選択肢クリック→通常ビューへ着地させる唯一の経路）が`WB_MAP[opt]`から拾うstateキーをハードコードした許可リスト（`kwTab`/`anTab`/`anRwKind`/`unassigned`/`ctKind`/`job1`）に限定していた。2026-07-08のS3/S4/S6/S7タブ分割で新設された`ctTab`/`auTab`/`knTab`/`seTab`がこの許可リストに追加されず、**設定（S7）だけでなくオートメーション（S4）・ナレッジ管理（S6）・コンテンツ作成（S3のカレンダー/ライブ生成）の4画面で、クリックしても必ず既定タブに誤着地**（S4「緊急停止」ですら`kill`タブに到達不能）していた。加えて`ROOM_DATA`の該当room `options`/`recTarget`とグローバル検索`GS_SETTINGS`が現行タブ構成（S4=6タブ・S6=6タブ・S7=5タブ）に対して2〜3世代分古いままだった。`keyword_research`と`technical_seo`は既に正しく機能しており対照群として確認（変更なし）。
- **画面マップ・監査結果を先にドキュメント化**: `docs/reference/ai-office-de-seo-view-sync-audit_2026-07-09.md` に現行タブ構成の正本表・部屋別の不整合一覧（🔴要修正/✅問題なし）・根本原因・修正方針を記録してから実装着手（ユーザー指示どおり）。
- **実装**（Sonnetワーカー fe-component に仕様書で委任・検収はメイン）:
  1. `openWb()`に`if (m.ctTab) next.ctTab = m.ctTab;` / `auTab` / `knTab` / `seTab` の4行を追加。
  2. `WB_MAP`の該当22エントリを現行タブキーで一本化（旧キー`サイト・メンバー管理`/`Google連携・WordPress連携`/`ユーザー要望の管理`/`サンプル記事の型`/`検索実績（URL×クエリ）`/`クエリ一致の品質`/`AI検索の影響`/`リライト原因解析`は削除。`クエリ一致の品質`/`AI検索の影響`はS2クエリ分析へ移設済みのため部屋対応から除外＝越境導線を解消）。
  3. `ROOM_DATA.automation`（4→6項目：予算管理・目標管理を追加）／`.knowledge`（4→6項目：成功学習・CVポイント台帳を追加）／`.settings`（4→5項目：現行5タブに合わせ全面更新）／`.traffic_report`（越境2項目→流入ページ・上位ベンチマークに差し替え）／`.content_creation`（カレンダーを追加・進捗着地先を`ctTab:'progress'`に修正）の`options`/`recTarget`を更新。
  4. `GS_SETTINGS`各要素に`seTab`を追加し`gsIndex()`のsettings行を`state: { seTab: s.seTab }`に変更（グローバル検索からの設定項目も正しいタブへ着地）。
  5. 新規/改名した日本語ラベル11件を`TXT.en`に追加（既存タブラベルと文字列一致するものは辞書再利用・追加不要と判断済み）。
- **検収**(メイン実施・preview実機): `openWb()`直接呼び出しで5部屋を検証 — automation「緊急停止」→`auTab:'kill'`✓／knowledge「成功学習」→`knTab:'winner'`✓／settings「お支払い・クレジット・通知」→`seTab:'billing'`✓／content「カレンダー」→`ctTab:'calendar'`✓／traffic_report「流入ページ」→`anTab:'traffic'`✓。`gsGo()`経由（グローバル検索）でも`クレジット残高・チャージ`→`seTab:'billing'`着地を確認。未変更の`keyword_research`（対策候補→`kwTab:'promo'`）で回帰なしを確認。部屋パネルのDOM描画確認（automation/settings/knowledge/traffic_reportの新規ラベル全て表示・スクショで6項目pill正常表示）＋EN切替（予算管理→Budget等）確認。consoleエラー0。タグバランス変更前と完全一致（sc-if 352/352・sc-for 169/169・div 1563/1563・button 425/425・svg 89/89・section 94/94＝JSオブジェクトのみの変更でHTML構造は無傷と確認）。`TXT.en`重複キーは既知の11件のまま増加なし。
- 書面: 本節を追記。`docs/reference/ai-office-de-seo-view-sync-audit_2026-07-09.md`が詳細な監査記録として正本。
- **プレビュー起動の副次対応**: ポート8756が他チャットのdevサーバーと衝突したため、`.claude/launch.json`の`proto`設定を`autoPort:true`＋`PORT`環境変数読み取り方式（`.claude/serve.py`新設・`python -m http.server`の固定ポート引数を廃止）に変更。今後はポート競合時も自動的に空きポートへフォールバックする。
- **同日追補（もれ確認・ユーザー指摘「もれなし？」）**: `openWb()`/`WB_MAP`（部屋パネル・グローバル検索の経路）は直したが、それ以外に直接`this.setState({screen:'automation'|'settings', ...})`する箇所が同じ「タブキー未指定→既定タブに固定着地」バグを持っていないか全量grepで再監査（メイン実施）。実際に12箇所で発見・修正:
  - `screen:'automation'`が`auTab`未指定だった9箇所に`auTab:'approve'`（承認待ちの結果が着地する箇所: 通知2件・DETAIL_PAGES CTA・gsIndexのJ-1018検索結果・CVポイント台帳のonFix・CRO onApply・好調記事onRipple・フラッシュリライトflashApply・インデックス問題onFix・S1おすすめ実行rec_fix）／`auTab:'sched'`が適切な1箇所（S3カレンダーの`ctCalGoAuto`）。
  - `screen:'settings'`が`seTab`未指定だった2箇所に`seTab:'acq'`（通知「WordPress接続: 再認可が必要です」→設定へ）／`seTab:'billing'`（`stubBilling`＝クレジット購入完了後のお支払い管理導線）。
  - 一般ナビ（`goAutomation`/`goSettings`/`goKnowledge`/`goContent`等のサイドメニュー・ドロワー・`navXxx`）は意図的に既定タブへ着地させる設計のため対象外（バグではない）。
  - 低確信のため見送り: W7通知「注目キーワード: 3クラスタが基準を超えました」→`screen:'keywords'`（`kwTab`未指定）。対応するクラスタ／候補と一意に紐付く根拠が薄く、誤ったタブを断定できなかったため変更していない。
  - 検収: 12箇所すべてを`window.__aos`経由で直接呼び出し、期待どおりのscreen/tabに着地することを実機確認（stage2Vals().ctCalGoAuto／stdVals().stubBilling／gsIndex+gsGo／ncVals().ncRows[].onGo／DETAIL_PAGES.keyword_research[kw_gift].cta.onClick／s5Vals()のflashApply・idxIssues[].onFix・cvCatalog[].onFix）。タグバランス・consoleエラーとも前回検証と同一（変化なし＝JSのみの変更）。
  - 監査doc（`docs/reference/ai-office-de-seo-view-sync-audit_2026-07-09.md`）は本追補（直接setState呼び出し12箇所）を含め同期済み（後日の第3ラウンド追記時に反映）。

## 2026-07-09 通常⇄Office 部屋パネル配線 第3ラウンド: Fable独立監査＋残存8箇所修正（ユーザー指示「あるべき配線では懸念をすべて調査して…Fableに相談して報告してくれ」）

- **ユーザー指示**: 上記の第1・第2ラウンド修正を「再検証して」「もれなし？」と重ねて確認された後、「あるべき配線では懸念をすべて調査して調査後にFableに相談して報告してくれ」という指示を受けた。自己監査の限界（確証バイアス）を踏まえ、経緯を共有しない独立エージェントに再監査させる方針を採った。
- **調査**: `Agent`ツールを`model: fable`・`subagent_type: Explore`（読み取り専用）で起動し、`AI Office de SEO.dc.html`全体を通常⇄Officeビューのルーティング整合性という観点のみでブラインド監査。第1・第2ラウンドの内容は一切共有せず。結果、同じバグパターン（タブキー未指定→既定タブ固定着地）の追加インスタンスをA〜Hの8項目で検出。詳細は`docs/reference/ai-office-de-seo-view-sync-audit_2026-07-09.md`の「第3ラウンド」節に記載（`volWatchGo`/`volPinAdd`のタブ誤り、`volAlerts`の一律誤着地、S3/S4/S6/S7への汎用導線6箇所、`anBenchRows`の行内容無視、`ctKind`はあるが`ctTab:'create'`欠落の12箇所、`WB_MAP`2エントリの同型欠落、`gsIndex()`の`screen:'notifs'`という**存在しない画面名**指定＝白画面バグ、`DETAIL_PAGES.cc_rewrite`CTAの誤タブ）。
- **実装**（Sonnetワーカー fe-component に委任）: 1回目の委任は完了報告が「Now修正E: 12 items. Let's find each.」で終わっており、実際にgrepで確認するとE以降（E/F/G/H）が未反映と判明（**ワーカーの完了報告を鵜呑みにしない方針どおり検出**）。同一エージェントを`SendMessage`で再開し、現在のファイル内容に基づく正確な行番号・コード片を再指定して残り4項目を完遂させた。
- **追加修正**: 第2ラウンドで「低確信のため見送り」としていた通知「注目キーワード: 3クラスタが基準を超えました」の着地先を、Fableの根拠付き推奨（`kwTab:'promo'`）を採用して確定・修正。
- **検収**（メイン実施・grep独立再確認＋preview実機）: A〜Hおよび追加修正分すべてをgrepで個別確認したうえ、`window.__aos`で`openWb`/`gsIndex`/`ncVals`/`DETAIL_PAGES`/`stdVals`/`s5Vals`の該当関数を直接呼び出し、期待どおりのscreen/tab着地を確認。consoleエラー0。タグバランスは3ラウンド通じて完全不変（sc-if 352/352・sc-for 169/169・div 1563/1563・button 425/425・svg 89/89・section 94/94）。
- 書面: `docs/reference/ai-office-de-seo-view-sync-audit_2026-07-09.md`に第3ラウンド節を追記済み（本節と両方が最新状態で同期）。

## 2026-07-09 クレジット不足見込み日「7/24」のハードコード解消（ユーザー指摘）

- **ユーザー指摘**: 「クレジットの予測書いてあるけど、これハードコードじゃないよね？」。調査の結果、ヘッダーのクレジットチャージ・Officeビューsettings部屋の`recommend`・S7設定画面`seBills`の3箇所4パターンに「7/24」という固定文字列が直書きされており、隣接する消費実績（付与1,200cr・8日時点消費392cr）と一切連動していなかったことを確認・報告。ユーザーから「ハードコードじゃない（ノットなハードコード）ようにして」と修正指示。
- **実装**（メイン実施・小規模のためワーカー委任せず直接編集）:
  1. `creditForecastDay()`（クラスメソッド、`AI Office de SEO.dc.html:7240`付近）を新設。`seBills`の実績値（8日時点で392cr消費＝日次49cr/日）を根拠に、`st.balance`（購入で増減する実データ）から不足見込み日を都度算出。月またぎ（31日超）も繰り上げ計算。
  2. `settingsRecommendText()` を新設し、Officeビューsettings部屋の`recommend`を`buildRoom()`内で描画時点に上書き（`ROOM_DATA.settings.recommend`は空文字に変更・実値は保持しない）。`buildRoom()`は`d`を`onOpen`のズーム表示・部屋メニュー（`menu.data`）双方に使い回す構造のため、`d`自体を上書きすることで両方の経路を一度に解消。
  3. ヘッダーのクレジットチャージ・ドロップダウン（テンプレート256行目）を`{{ creditForecastNote }}`バインディングに変更し、`stdVals()`側で`st.locale`に応じたJA/EN文言を都度生成。
  4. `seBills`（`stage2Vals()`内）の「不足見込み」行の`value`を`this.creditForecastDay() + ' 頃'`に変更。
  5. TXT.enの旧固定文言に対応する辞書エントリ（今月のペースだとクレジットが 7/24 頃に不足見込み…）を削除（動的生成に伴い恒久的に不一致になるため）。
- **検収**（`window.__aos`実機）: 4箇所すべてが同一の計算値（初期`balance:1240`で"8/2"）で一致することを確認。`setState({balance: balance+2000})`で残高を増やすと見込み日が"8/2"→"9/12"へ実際に後ろへ伸びることを確認（月またぎ計算も正常）。`locale:'en'`でのJA/EN切替も確認。スクリーンショットでヘッダードロップダウンの表示を目視確認。consoleエラー0。タグバランス変更前と完全一致。
- **追補（ユーザー指摘「クレジット関連の表示パーツはオブジェクト化したほうがいいんじゃないか」）**: `creditForecastDay()`/`settingsRecommendText()`の2メソッドに分かれていたのを、`creditForecast()`という単一メソッドに統合し`{date, headerNote, billsValue, settingsRecommend}`のオブジェクトを返す形に変更（`AI Office de SEO.dc.html:7237`付近）。`stage2Vals()`冒頭で`const creditFc = this.creditForecast();`と1回だけ計算し`seRecommend`/`seBills`の両方で使い回す（従来は同じ計算をサイトごとに個別呼び出し）。`buildRoom()`・`stdVals()`側もそれぞれ`this.creditForecast().xxx`を直接参照する形に統一。検収: 4箇所すべて再度`window.__aos`で一致確認、タグバランス・consoleエラーとも変化なし。

## 2026-07-09 コンテンツ整合性の全量監査＋全件修正（ユーザー指示「ページと内容が一致していないのをすべて洗い出せる？...調査したらFableに相談して報告して」→`/goal レポートにまとめてから全件修正。`）

- **背景**: クレジット予測「7/24」ハードコード修正の流れで、ユーザーから「ページと内容が一致していないのをすべて洗い出せる？タブとかポップアップ系もすべて」という全量監査依頼を受けた。これは第1〜3ラウンドが対象にした**ルーティング**（クリックで違うタブに着地する）とは別カテゴリの、**コンテンツ**（表示されている文言・数値・状態が隣接データや別画面の正本と矛盾する）の不整合を指す。
- **調査**（Workflow機能・12領域並列discover→verify＋独立Fableブラインド監査）: Sonnet側32件確認（33件中1件は誤検出棄却）、Fable側22件発見、うち7件は両者が独立に同一バグへ到達（クロス確認・最高確信度）。実質ユニーク約46件。詳細・全リストは`docs/reference/ai-office-de-seo-content-consistency-audit_2026-07-09.md`。代表例: Officeビュー各部屋パネルの件数固定文字列（承認待ち3→実5・要望7/NG12→実3/3・メンバー4→実3）、同一キーワードの順位が画面によって12位/11位で食い違う、同一ジョブIDが2つの別ジョブを指す、本人確認モーダルが別人（吉田幸之介）を表示、等。
- **報告**: ユーザーに一旦報告した後、`/goal レポートにまとめてから全件修正。`が発行され、以降は確認を挟まず全件修正まで自律的に継続する方針で作業。
- **実装**（Workflow機能・テキスト領域が重ならない11クラスタに再編成しSonnetワーカーへ並列委任。1クラスタのみ最難関のためメイン注記で追加ガイダンス）: automation_room／knowledge_room／settings_room／notifications_and_badges／topic_coverage／s1_recs／s3_content／s5_analytics／office_detail_pages／identity_help／job_data_consistency（J-1009/1015/1018/1021/1019のID衝突・A-2/A-4/A-5承認カードの根拠データ・「引越し ダンボール 無料」公開状態統一など最も判断を要する項目群）。
- **検収**（メイン実施・ワーカー報告を鵜呑みにせず全件独立検証）: 全クラスタの変更をgrepで個別再確認、TXT.en重複キー数が既知の11件のまま変化なし（並列編集による破損なし）、タグバランス完全不変、`node --check`で構文エラーなし。**ランタイム検証で2件の新規不具合を発見・追加修正**: (1) `s3_content`クラスタが導入した`s2En`/`sampleCount`変数の未宣言（構文チェックでは検出不可、`window.__aos`での実行時テストで顕在化）→`stage2Vals()`冒頭に宣言を追加、(2) `job_data_consistency`クラスタが「ダンボール」の代替キーワードに採用した「引越し 単身パック 比較」でKW_ROWS（rank:14）と新設したanRows/anPageTraffic（rank:7位）が矛盾→KW_ROWSのrankを7に統一。修正後、S1〜S7全43タブ状態・Officeビュー7部屋・主要ポップアップ（クレジット購入フロー含む）を`window.__aos`で網羅的にrenderVals()呼び出しし、consoleエラー0を確認。
- **並列編集の技術メモ**: 11エージェント同時編集により一部でEditツールの「file modified since read」競合が発生。該当ワーカーはPythonでの読み取り→一致件数アサート→書き込みにフォールバックして対応（Edit toolの排他制御と同等の安全性をアプリケーション層で再実装）。事後のTXT.en重複キー検査・タグバランス検査で、この競合が実際のデータ破損を招いていないことを確認済み。
- 書面: `docs/reference/ai-office-de-seo-content-consistency-audit_2026-07-09.md`に「実施結果」節を追記済み。3ラウンド（ルーティング）＋本ラウンド（コンテンツ整合性）で合計90件超の通常⇄Officeビュー不整合を検出・修正。

## 2026-07-09 Office詳細ページを全7部屋に拡張（/goal「全部屋の詳細ページ作り込み」・オーケストラ方式）
- **背景**: Office詳細ページ（`officePage`/`detailVals()`/`DETAIL_PAGES`）はこれまで keyword_research(型A)・content_creation(型B) の2部屋のみ。残り5部屋（レポート分析/自動化/ナレッジ/設定/テクニカルSEO）を作り込む。要求・リサーチ由来のデータを正本に、アプリ層の挙動を逆引きして「エージェントの作業が見える」詳細ページを構築。実装はSonnet5ワーカー、設計/ビジュアルはFable相談、メインはオーケストレーション＋全件検収。
- **設計（Fable相談で確定）**: 新規DOMテンプレは3つだが**すべて既存骨格を再利用**し一貫性を担保。ページ型は計5種:
  - **型A メトリクスグリッド**（2列6カード）: keyword_research（既存）＋**traffic_report（流用）**。カード見出しをデータ駆動化（`dpStatsTitle`/`dpTrendTitle`/`dpSerpTitle`/`dpRelatedTitle`・kw既定にフォールバック）してtrafficに転用。`isKw`条件を `rv==='keyword_research' || rv==='traffic_report'` に拡張。
  - **型B ドキュメント進行**（3fr:2fr）: content_creation（既存）。
  - **型C 承認差分**（型B骨格流用・`dpApOn`・rv==='automation'）: 左=変更差分(before→after縦ペア＋SVG矢印)、右上=実行条件(予算バー・80%目盛)、右下=安全ルール判定(✓/!/✕＋根拠値)＋NEXT ACTION。CTAは該当承認案件を `state.approvals.find→modal` で通常ビューに開く。
  - **型D 学習知見**（型A骨格流用・`dpLnOn`・rv==='knowledge'）: 学んだこと(引用文＋種別/状態チップ)/根拠の実測値(before→after統計ペア＋矢印)/効果の推移(適用時点=紫縦線マーカー・学習中は点線)/反映先/確信度と観測範囲/分析。上昇=良い用に `trendPtsUp`/`trendAreaUp` を新設（既存 `trendPts` はrank用=下が良い）。
  - **型E ステータス盤**（型A骨格流用・`dpStOn`・rv==='settings'||'technical_seo'）: ステータス行/ヘルスゲージ(SVG円弧・`aosArcIn`)/フォーカス可視化(`visKind`: `cells`稼働格子 / `linkgraph`リンクグラフ / `cwv` Core Web Vitals / `ledger`クレジット内訳)/監視ログ/影響・修正候補/分析。cellsは `buildCells()` で決定論生成（Math.random禁止）。
- **各部屋のページ（1ページ=1対象・正本準拠 REQ-AOUI-01）**:
  - traffic_report(3): tp_souba(好調+12%)/tp_yarukoto(安定・改善前後+32%)/tp_gift(急落−15%→差分承認へ)。`anPageTraffic`正本＋派生指標(パフォーマンス評価バー・順位推移・分析はアプリ層想定)。
  - settings(3): st_integration(連携ヘルス・**WordPress既定「再認可が必要」を正本一致**・security_admin)/st_credit(**`creditForecast()`動的注入**・残高変更で不足見込み日が実移動＝固定文字列禁止を順守)/st_access(メンバー2名+招待1＝buildRoom整合)。
  - technical_seo(3): tk_links(リンクグラフ・**孤立0未対処/1予防検知で stats:0 vs topoTree:1 の矛盾を整合表示**)/tk_crawl(idxKpis 128/9/5)/tk_speed(Core Web Vitals LCP2.1s/CLS0.06/INP180ms＝正本に無くアプリ層想定で構築)。
  - automation(4): ap_rewrite(A-1・5差分)/ap_new(A-2)/ap_link(A-3)/ap_cv(A-5)。`state.approvals`正本と差分・理由・品質・予約が一致。
  - knowledge(3): kn_structure(構成・滞在+38%)/kn_appeal(訴求・CTR+1.2pt)/kn_link(内部リンク・**学習中＝点線トレンド＋確信度58%/再現1/3**)。knowledge_trainerは `mapping.stages:[]` で常時idleのため「学習中/観測件数/過去実績」で作業感を演出。
- **型Eレイアウト是正（検収で発見・メイン直接修正）**: ヘルスゲージとリンクグラフのSVGが `width/height:100%` でviewBoxのアスペクト比由来の固有min-heightを課し、2つのSVG行が715px窓で159px溢れた（型A/Bは同窓でフィット＝型E固有）。両SVGを `position:absolute; inset:0`（wrapper `position:relative`）に変更して圧縮可能化→6カード均等198px・溢れ0・リンクグラフも拡大。settings3ページ回帰なし。**教訓: 詳細ページのSVGカードは `position:absolute` で固有アスペクト比の押し上げを断つ（Design.md §6.6に反映）。**
- **検収（全ラウンド・メイン実施・ワーカー報告を鵜呑みにせずgrep＋preview実機で独立検証）**: 最終スイープで**全21ページ×7部屋を `detailVals()` 走査＝例外0・全ページ `dpOpen`・全部屋 `dPagesOn`・ページ型正しい(A×6/B×2/C×4/D×3/E×6)**、preview実機で各部屋の描画・**スクロール0（overflow/innerScrollers）**・正本一致・EN(クロームのみ英訳・データJA維持)・CTA着地・consoleエラー0・タグバランス最終 `sc-if 362/362・sc-for 181/181・div 1654/1654・svg 94/94`。
- **ワーカーの過大報告を複数検出**（再確認）: R1=型Aテンプレのバインディング化4行をスキップして完了報告→メイン是正／R2a・R3・R4=報告が検証途中で途切れ→メインが全件grep+preview再確認で実在確認。**テンプレ挿入は固有バインディング(dpApOn/dpLnOn等)のGrep存在確認で自己検出させ、メインも独立検証必須。精密スペック（コード・データを一字一句）をscratchpadに書いて委任すると発明・逸脱を防げる。**
- 導線: 部屋詳細の「エージェントの作業を詳しく見る」・「何を見ますか？」の「DETAIL — 詳しく見る」は `DETAIL_PAGES[room_id]` で全部屋自動追従。ヘッダーval(dInterior/dAccent/dNo/dFloorLabel等)も buildRoom から全部屋供給済み＝部屋追加でコード変更不要。
- 残（任意）: 各部屋のページは代表対象に絞っている（automation承認5件中4件・traffic5ページ中3ページ等）。必要に応じ追加可。ツアー(TOUR_STEPS_OFFICE)の詳細ページ案内ステップは未追加。書面: screen-inventory §3・Design.md §6.6（ルート＋prototype両方）を全7部屋・5ページ型に更新済み。

## 2026-07-09 Office詳細を「詳細エクスプローラ」化（全KW・全記事をデータ生成＋関連度サイドバー・ユーザー要求）
- **背景/要求**: ユーザー制定「**通常ビュー=見やすさ優先／Officeビュー=詳しさ優先**。キーワードを見たくなったらそのキーワードの詳しい情報が欲しくなる＝1対象を個別に深掘りできる粒度」。既存の型A(keyword/traffic)は代表3件のみ手書き→**全キーワード・全記事に拡張**し「対象が変わっても同じ器で表示される仕組み」に。データ源泉は **DataForSEO(市場・SERP)＋Search Console(自サイト実績)** を想定し、逆引きデータを実APIで取れる値に揃える。**AI Overview(AIO)は必須表示**（ユーザー明示）。実装Sonnetワーカー3ラウンド／設計Fable相談／メインはオーケストレーション＋全件preview検収。
- **方式=手写経しないデータ生成＋上書きマージ**（`officePagesFor(rv)` 集約）:
  - `buildKwPages()`: `KW_ROWS`(10)×`KW_CLUSTER_DETAIL` から型Aページ生成。既存override(kw_mitsumori/gift/danball)は `_kw` 一致で deep-merge＝作り込み温存・`depth:'deep'`。
  - `buildArticlePages()`: 新設 `ARTICLES`(記事のGSC+DataForSEO統合レコード10件・スラグ不整合を避けるため一元集約) から型A"記事版"生成。override(tp_souba/yarukoto/gift)は `_slug` 一致で merge。
  - `detailVals()` 縫い目 `const pages = this.officePagesFor(rv)`（旧 `DETAIL_PAGES[rv]` 直参照を置換）。cur は `pages.find(p=>p.id===officePage)`。id=override保持 or `kw_row{i}`/`ar_{slug}`。
- **関連度は単一関数 `relatedOf(cur, kind)`**（Fable P0＝サイドバー/関連カード/クロス動線が同一ソース＝整合性バグの構造的防止）。同クラスタ→intent/target一致→rel/syn/共有KWで加点。`kwTokenMatch()`(トークン包含一致)で表記ゆれ・語順ゆれ(「引越し 単身 相場」⇔「単身 引越し 相場」/「挨拶 ギフト」⇔「挨拶 ギフト のし」)を吸収し kw↔記事クロスを双方向解決。
- **基本データカード=GSC/DataForSEO 来歴段組み(3×2)**: 上段 **MARKET·DataForSEO**(検索数/難易度=`clusterKD`/**AI Overview 出現·なし**)／下段 **PERFORMANCE·Search Console 28日**(クリック/表示回数/CTR)。キーワードのperfは**割当記事の実績**（`row.status!=='未割当'` を尊重＝未割当は「—」）、記事のperfは `ARTICLES` 実値。順位はトレンドカード・割当はヘッダーへ移設。**AIOは MARKETセル＋SERP行＋分析文の3面**で第一級表示。
- **ヘッダーバッジ**(好調/急落/学習元/未公開/注目・最大1)＋**分析深度チップ**(override=実データ精査／生成=自動要約＝温度差を正直に可視化・Fable)。
- **関連度サイドバー(2グループ・`officeNavGroups`→`dpNav`)**: 「関連{キーワード|記事}」(現在＋同種Top3・各クラスタ理由サブ行)＋「別の視点で見る」(クロス=割当記事/主KW・紫・1〜2件・**クリックで部屋跨ぎ切替**)＋あふれは「通常ビューで一覧」へ逃がす。**最大6行・スクロール0**。非kw/記事部屋(settings等)は `dpMenuFallbackOn` で従来flat描画(回帰なし)。
- **エントリ整合**: 部屋ピル`dPages`・部屋メニュー`menuPages`(旧 `DETAIL_PAGES` 直参照でstale3件)を `topOfficePages(rv,6)`(override→badge有→上位) に統一。**CTA2段**: 分析カードに ghost「→{クロス先}」を追加。
- **検収(メイン・preview独立)**: 生成ページ(kw_row2/checklist/hiyou-osaeru/引越し挨拶いらない 等)の型A全カード描画・**スクロール0**・未公開の空状態・override作り込み温存(kw_gift下落ストーリー健在)・6statカード視覚確認・クロス遷移(kw⇄記事の部屋跨ぎ・往復)・settingsフォールバック回帰・**consoleエラー0**。QA用一時フック `window.__aos` は検収後に除去済み(`__expose` 行削除・リロードで正常起動を screenshot 確認)。正本不整合1件(未割当KWにperf実績が出る)を検収中に発見しメイン修正(`row.status` 尊重)。
- **教訓(再確認)**: DCプロトのインスタンスは非公開スコープ→QA駆動に一時 `window.__aos=this` フックが有効(検収後必ず除去)。DCはタイマー/演出で `roomView` が revert し得るのでDOMクリックより **onClick直接呼び＋state プローブ** が確実。ワーカーは grep自己確認＋メイン独立検証を厳守。
- **追修正(ユーザー指摘「スライダー禁止に引っ掛かっている／デザイントーンを確認」)**: ①§6.6違反スライダーを是正——部屋詳細（室内ズーム）本体2カラムラッパ(旧 `overflow-y:auto`)＋右カラム(旧 `overflow-y:auto`)を **`overflow:hidden`** に変更（§6.6=overflow-y禁止・クリップ許容の原則どおり）。原因は `dPages` を 3→6 に増やした高さ超過(768px窓で43px溢れ)なので **部屋入口ピルは `topOfficePages(rv,4)` に戻す**（詳細ページのサイドバー関連度Top-6は据置。入口≠全列挙）。②デザイントーン是正——R2で入れたトークン外色を design-tokens.css の office 値に snap: MARKET見出し `#6E86FF→#4DA3FF`(--aos-primary)・PERFORMANCE見出し `#3E9C7A→#3ED598`(--aos-state-done)・補助文字 `#8FA0BE`/`#7C8DA8→#93A2C0`(--aos-text-2)。**教訓: 新色は必ず design-tokens.css から採る（勝手色禁止）。** 既知の残: アプリ左シェルメニュー(NAV)が画面高768pxで約43px溢れ auto-scroll（詳細エクスプローラ外・シェル既存。hidden化は残高/通常ビューへ等の重要項目クリップになるため据置＝要判断）。
- **追修正2(ユーザー指摘「キーワードカードのトーンが合ってない・デグレでは？」＝実バグ確定)**: 関連度サイドバーの primary カードが**ブラウザ既定の灰色ボタン(#f0f0f0)＋黒文字**で描画されトーン不一致。原因は **DCテンプレが style 属性内の三項式 `{{ m.on ? 'a' : 'b' }}` を評価できず**、border/background/color/font-weight/dot が無効化→既定スタイルにフォールバックしていた（border-radius等のリテラルは効くので気付きにくい）。**修正**: 原典 dpMenu と同方式で `officeNavGroups` に `styd()` を追加し border/bg/labelColor/fw/dotColor を **JS側で precompute**、テンプレは単純バインド `{{ m.border }}` 等に。非選択カードは `rgba(5,10,24,.82)`（データカード `rgba(5,10,24,.88)` と同格の濃紺ソリッド）＋border `#243352`、選択は青ティント。ファイル全体を `\{\{ …\?…\}\}` で grep し style内三項式は他に無いことを確認。**教訓: DCの `{{ }}` は単純プロパティパス専用。条件分岐・演算はJSで解決して単純バインドで渡す。検収は"要素の存在/構造"だけでなく getComputedStyle で背景/境界/色の実値を必ず計測する（構造QAでは既定スタイルフォールバックを見逃す）。**
- **追修正3(ユーザー指摘「メイン画面の背景色と開いたとき[部屋ズーム]が合ってない」＝実バグ確定)**: 部屋の背景は1素材(`scene_03.webp`)をフロアごとに `FLOOR_HUE`（色相回転・1F0deg/2F52deg/3F-38deg/4F92deg/5F26deg/6F68deg/7F-16deg・`buildRoom()`）で染め分ける設計。**俯瞰のフロア別部屋カード（line 4164 `room.floorHue`）は正しく適用**していたが、**部屋ズーム画面（line 4343）・Office詳細ページ（line 4547）には hue-rotate が一切配線されておらず**（`dHue` という render val 自体が存在しなかった）、ズーム後は素の未回転トーンに戻っていた。**修正**: render val に `dHue: dRoom.floorHue` を追加（line ~11671）、ズーム画面・詳細ページ双方の背景フィルタに `hue-rotate({{ dHue }})` を追記。2F(紫)で俯瞰カード/ズーム/詳細ページ3画面のトーンが一致することをスクリーンショットで確認、全7部屋で実測フィルタ値が `FLOOR_HUE` 定数と完全一致することを検証。**教訓: 1つの視覚効果(フィルタ・色)を複数画面で使う場合、buildRoom等の共通ビルダーが値を持っていても、各テンプレの render val 配線は個別に見落とされ得る。新しい視覚パラメータを追加したら、それを使う"べき"全画面(俯瞰/ズーム/詳細ページ等の階層)を横断grepして配線漏れがないか確認する。**
- **追修正3b(同ユーザー指摘の続き「色が変わる過程が丸見え」＝配線後もアニメーション中だけ再発)**: 追修正3でdHue配線後も、部屋ズーム画面だけ**ズーム演出の0.8秒間だけ色相が戻る**新種の不具合が発覚。原因は `@keyframes aosZoomBg { from{filter:...} to{filter:...} }`（line 31）が **hue-rotateを含まないfilterを明示**しており、CSSアニメーション中は**同一要素の静的style filterがkeyframesのfilterで丸ごと上書きされる**ため、アニメーション再生中（実測 t=20〜300ms相当）は hue-rotate が完全に消え、アニメーション終了の瞬間に正しい色相へスナップしていた（俯瞰カード・詳細ページはアニメーションなしのdivなので無関係・対象外）。**修正**: hue-rotateを**アニメーションしない親wrapper**（`filter: hue-rotate({{ dHue }})`のみ・静的）に分離し、brightness/saturate/blurのアニメーションは**子div**（keyframes専用・CSS filterは親子で重ね掛けされる）に閉じ込めた（line 4343-4345）。子divには settled後に brightness(1.3) saturate(1.12) を保つ静的filterも残す（無いとアニメーション終了後にfilter:noneへ戻り明るさ/彩度が落ちる回帰を誘発したため気づいて追加）。実測: t=20ms〜900msの全時点でwrapperのhue-rotateが定数（52deg/92deg等）で不変、内側は2.2→1.3・1.4→1.12に正しく推移することを2フロアで確認。**教訓: CSSアニメーションのkeyframesが`filter`プロパティを指定する場合、そのkeyframesは同一要素の静的filter全体を演出中だけ丸ごと差し替える（個々のfilter関数を追加するのではない）。動的に変える視覚パラメータ(hue-rotate等)をアニメーション対象要素に足すときは、必ず「アニメーションしない親でstatic filter・アニメーションする子でkeyframes filter」に分離する。修正後は静的スクショだけでなく、setTimeout刻みでアニメーション区間中のgetComputedStyleをサンプリングして検証すること（settled状態だけの検収では見逃す）。**

## 2026-07-09 引き継ぎ：S3/S5タブ内詳細パネル（スペック完成・実装は未着手・ここから再開）

- **背景**: ユーザーが「サイドメニューからの詳細ページに近い？タブ内の要素配線レベルの詳細ページ化したい」と相談→Fable設計相談→S2キーワード一覧の既存パターン（`kwSel`/`kwSelRow`＝行選択→**同タブ内・一覧の直下に縦積みされる**詳細パネル。左右分割ではない。`display:grid;gap:14px`の単一カラムであることを実機で確認済み）を土台に、**S3「成果物・生成履歴」タブ**と**S5「サイトページ管理」＞「流入ページ」タブ**に同型の詳細パネルを追加する設計。ultracode指示によりWorkflowでリサーチ→精密スペック作成→アドバーサリアルレビュー（3視点×2スペック＝6レビュアー並列）→承認後に逐次実装、という流れで進行中に**「終了してくれ」「時間切れ」でユーザー中断**。
- **現状**: **精密スペック2本は完成・4ラウンドのアドバーサリアルレビューを経て全指摘を修正済み**。ただし**実装（コード変更）は一度も実行されていない**（レビューが通った直後、Implement フェーズに入る前に毎回スペック側の不備が見つかり差し戻し→4ラウンド目を起動した直後にユーザーが中断）。`AI Office de SEO.dc.html` はS3/S5に関して**無改変**（既存のOffice詳細エクスプローラ・色相バグ修正のみが反映済み）。
- **スペックの保存場所**（永続化済み・tempのscratchpadではない）:
  - `prototype/pending-specs/spec-S3-panel.md`
  - `prototype/pending-specs/spec-S5-panel.md`
  - どちらも「現状コード引用→変更後コード」の形で一字一句コピペ可能な精密さで書かれている。ワーカーへそのまま渡せる。
- **再開手順**: 2つのスペックをSonnetワーカーに順番に（**S3→S5の順で逐次**・同一ファイルのため並列不可）実装させ、メインがgrep+preview実機で独立検収する（このセッション終盤と同じ「オーケストレーション＋検収」の型）。実装前にもう一度アドバーサリアルレビューをかけ直すことを推奨（スペックは完成済みだが、実装直前に再度ファイルの現物とold_stringが一致するか確認する価値はある＝行番号は前回検証時点のものなので、他の変更が入っていれば再確認要）。
- **このラウンドで発見・修正された実バグ（4件・スペックに反映済みだが、実装時に同種の見落としが再発しないよう記録）**:
  1. §2の`onRetry`/`onPause`（S3）で存在しないLABELSキー`wpRetrySuccess`を参照＋`roGuard`ガード欠落を発明していた→現物（line 9253-9254相当）と一字一句同一に修正済み。
  2. バッジ色（S5）を`急落`でも緑固定にしていた→Office側`buildArticlePages()`の`badgeColor`分岐と意味を合わせつつ、暗色ネオン値(`#FF6B6B`)は使わず通常ビュー既存のライトトーン(`#C2543F`)に置換。
  3. TXT.en追加で`"AI検索の影響"`を重複登録しかけた（既存行7022と衝突）＋独立`"）"`キーを追加しかけて無関係4画面の閉じ括弧まで誤訳する事故を招くところだった→重複キー削除、閉じ括弧はJS計算値側に`st.locale`分岐で同居させる方式に修正。
  4. S3 §4の挿入アンカー`</sc-for></section></sc-if>`がファイル内**4箇所で完全一致**（S2の別タブ2箇所を含む）→`ctHistory`という一意な文字列を含む複数行ブロック全体をアンカーにするよう修正。
- **教訓**: **メイン自身が書いたスペックの修正パッチも、必ずレビューにかけ直すこと**（今回、ラウンド2の修正が新たな重複キー・括弧不一致バグをラウンド3で生んだ→再修正→ラウンド4起動直後に中断）。アドバーサリアルレビューは「1回通ったら終わり」ではなく、**パッチのパッチにも同じ厳密さを適用**する必要がある。

## 2026-07-10 前回引き継ぎのS3/S5パネルを実装完了（オーケストラ方式・「再開して」指示）

- **背景**: 前セッションの引き継ぎメモ（直上節）どおり再開。セッション冒頭で `spec-S3-panel.md`/`spec-S5-panel.md` の全アンカー文字列・データ内容が対象ファイルの現物と完全一致することを確認済み（`articlePageId(slug)`未定義＝両スペックとも無改変を再確認）。行番号の微小なズレのみで内容は無傷だったため、再レビューなしで実装フェーズへ直行。
- **実装**（Sonnetワーカー`fe-component`に委任、S3→S5の順で逐次・同一ファイルのため並列不可）: 両スペックとも§1〜§5を完遂（S5の§1=`articlePageId`はS3で既存のためスキップ、の想定どおり）。ワーカーの完了報告は簡潔だったため鵜呑みにせず、メインが全項目grep再確認＋タグバランス確認（`sc-if`/`sc-for`/`div`/`button`/`section`/`svg`全て開閉一致）＋`node --check`相当の構文確認を実施。
- **検収**（メイン・`window.__aos`実機、S3→S5とも独立検証）:
  - S3: 5行クリック→パネル切替（タイトル・状態・割当キーワード・QA・消費クレジット・直近履歴の紐付け）を確認。「オフィスで詳しく見る」着地を override（単身相場→`tp_souba`）・生成記事（ダンボール→`ar_/hikkoshi/dambouru-muryou`）の両パターンで確認。既存の再送/一時停止ボタン（`onRetry`）の回帰なしを直接呼び出しで確認。EN切替でチェスト新規追加5文言すべて翻訳・データ非翻訳を確認。console 0。
  - S5: 5行クリック→パネル切替（クリック・前期比・CTR・順位・CV・主要クエリ・AIO・クラスタ）を確認。バッジ色を**getComputedStyle実値で計測**（急落=`rgb(194,84,63)`/`rgba(210,91,91,.1)`、好調=`#21725C`系）。「オフィスで詳しく見る」着地を5行全パターン（override3件`tp_souba`/`tp_gift`/`tp_yarukoto`＋生成2件`ar_/hikkoshi/mitsumori-hikaku`/`ar_/hikkoshi/tanshin-pack-hikaku`）で確認。「ページ一覧」タブ（`kwMapping`）無改変を確認。console 0。
- **検収中に発見・修正した実バグ（構造チェックでは検出不可・実値確認で顕在化）**: S5workerが追加したTXT.en辞書キー`"割当キーワード（クラスタ: "`（末尾に半角スペース）が**EN切替時に一切翻訳されない**不具合。原因は`_i18nSwapAll()`（line 8153）が`const key = raw.trim();`でテキストノードの前後空白を落としてから辞書照合するため、末尾スペース付きの登録キーは trim 後のキーと文字列不一致になり永久にマッチしない（`reg[key]`が`undefined`→無視）。ファイル内の既存辞書エントリを全数走査し、この「末尾コロン+スペース」パターンが他に存在しないこと（＝今回が初出の踏み地雷）を確認。**修正**: キー・値とも末尾スペースを削除（`"割当キーワード（クラスタ:"` / `"Assigned keyword (cluster:"`）。空白は`_i18nSwapAll`側の`next = raw.match(/^\s*/)[0] + val + raw.match(/\s*$/)[0]`が**trim前のrawから復元**するため、キー側の末尾スペースを削っても表示上のスペースは正しく1個だけ再現される（実機で"Assigned keyword (cluster: 挨拶)"を確認）。**教訓**: 静的ラベルを`{{ }}`直前で分割してTXT.en登録するパターン（閉じ括弧を動的値側に同居させる手口・本ファイルで複数回使用）では、**ラベル側の辞書キーに前後空白を含めてはならない**（`_i18nSwapAll`のtrim仕様と衝突する）。この種の不具合はgrep（キー存在確認）では検出できず、**実機でEN切替後のテキスト内容を直接読む**検収でのみ見つかる。
- **後始末**: `prototype/pending-specs/spec-S3-panel.md`・`spec-S5-panel.md` は適用完了につき削除（内容は本節に集約済み）。
- 書面: 本節を追記。screen-inventory等の外部正本ドキュメントへの反映は未実施（このS3/S5詳細パネルはタブ内要素配線レベルの追加のため、影響範囲は本ファイルの記述で十分と判断。必要なら次回追記）。

## 2026-07-10 「詳細ページ＝深掘り閲覧の唯一の代替手段」原則の全画面適用（ユーザー指摘起点・Workflow監査→オーケストラ実装）

- **背景**: S3/S5パネル実装完了後、ユーザーから「これならこのテーブルのプルダウン的役割を詳細ページに期待しているけど大丈夫か？」という質問があり、当初「プルダウンかフルスワップかは実装手段の選択」と誤答→ユーザーが訂正：「プルダウン化しろという意味ではない。各コンテンツがより深い層で見るためのページが詳細ページという役割になる」「プルダウンやスワップでのなんでもいいではなく、それらの代替手段としての詳細ページだということ」。つまり **Officeビューの詳細ページ（`officePage`/`DETAIL_PAGES`）こそが「深掘り閲覧」の本来の解決策であり、各タブでプルダウンや別画面スワップを個別に作り込むのではなく、既存のOffice詳細ページへ導線を張ることが正解**、という設計原則の明確化。「全体の話」として、S1〜S7全画面でこの原則が漏れなく適用されているかの棚卸しをユーザーから要求された。
- **調査（Workflowツール・7画面並列監査＋統合レビュー、9エージェント）**: S1〜S7の全タブ・全リストを対象に、「①項目がOffice詳細ページ対応コンテンツ種別（キーワード/記事実績/承認案件/学習知見/設定ステータス/テクニカルSEO状態）か」「②対応するなら通常ビュー側から`officeNav`+`officePage`で該当詳細ページに飛べる導線が実在するか」を独立ブラインド調査。結果、**導線が実在したのは3箇所のみ**（S2キーワード一覧の行選択パネル・S3ライブ生成の固定リンク・今回セッション新設のS3成果物一覧/S5流入ページ）で、対応データが完全に揃っているのに導線が0本という体系的な欠落が`automation`（承認案件）・`settings`（3ステータス）で発覚。次点でkeyword_research/traffic_reportの主要一覧（S1おすすめ・変動/S2順位モニタリング・クラスター図・対策候補/S5ページ一覧・カテゴリツリー・リライト各種別・上位ベンチマーク・テクニカル分析/S6成功学習/S3カレンダー/S2流入クエリ）多数を検出。詳細は本ファイル前々節「棚卸し」相当（会話内で報告済み・別ドキュメント化はせず本節に統合）。
- **実装方針の確認**: ユーザーへ優先度別（最優先2件/高優先度3件/中〜低優先度多数/対象外）を報告し、「ギャップ全件（中・低優先度も含む）」の指示を得て全件着手。
- **実装（オーケストラ方式・Sonnetワーカー`fe-component`5クラスタ委任＋メイン直接編集2クラスタ、全てメイン独立検収）**:
  1. **automation**（S1やることタブ・S4承認待ちタブ、共通の差分プレビューモーダル1箇所への追加で両画面を一挙カバー）: 新設`automationPageId(approvalId)`（A-1→ap_rewrite/A-2→ap_new/A-3→ap_link/A-5→ap_cv、A-4は対応ページ無しで意図的除外）。モーダルフッターに「オフィスで詳しく見る」を追加（`margin-right:auto`で承認操作ボタン群と分離）。
  2. **settings**（S7 site/acq/billingの3セクションヘッダー）: `seAccessOffice`/`seIntegrationOffice`/`seCreditOffice`の3関数（`st_access`/`st_integration`/`st_credit`は固定3件のため常時表示）。
  3. **keyword_research系7箇所**（S1おすすめ`rec_rw`・S1変動`volAlerts`/`volWatch`・S2順位モニタリング`kwWatchRows`・S2クラスター図の葉chip・S5カテゴリツリーのキーワードchip・S2対策候補`PROMOS`）: 共有ヘルパー`resolveKwOfficeId(text)`新設（完全一致優先、`kwTokenMatch`でのフォールバック。**語順ゆれ**「単身 引越し 相場」⇔「引越し 単身 相場」等を吸収し、KW_ROWS非実在の候補は自動的に導線非表示＝安全側）。**ワーカーの完了報告が「Now edit JS for kwWatchRows...」で途中終了**し、実際は7箇所中4箇所（rec_rw/volAlerts/volWatch/kwWatchRows）のみJS+テンプレ完了、クラスター図はテンプレのみでJS未配線（死んだ導線）、S5チップとPROMOSは未着手という不完全な状態だった。**grep監査で発覚（毎回の慣行どおり鵜呑みにしない）→メインが残り3箇所を直接実装して完遂**。
  4. **traffic_report系6箇所**（S5ページ一覧`kwMapping`・S5リライトcv`anCvTrendRows`/`cvCro`・flash`anSmallRows`・S5上位ベンチマーク`anBenchRows`・link`anLinkCands`）: 共有ヘルパー`resolveArticleOfficeId(ref)`新設（slug直接一致優先、`ARTICLES.title`完全一致でフォールバック）。今回はワーカーが必須5箇所を完遂した上に任意のStep3（`anLinkCands`）のJS側まで着手して報告が切れたため、テンプレ側1箇所のみメインが追加して完遂。
  5. **S6成功学習**（`knWinLearn`3件・`winnerRows`2件）: 新設`knowledgePageId(kind)`（学習知見の`what`文字列先頭「構成:/訴求:/内部リンク:」から`kind`を抽出しDETAIL_PAGES.knowledgeと突合）。メイン直接実装。
  6. **S5テクニカル分析**（`idxIssues`4件）: 新設`technicalPageId(issue)`（technical_seoは3件固定で課題単位のidを持たないため、`type==='tech'&&/リンク切れ/`→`tk_links`、それ以外（index登録状況・canonical等）→`tk_crawl`に丸める設計。`tk_speed`は現状のidxIssuesデータに該当課題が無いため導線元なし＝許容）。メイン直接実装。
  7. **S3カレンダー・S2流入クエリ**（従来onClickが一切存在せずクリック自体不可能だった箇所への新規追加）: `ctCalList`4件はタイトル文字列から`J-\d+\s+`プレフィックスと`（リライト）`サフィックスを除去した上で`artForKw()`→`ARTICLES.title`完全一致の順で解決（2/4件が解決、残り2件はメンテナンス通知等で対応コンテンツ無しのため導線なしが正解）。`ctCalCells`は35日グリッドの中で「生成中 J-1024」という一意に特定可能なイベントのみクリック可能化（他の汎用ラベルは対象を一意に絞れないため非対応のまま=意図的）。S2流入クエリ`anRows`はCSS Gridに8列目を追加（ヘッダー行・データ行双方のgrid-template-columnsとmin-widthを同期）してofficeボタン列を新設。全てメイン直接実装。
- **検収（メイン・`window.__aos`実機、全クラスタ横断）**: `renderVals()`/`s5Vals()`/`s1Vals()`/`stage2Vals()`/`kwTopoWatchVals()`を直接呼び出し例外0を確認した上で、以下を個別に実測:
  - automation: A-1→`ap_rewrite`着地・A-4は`mOfficeOn:false`（意図通り除外）を確認。
  - settings: site/acq/billing各tab起点で`st_access`/`st_integration`/`st_credit`に個別着地を確認。
  - keyword_research: `rec_rw`→`kw_mitsumori`、volAlerts「引越し 単身 相場」（**語順ゆれ**あり）が`kwTokenMatch`フォールバックで正しく`officeOn:true`になることを確認、PROMOS 5件中1件のみ`officeOn:true`（意図通り）。
  - traffic_report: `kwMapping`4件全`officeOn:true`、`anCvTrendRows`の1件（「引越し見積もり 比較ガイド」＝**元データにスペース欠落の既存タイポ**があり`ARTICLES.title`と完全一致せず`officeOn:false`。新規バグではなく安全側フォールバックの結果として許容・データ修正は今回のスコープ外）、`anBenchRows`の`url:'—'`（未作成記事）は正しく除外。
  - S6: `knWinLearn`3件クリックで`kn_structure`着地確認。S5テクニカル: 「リンク切れ」課題→`tk_links`着地確認。
  - S3カレンダー: `ctCalList`「引越し 挨拶 ギフト（リライト）」→`tp_gift`、セル内「生成中 J-1024」→`cc_rewrite`着地確認。S2流入クエリ: 6行全`officeOn:true`。
  - タグバランス最終確認: `sc-if 393/393・sc-for 186/186・div 1710/1710・button 457/457・section 96/96・span 1052/1052・h2 88/88・svg 95/95`（全て開閉一致）。console エラー 0（全クラスタ・全画面遷移を通じ一貫）。
- **教訓（このセッションで2回発生・再確認）**: **Sonnetワーカーの完了報告が文の途中で切れることがあり、これは「時間切れ」ではなく「実装が本当に途中で終わっている」ことを意味する**。両ケースともgrep監査で「JSは無いがテンプレはある（死んだ導線）」「一部箇所は未着手」を検出し、メインが直接補完した。**ワーカー委任のたびに、完了報告の文が自然に終わっているか（尻切れでないか）をまず確認し、尻切れなら即座にgrepで実装済み範囲を特定してから残りを判断すること。**
- **追修正（ユーザー「全て完了？」の確認質問を受けて再監査・発見）**: 上記完了報告後、ユーザーに一言で「はい」と即答せず自分でgrep再監査したところ、S2「対策候補」タブの**除外候補（`DEMOTES`/`demotes`）2件に導線が未実装**であることが発覚（`PROMOS`は実装したが`DEMOTES`はタスク一覧のラベルに含めていたのに実際の委任指示から漏れていた＝自分自身の指示漏れ）。同じ`resolveKwOfficeId(p.name)`パターンでJS（10751-10762行付近`demotes`定義）とテンプレ（1102-1107行付近の採用/却下ボタン列）に追加。実機確認: 2件とも`officeOn:false`（「引越し はがき テンプレート 無料」「引越し 六曜 縁起」はいずれもKW_ROWS10件に非実在＝正しい非表示、バグではない）。タグバランス`sc-if 394/394・button 458/458`に更新。**教訓: 「全て完了？」と聞かれた時に前回の完了報告を鵜呑みにして即答せず、実際にgrepで再監査する価値がある（今回はタスクリストの粒度が粗く、複数項目を1タスクにまとめた際に一部が実装から抜け落ちた）。**
- 書面: 本節に統合。棚卸し結果の詳細一覧は会話ログのみに存在し別ファイル化していない（次回参照が必要になった場合は本節の実装リストから逆引き可能）。

## 2026-07-10 画面接続マップの新規整備（`/goal`起点・6系統調査による実装準拠の不一致台帳）

- **背景**: ユーザーが`/goal`で「画面マップとしてドキュメントに整備して。どこがどこにつながっているか。内容不一致がないかなどをチェックしながら実装時に要件定義を書けるレベルまで整理すること」を指示。既存の`screen-inventory`（責務・データの正本）・`screen-flow`（ジャーニー起点の抽象フロー図）はいずれも目的・責務レベルの記述に留まり、実装コード（state key・行番号・実際の遷移先）まで踏み込んだ接続グラフが存在しなかったため、新規ドキュメントとして整備する方針とした。
- **調査**: ユーザー面DC（S1〜S7・W系・Officeビュー）を5並列Agent、管理コンソール（ADM-S1〜S12）を3並列+統合のWorkflowで独立調査（のべ9エージェント、実コードを`Grep`/`Read`で直接確認）。各グループが正本ドキュメント（screen-inventory / admin-screen-inventory）との突合も実施。
- **成果物**: `docs/design/ai-office-de-seo/L3-ui-prototype/ai-office-de-seo-screen-connection-map_v1.md` を新規作成。全画面・全タブの目的/主な遷移一覧に加え、§3に発見した不一致・ギャップを4分類29件で台帳化（各項目に「要件化する際の論点」を付記）。
- **主な発見（実装バグ・見せかけの操作、詳細はドキュメント§3.1参照）**:
  - S2順位モニタリングの「解除」ボタンが`state.kwWatch`を実削除しない（トーストのみ）。
  - S5リライト＞リンク調整の「承認へ送る」が`automation`へ遷移しない（同種の他ボタンは遷移する）。
  - ADM-S11（データ保護・DR運用）が4セクション14行すべて`onClick`ゼロの完全静的表示＝方針5「閲覧専用に倒さない」に抵触の疑い。
  - ADM-S12「なりすまし調査」がADM-S9への遷移・監査記録を文言上謳うが実装は`showToast`のみ。
  - S4緊急停止`doStopAll`に確認ダイアログがない＝方針11（緊急停止は確認ダイアログ必須）に違反。
  - W7通知センターが書面の11種カタログ中5種のみ実装（残高低下・支払い失敗・Kill Switch等6種が未実装）。
  - W9テナント切替（`seTenantSwitch`）が`showToast`のみで権限再判定なし。
  - 前セッションで構築したOffice詳細ページ導線メッシュにも非対称を発見: S5リライト＞順位改善（`anRankRows`）だけ同タブ内の他3種別（link/cv/flash）と異なりOffice導線が皆無。S1ピックアップの`rec_new`/`rec_fix`も`rec_rw`と同型カードなのにOffice導線がない。
- **注記**: 本ラウンドでは調査・文書化のみを行い、発見した実装ギャップの修正（S3.1〜3.4の29件）は着手していない（ユーザー指示のスコープはドキュメント整備）。次セッションで修正に着手する場合は同ドキュメント§3の優先度（§3.1実装バグ→§3.2方針違反→§3.3 Office導線非対称→§3.4データ不整合）に従うこと。
- **教訓**: 既存の「正本ドキュメント」は責務・目的レベルでは実装とよく一致していても、state key単位の接続グラフや「ラベルはあるが実処理がない」類のバグは目的レベルの記述だけでは検出できない。実コードを読む独立調査（今回は9エージェント）でしか見つからない不一致が多数あった。今後、大きな機能追加のたびに本ドキュメントを更新するか、次回同種の棚卸しを行う際の出発点として扱うこと。

## 2026-07-10 画面接続マップ§3の29件を全修正（ユーザー指示「不備の修正対応をしてきれいな状態に」）

- **背景**: 直前の画面接続マップ整備（上記節）で発見した29件の不一致・ギャップに対し、ユーザーから「不備の修正対応をしてきれいな状態に持って行って」の指示。ultracode継続中のためWorkflowで対応。
- **実装**: 2ファイル（ユーザー面DC・管理コンソール）を並列ストリーム化し、各ファイル内は逐次バッチ（DC=4バッチ・管理コンソール=5バッチ、計9 fe-componentエージェント）で29件中27件を修正。残り2件（#18 辞書候補承認キューの帰属＝実装は正しく機能しているため書面側の追従課題として据え置き、#23 tk_speed/cc_danballの外部deep-link不在＝部屋内導線から到達可能で実害小のため据え置き）はコード変更せず据え置きと判断。
- **ワーカーの空応答2件（尻切れの新パターン）**: 2バッチ（DC batch1・管理コンソールbatch3）が完了はしたが最終メッセージが**完全な空文字列**で返った（従来の「文の途中で切れる」パターンとは違う失敗モード）。journal.jsonlを直接読み、実際のファイル変更をgrepで個別確認したところ、DC batch1は5件中4件（#1,#2,#9,#10）が実際には完了しておりメインが#24（データtypo）のみ直接補完、管理コンソールbatch3は4件中3件（#14,#16,一部#17）が完了しておりメインが#17の残り（S7側cfgEmptyOn）と#25（プラン名統一）を直接補完。**教訓（既存教訓の拡張）**: ワーカーの完了報告が「文の途中で切れる」だけでなく「完全に空」というパターンもあり得る。空応答＝「何もしていない」ではなく「実装はしたが最終要約の出力に失敗した」の可能性があるため、空応答を見たら即座にgrepで実装済み範囲を特定してから残りを判断すること（未確認のまま全項目を再実装させると重複作業になる）。
- **修正作業中に発見した重大な回帰バグ（W7通知カタログ拡張＝#13の実装中）**: ワーカーがヘッダーのミニ通知ポップオーバーを`ncVals().base`から動的生成する方式にリファクタした際、`ncVals()`の`return`文に`base`を追加し忘れ（`ncRows`/`ncEmpty`等は返すが`base`は返さない）。呼び出し側`this.ncVals().base.slice(0,5)`が`undefined.slice()`で例外を投げ、**`renderVals()`が呼ばれるあらゆる場面＝全画面描画で例外が発生しアプリが完全にクラッシュする状態**になっていた。ワーカー自身のタグバランス確認・`node --check`構文チェックはいずれもパスしており、構造チェックでは検出不可能な種類のバグだった。メインが実機で`window.__aos.renderVals()`を直接呼び出して発見し、`base: base,`を`return`文に追加して即修正。**この教訓は`preview-verify-dc-prototype`メモリに追記済み**: 複数バッチ・複数エージェントでの並列/逐次修正が完了した後は、自己報告が「タグバランスOK」でも必ず実機で主要描画関数を直接呼び出し例外の有無を確認すること。`preview_console_logs`はブラウザの累積ログバッファのため、修正前の古いエラーが残り続けて「直ったのか古いログなのか」誤認しやすい点にも注意。
- **検収**: 29件全てを`window.__aos`/`window.__adm`実機（state直接操作・renderVals直接呼び出し）で個別に再検証済み（1件=#15 ADM-S3プロバイダ登録UIのみ、タグバランス確認に留め個別の実機クリック検証は簡略化）。タグバランス最終確認: DC側`sc-if 401/401・sc-for 187/187・div 1714/1714・button 463/463・section 96/96・span 1051/1051`、管理コンソール側`sc-if 83/83・sc-for 73/73・div 704/704・button 108/108・section 44/44・span 304/304`、いずれも開閉一致。
- 書面: `ai-office-de-seo-screen-connection-map_v1.md`に「§5 修正対応記録」を追記（29件の対応内容・検証結果一覧＋上記回帰バグの詳細）。

## 2026-07-10 29件修正の独立再検証（ユーザー指示「再検証して是正できているかチェックを」）

- **背景**: 上記29件修正のあと、ユーザーから独立した再検証の指示。前回の「実機検証済み」という自己申告を鵜呑みにせず、①実装者とは別のWorkflowエージェント5体によるコード配線の独立再読解（JS↔テンプレートの参照が実在するかを重点確認）、②メインによる実UIクリックスルー検証（`element.click()`・React controlled inputへのネイティブsetter経由の入力、直接関数呼び出しだけに頼らない）の2段構えで実施。
- **発覚した問題（4件、コード修正済み・実機確認済み）**:
  1. **#15（ADM-S3プロバイダ登録UI）が実装丸ごと欠落**: 前回の29件修正用Workflowスクリプトで、自分自身のバッチ分割ミスにより#15が9バッチのどれにも割り当てられておらず、誰も実装していなかった（前回の§5記載「ワーカー実装・実機確認は簡略化」は誤り）。再検証時にメインが直接実装（「+ プロバイダを追加」ボタン→モーダル4項目→`pvExtra`へ実追加→監査記録）し、実UIクリックで動作確認。
  2. **#3（なりすまし調査）の一覧行ボタンが死んだコード**: JS側に一覧行用`onImpersonate`ハンドラが定義されているが、テンプレートのどこからも参照されておらず実質未使用（詳細パネル側の1箇所のみ機能）。兄弟操作（返信・FAQ還流）も同型のため設計として妥当と判断し、コード修正はせず書面の記述を訂正するに留めた。
  3. **#4（ADM-S11再実行）が無反応**: `dpJobs`の「再実行」ボタンが監査ログとトーストのみでジョブ一覧自体を更新しておらず、クリックしても見た目が変化しないUXギャップだった。`dpJobsExtra`へ「再実行中」の新規行を追加するよう修正。
  4. **新規発見: `calRows.onApprove`の見せかけの成功トースト**: ADM-S7の較正提案承認キューが、辞書候補（`dict.synonym.*`）承認時に`this.CFG`（別のキー体系）を検索していたため常にヒットせず何も変わらないのに「draft版を作成しました」という成功トーストが出ていた。`DICT`配列を正しく検索し`state.dictPromoted`で「集合知候補」バッジを実際に解除するよう修正。ACT_JP辞書の不足キーも追加。
- **教訓**: 「29件全部やって検収も済んだ」という自分の申告そのものにも、独立した2周目の検証をかけると新たな穴が見つかる。今回は実装ワーカーのミスだけでなく**発注側（メイン自身）のオーケストレーションミス**（#15の配分漏れ）も発覚した。委任元・委任先どちらにも見落としは起こりうるため、「検収した」で終わらせず別の視点（別エージェントのコード再読解＋実UIクリック）でもう一段検証する価値がある。直接関数呼び出しだけでは見えないテンプレート配線漏れは、実際に`element.click()`する検証でのみ検出できる。
- 書面: `ai-office-de-seo-screen-connection-map_v1.md`に「§6 独立再検証記録」を追記。§5の#15記載も訂正済み。

## 2026-07-10 ビジュアル検査・WP配布premise・データ取込みUI点検（ユーザー指摘「ビジュアル検査もした？WP配布前提の抜け漏れ・パーツ抽出/記事登録の操作は入っている？」）

- **背景**: 29件修正＋独立再検証が完了した直後、ユーザーから3点の指摘。(a)これまでの検証は`renderVals()`直接呼び出し＋実DOMクリックが中心で、スクリーンショットによる見た目検証をしていなかったのでは、(b)WPプラグイン配布が前提のはずだがそれが画面マップから抜けていないか、(c)パーツ抽出結果・記事登録のユーザー操作が正しく入っているか。
- **(a) ビジュアル検査**: `preview_screenshot`を複数回試みたが本セッションでは毎回30秒でタイムアウト（`document.hidden===true`でタブ背面化が一因と推測、既知のハーネス側不具合でアプリ側の問題ではない）。代替として`getComputedStyle`による実測（設定＞データ設定タブの「再認可する」ボタン＝正しいグラデーション・文字色、WordPress行のステータスドット＝正しい`#C2543F`を確認、CSS三項式無効化によるブラウザ既定フォールバックは無し）とアクセシビリティスナップショットで代替した。調査中に`preview_console_logs`へ`renderVals`の`TypeError`が200件溜まっているのを発見し「新規回帰では」と一瞬疑ったが、`window.__aos.renderVals()`の直接呼び出しでは例外が再現せず、**累積ログバッファの古いエントリ**と判定（§5.2の教訓が今回も的中）。
- **(b)(c) 実装コード全文検索で確認した新規ギャップ（#30〜#32、いずれもバグ修正ではなく設計判断を要する新規/拡張スコープのため、コード修正はせず書面記録に留めた）**:
  1. **#30 WPプラグイン配布・初回連携UIが丸ごと存在しない**（重要）: `REQ-WPA-07`（ZIP配布・導入だけで自動ペアリング）に対応するUIが両DCファイルのどこにも無い。設定＞データ設定`acq`のWordPress行は「解除」（接続済み）と「再認可する」（`!wpOk`）の2状態のみで、「一度も接続したことがない」状態を区別する仕組みがなく、GSC行（`gscNgOn`→「接続する」）との非対称も確認。Admin Console「サイトを追加」モーダルは「GSC・WordPressの接続に進んでください」と案内するが`siteAddSave`は画面遷移を一切せず、遷移先自体にも初回接続UIが無いため二重に行き止まり。Admin Console側の「プラグインversion台帳」も監査ログの1件のデモ文言のみで実体無し。
  2. **#31 「投稿形式チェック」がパーツ単位の内訳を見せない**: `REQ-WPA-02`が列挙する11種のderivation対象（FAQ support/table support/CTA slot等）はどれも表示されず、実装は単一の集約バッジ「合格」のみ。要求書が指定する「投稿プレビュー」画面も存在しない。
  3. **#32 サンプル記事登録が「記事サマリー同期」と矛盾する手動貼付のみ**: S4/S7の連携パネルは「記事サマリー同期: 今朝6:00完了」と自動同期済みを表示するが、実際にスタイル学習用サンプルを登録する唯一の手段（S6「サンプル記事を追加・差し替え」モーダル）はタイトル・本文の手動貼り付けのみで、同期済み記事から選ぶ導線が無い。
  - なお、生成記事をWordPressへ「登録（公開）」する側（下書き作成・予約投稿・承認・公開）は§1.3/§1.4で既に導線確認済みで問題なし。今回の欠落はいずれも**WPから記事を取り込む側（インバウンド方向）**。
- **教訓**: 「実UIクリックまで検証した」と言っても、スクリーンショットでの見た目確認・要求書の premise レベルでの網羅確認は別軸であり、両方やらないと見逃す種類の不備がある。今回のようにコード上は正しく動く画面ほど「その手前の前提（配布・連携そのもの）が丸ごと無い」ことに気づきにくい。実装バグの検証だけでなく、要求書の各REQ項目が「そもそもどの画面で満たされているか」を定期的に逆引きする価値がある。
- 書面: `ai-office-de-seo-screen-connection-map_v1.md`に「§7 WP配布前提・データ取込みユーザー操作の点検」を追記（version 1.3）。#30〜#32はユーザー判断待ち（実装するか、要件化時の論点記録に留めるか）。

## 2026-07-10 §7の#30〜#32を実装（ユーザー指示「3件ともプロトへ実装する」）

- **背景**: 上記§7の3件（WP配布・初回連携UI欠落／投稿形式チェックのパーツ内訳非表示／サンプル記事登録の矛盾）について、AskUserQuestionで対応方針を確認したところ「3件ともプロトへ実装する」の回答。設計判断を要する新規スコープのためメインが直接実装。
- **#30**: 設定＞データ設定のWordPress行の下に常時表示の「プラグインの入手・再インストール」行→「セットアップガイドを見る」ボタン→モーダル（ZIPダウンロード・サイト連携キー＋コピー・3ステップ手順）を追加。既存の接続済み/再認可が必要の状態遷移ロジックには一切手を加えず（`REQ-SEC-16`の解除＝再認証前提という既存設計意図を壊さないため）、純粋に加算する設計にした。あわせて、Admin Console「サイトを追加」保存後に画面遷移していなかった`siteAddSave`を修正し、実際に`settings/acq`へ遷移するようにした。
- **#31**: 「投稿形式チェック: 合格」の隣に「内訳を見る」→`REQ-WPA-02`の11パーツ（post type〜カスタムフィールド）の対応状況モーダルを追加。FAQ block・カスタムフィールドは意図的に「フォールバック」「未検証」＋理由注記のデータにして一本調子にしなかった。
- **#32**: サンプル記事登録モーダルに「WordPressから同期済みの記事を選ぶ（おすすめ）」ピッカー（モック3件）を追加。手動貼り付け欄はそのまま残し併存させた。
- **実装中の誤検知（教訓）**: パーツ内訳モーダルの検証で`querySelectorAll('span')`の広いテキスト一致検索が期待値の2倍（22件）を返し「sc-forの二重レンダリングでは」と疑ったが、`data-dc-tpl`属性でスコープを絞った再測定と`renderVals()`直接呼び出しでは一貫して正しい件数（11件）だった。既存の他要素（無関係な「対応」等の汎用ステータス文言）が広いテキスト一致に紛れ込んだだけで実際のバグではなかった。**教訓**: 疑わしい二重描画の検証は、広いテキストマッチではなく一意な属性（`data-dc-tpl`等）でスコープを絞ること。
- **検証**: `renderVals()`を全7画面横断で直接呼び出し例外0を確認。実DOMクリックで(a)セットアップガイドの一連操作、(b)パーツ内訳モーダルの11項目表示、(c)同期記事ピッカーのクリック登録、(d)サイト追加→`settings/acq`への実遷移、をすべて確認。タグバランス最終確認: `sc-if 403/403・sc-for 189/189・div 1741/1741・button 470/470・section 96/96・span 1058/1058`。
- 書面: `ai-office-de-seo-screen-connection-map_v1.md`に「§7.6 対応実施」を追記（version 1.4）。

## 2026-07-10 ターゲット軸・主張軸の構造化（ユーザー指摘「学習ナレッジの項目ライティングスキルとか自社サービスのUSPだったり」）

- **背景**: §7とは別に、ユーザーから「学習ナレッジにライティングスキルやUSPを入れられるか」の指摘。ナレッジ管理＞「ターゲット軸・主張軸」タブを調査。
- **ライティングスキル**: `REQ-PRODUCT-12`（意図はユーザー・技巧は製品、ユーザー自作few-shot登録は提供しない）により**意図的にスコープ外**と判明。欠落ではなく仕様のため対応不要と判断（「サイト方針・執筆ルール」タブで軽量な推奨ルールとしてなら登録可）。
- **USP・差別化**（実装漏れ）: `REQ-PRODUCT-12`が主張軸を「押したい主張／避けたい主張／差別化の立ち位置」の3区分・構造化フィールド（`allowed_claims[]`/`avoided_claims[]`）として要求しているが、実装は単一フラットリストのみだった。
- **ターゲット軸が完全に静的**（調査中に発覚した追加バグ）: 「引越し初心者（主）」「経験者（副）」の2チップがテンプレートへの**ハードコード文字列**で、`state`と一切紐づかず追加・編集・削除が一切不可能だった。3件中最も深刻な欠落。
- **対応**（AskUserQuestionで範囲確認、「ドメイン的な知識は入れられるようにしないとダメだろ」との回答で両方フル対応）: ターゲット軸を`KN_TARGETS`クラス定数＋`state.knTargets`配列に変更しチップの追加（主/副トグル付き）・削除を実装。主張軸を「押したい主張」「避けたい主張」「差別化の立ち位置（USP）」の3セクションに分割し、各々独立した入力欄・NG表現バリデーション・削除を実装。
- **検証**: 実DOMクリック・ネイティブsetter入力で、ターゲット追加/役割トグル/削除、USP登録、避けたい主張でのNG差し戻し（「必ず」検出）を確認。`renderVals()`全画面横断で例外0。タグバランス: `sc-if 409/409・sc-for 192/192・div 1760/1760・button 477/477・section 96/96・span 1057/1057`。
- 書面: `ai-office-de-seo-screen-connection-map_v1.md`に「§7.7 ターゲット軸・主張軸の構造化」を追記（version 1.5）。

## 2026-07-10 管理コンソールADM-S8のタブ分割＋サイドメニューのスクロール位置リセット（ユーザー指摘「開発コンソール側のページが縦詰みすぎているからタブ分割するのと謎のサイドメニューのスクロール挙動があるから修正」）

- **背景**: ユーザーから2点の指摘。(a)管理コンソールのどこかの画面が縦に詰まりすぎている、(b)サイドメニューのスクロール挙動が謎（意図不明な動き）。全12画面の行数を計測したところ、ADM-S8「Pack・プロンプト・ゲート管理」が459行で突出（2位のADM-S4は221行）。実機でスクロール位置を計測して調査したところ、サイドメニュー自体の描画（`position:sticky`）は正常だったが、**サイドメニューの画面切替ボタンをクリックした際、直前の画面のスクロール位置がそのまま持ち越され、遷移先の画面が短いと本文の途中（最悪の場合は末尾）に着地する**実バグを発見（ブラウザのネイティブなスクロール位置クランプ挙動が原因。`scr`変更時に明示的なスクロールリセットが一度も呼ばれていなかった）。
- **(a) ADM-S8のタブ分割**: 459行の内訳を精査すると、実質7つの独立した機能（UIテキストレジストリ、ゴールデン評価・段階ロールアウト、Packレジストリ一覧＋詳細＋draft統制フロー、表示ラベルレジストリ、通知テンプレート、類語・言い換え辞書、office_layout編集）が1画面に無関係に積み重なっていた。新規`packTab`state（既定`'registry'`）でタブ分割: 「Pack管理」（既定・レジストリ一覧＋詳細＋draft統制フローの核となる編集フロー）／「UI・評価」／「表示ラベル」／「通知テンプレート」／「類語辞書」／「オフィス構成」の6タブ。既定タブの高さは3335px→1745pxに縮小、他5タブは最長でも1画面（946px)以内に収まる。表示ラベル・通知テンプレート・類語辞書・office_layoutの4ブロックはPack詳細列の内部から抽出し、独立したトップレベルのタブコンテンツとして再配置（`max-width:760px`を付与し読みやすさを担保）。
- **(b) サイドメニューのスクロールリセットバグ修正**: サイドメニューの`navItems`の`onClick`と、新設した`packTabs`の`onClick`の両方に`window.scrollTo(0, 0)`を追加。画面/タブを切り替えるたびに必ずページ先頭へ戻るようにした。
- **検証**: 実機で「長い画面を下までスクロール→短い画面へサイドメニューから切替」を再現し、修正前は着地位置がスクロール位置に依存すること（バグ再現）、修正後は毎回`scrollY:0`に戻ることを確認。同様にPackタブ切替でも確認。`renderVals()`を12画面＋Packの6タブ全通り直接呼び出し、例外0を確認。タグバランス最終確認: `sc-if 90/90・sc-for 74/74・div 717/717・button 112/112・section 44/44・span 308/308・main 12/12`。
- **教訓**: 「スクロール位置がおかしい」系の不具合は、個々の要素のCSS（`position:sticky`等）だけを見ても見つからない。実機で`window.scrollY`とページ高さを直接計測し、「長い画面→短い画面への遷移」のような具体的な操作シーケンスを再現して初めて発見できた。

## 2026-07-10 ADM-S4・ADM-S1もタブ分割（ユーザー指摘「全サイドメニューの全ページで詰みすぎなんだけど」）

- **背景**: 直前のADM-S8タブ分割だけでは不十分との指摘。全12画面の実測高さ（`requestAnimationFrame`を2回挟んだ非同期計測、viewport=946px）を取得し直したところ: ADM-S8/Pack管理タブ=1453px、**ADM-S4コスト・観測=2225px（新たに突出）**、**ADM-S1運用ダッシュボード=1468px**、ADM-S2課金=1024px、ADM-S7設定=1027px、ADM-S9テナント=950px、残り5画面（ADM-S3/S6/S10/S11/S5/S12）はいずれも946px（1画面）以内。
- **対応**: 実測で明確に1画面を超えていたADM-S4とADM-S1を追加でタブ分割。billing(1024px)・config(1027px)・tenant(950px)は1画面をわずかに超える程度（8〜9%）で、タブ分割の複雑さに見合わないと判断し据え置き。
  - **ADM-S4（コスト・観測ダッシュボード）**: 新規`costTab`state（既定`'overview'`）で3タブ化。「概要」（負荷平準化＋キャパシティ＋KPIカード、既定）／「コスト内訳」（作業手順別費用・実行タイミング別・テナント別コスト＋月次推移グラフ）／「キャッシュ・GSC」（GSCマッチ率＋使い回しキャッシュ詳細＋運用パラメータ）。結果: 概要946px・コスト内訳1451px（3つの密なテーブル+グラフを含むため残存、単独では意味のある1塊なのでこれ以上は分割しない判断）・キャッシュGSC946px。
  - **ADM-S1（運用ダッシュボード）**: 新規`opsTab`state（既定`'health'`）で3タブ化。「システム状態」（メール送達性＋自動復旧＋golden signals KPI＋SLO・検知、既定）／「通知・お知らせ」（管理者向け通知集約＋運営お知らせ配信）／「インシデント」。SLO・検知は元はサイドバーカードだったが、タブ分割にあたり通常のセクションとして「システム状態」タブへ統合。結果: システム状態1022px・通知お知らせ946px・インシデント946px（元1468pxから最大30%減）。
  - 両画面ともサイドメニューと同じ`window.scrollTo(0,0)`パターンをタブのonClickに適用。
- **検証**: `renderVals()`を12画面＋Pack6タブ＋cost3タブ＋ops3タブの全通りで直接呼び出し例外0を確認。実クリック（`element.click()`）でops「通知・お知らせ」タブへの切替＋スクロールリセットを確認。タグバランス最終確認: `sc-if 96/96・sc-for 76/76・div 720/720・button 114/114・section 44/44・main 12/12`。
- **教訓**: 「一番長い画面を直せば十分」という判断は禁物。ユーザーの「全ページで」という指摘を受けて全画面を再実測したところ、最初のラウンドで見えていなかった2番目に長い画面（ADM-S4、当時2位だった221行が実測では最長2225pxに）が新たに最悪値として浮上した。行数（静的なソース行数）と実際のレンダリング高さ（テーブルの列数・グラフ・折返し等に依存）は必ずしも比例しない。対応範囲を決める際は行数の見積もりだけでなく実測値で判断すること。

## 2026-07-10 管理コンソールにグローバル検索を追加（ユーザー指摘「見たいものにたどり着けない運用コストが高い」）

- **背景**: タブ分割（ADM-S8/S4/S1）で縦の詰みは解消したが、その副作用として「画面を選ぶ→タブを選ぶ」の2段階クリックが必要になり、ナビゲーションコストがかえって上がっていた。さらに管理コンソールにはユーザー面DC（`AI Office de SEO.dc.html`）のようなヘッダー常設のグローバル検索（REQ-PRODUCT-15）が存在せず、画面ごとのローカル検索（Pack一覧・設定レジストリ・監査ログ・トレース・用語辞書）しかなかった。「見たいものがどの画面のどのタブにあるか」を利用者が記憶して辿るしかない構造が根本原因と判断。
- **対応**: ユーザー面DCの`gsIndex()`/`gsGo()`/`gsVals()`パターンをそのまま管理コンソールに移植し、ヘッダーに常設のグローバル検索ボックスを新設（既存の「用語辞書」ボタンの左、ダークテーマに合わせたスタイル）。検索対象は39件のインデックス:
  - 画面12件（ADM-S1〜S12、タブを持つ画面は既定タブ込みで直接着地）
  - タブ9件（Pack管理の5タブ、コスト観測の2タブ、運用ダッシュボードの2タブ — 既定タブ以外）
  - Pack項目4件（クリックで該当Packを選択した状態でPack管理タブへ、`sel`インデックスを指定）
  - 設定レジストリキー10件（クリックで該当キーに`cfgQuery`をセットし、設定レジストリ画面が自動的に1件へ絞り込まれる）
  - プロバイダ・モデル4件
  - 結果クリックで`window.scrollTo(0,0)`も実行し、既存のスクロールリセット方針と統一。
- **検証**: 実際に検索ボックスへ入力→実クリックで、(a)「キャッシュ」→ADM-S4のタブへ直接ジャンプ、(b)「TTL」→設定レジストリの該当キー1件だけに絞り込み、(c)「誇大表現」→Pack管理タブでその項目が選択された状態、の3パターンを確認。`renderVals()`を12画面横断で例外0確認。タグバランス最終確認: `sc-if 99/99・sc-for 77/77・div 725/725・button 116/116・section 44/44・main 12/12`。
- **教訓**: ナビゲーション構造を改善する施策（タブ分割で縦詰み解消）が、別の指標（到達までのクリック数）を悪化させることがある。「詰み」を解消するだけでなく、「情報アーキテクチャを深くした分だけ検索・ショートカットで到達コストを相殺する」というセットで設計する必要がある。ユーザー面DCに既にあったパターン（グローバル検索）を横展開するだけで解決できたので、姉妹画面の既存パターンをまず確認する価値がある。

## 2026-07-10 管理コンソールに企業（テナント）単位の切り替え機能を追加（ユーザー指摘「企業単位の切り替え機能がいるな」）

- **背景**: グローバル検索追加の直後の指摘。管理コンソールは複数テナント企業（タナカメディア合同会社・サンプル商事株式会社、`TENANTS`定義）を横断的に扱うが、ヘッダーには常に固定の「scope: 全テナント（read）」テキストがあるだけで、特定の1社に絞り込んで見る手段が皆無だった（監査ログ・コスト内訳・サポートチケット・設定レジストリの行が全テナント混在で表示され続ける）。ユーザー面DCには既に「契約組織の切替」（`ORGS`/`currentOrg`）があるが、これは管理コンソールとは別ファイル・別概念（ユーザー自身が所属组織を切替＝W9）であり、管理コンソール側の「オペレーターがどのテナント企業を見るか」という要望には対応していなかった。
- **対応**: ヘッダーの静的な「scope: 全テナント（read）」テキストを、クリックで開くテナント選択ドロップダウン（`tScope` state、既定`null`=全テナント）に置き換えた。選択すると以下4画面のデータが実際に絞り込まれる:
  - **ADM-S5監査ログ**（`auditRows`）: `a.tenant`に選択企業名を含む行のみ
  - **ADM-S4コスト・観測「コスト内訳」タブ**（`tcRows`＝テナント別コスト内訳、`gscRows`＝GSCマッチ率）: 各行に`tenant`フィールドを新設し一致するものだけ表示
  - **ADM-S12サポートデスク**（`sdTickets`・件数・選択チケット）: `t.tenant`が一致するチケットのみ（未解決件数バッジも連動）
  - **ADM-S7設定レジストリ**（`cfgRows`）: `テナント: X`とスコープ指定された項目のうち他社向けのものを除外（グローバル・プラン単位のものは常に表示）
  - `TENANTS`に短縮名`short`フィールド（'タナカメディア'/'サンプル商事'）を追加し、既存データの表記ゆれ（正式社名 vs 監査ログ等で使われる略称）を吸収。
- **検証**: JS直接呼び出しで「タナカメディア」選択時の各画面の絞り込み件数（監査ログ1件・コスト内訳3件・サポート1件・設定8→10件の差分）、「サンプル商事」選択時の設定レジストリが正しく2件除外され8件になることを確認。実クリックでドロップダウンを開いてタナカメディアを選択→ヘッダー表示が「scope: タナカメディア（read）」に変化→監査ログ画面へ遷移して実際に1件に絞り込まれることを確認。`renderVals()`を12画面×3スコープ状態（タナカメディア/サンプル商事/全テナント）の全通りで例外0確認。タグバランス最終確認: `sc-if 100/100・sc-for 78/78・div 728/728・button 118/118・section 44/44・main 12/12`。
- **教訓**: 「グローバル検索で個別項目に直接ジャンプできる」ことと「特定企業の視点で画面全体を見渡せる」ことは別の要求。前者はピンポイントの到達コストを下げるが、後者（当ラウンドで対応）は「今どの企業の文脈で作業しているか」という状態を保持し続ける機能で、複数テナントを横断管理する管理コンソール特有の需要。ユーザー面DCの対応する概念（契約組織切替）を安易に流用せず、管理コンソール側の実際のデータ構造（`AUDIT`/`tcRows`/`SD_TICKETS`/`CFG`それぞれのtenant表記）を個別に確認してから実装した。

## 🔖 セッション引き継ぎメモ（2026-07-10 セッション終了時点・本ラウンド完了後）

- **未了・次セッション候補**:
  1. ~~Officeビュー「エージェントの作業を詳しく見る」ツアー（`TOUR_STEPS_OFFICE`）に詳細ページ案内ステップが未追加（複数セッション前から継続）。~~ **実装済み（2026-07-10 後半ラウンド。下記「独立検証第2弾＋ゆとり化」参照）**
  2. `ai-office-de-seo-screen-connection-map_v1.md`の§3 #18（辞書候補承認キューの帰属）・#23（tk_speed/cc_danball到達経路）は意図的に据え置き（理由は§5参照）。正本ドキュメント（admin-screen-inventory等）側への反映は次回書面更新時に。
  3. 管理コンソールのタブ分割: ADM-S8(`packTab`)・ADM-S4(`costTab`)・ADM-S1(`opsTab`)の3画面を実測ベースでタブ分割済み。ADM-S2課金(1024px)・ADM-S7設定(1027px)・ADM-S9テナント(950px)は1画面をわずかに超える程度で意図的に据え置き（さらに指摘があれば追加分割を検討）。サイドメニュー・全新設タブに`window.scrollTo(0,0)`のスクロールリセットを適用済み。
  4. 管理コンソールにグローバル検索（`gsIndex`/`gsGo`/`gsVals`、ユーザー面DCと同型）を新設済み。画面12・タブ9・Pack項目4・設定キー10・プロバイダ4の計39件をインデックス化。今後、新しい画面・タブ・登録型データ（例: テナント一覧・監査ログ種別等）を追加する際は`gsIndex()`にも追加してインデックスの網羅性を保つこと。
  5. 管理コンソールに企業（テナント）単位の切り替え（`tScope` state、ヘッダーのscopeドロップダウン）を新設済み。現在は監査ログ・コスト内訳・サポートデスク・設定レジストリの4画面に適用。他画面（例: ADM-S9テナント一覧の強調表示、ADM-S3プロバイダのテナント別割当等）へ拡張が必要になった場合は`tScope`をそのまま参照すればよい（`this.TENANTS`の`short`フィールドで表記ゆれを吸収する設計）。
  6. その他は既知の残課題なし（画面接続マップ§3の29件＋再検証で発覚した4件＋§7の3件（#30〜#32）＋§7.7のターゲット軸・主張軸構造化、すべて対応・実機クリック検証済み）。
  7. 上記3〜5（ADMタブ分割・グローバル検索・テナントスコープ）は`ai-office-de-seo-screen-connection-map_v1.md`の「§8 管理コンソールのタブ分割・グローバル検索・テナント切替」（v1.6）に正式反映済み。あわせて§2.0/§2.1の「ADM-S1〜S12に画面内タブは存在しない」という旧記述も実装準拠に更新済み（ADM-S1/S4/S8はタブ持ちに変更）。

## 2026-07-10 独立検証第2弾＋通常ビューのゆとり化（/goal「画面検証→マップ修正→ブラッシュアップ→詳細ページ前提でゆとり」）

- **調査**: 3系統の独立エージェント（①通常ビュー一貫性・gsIndex網羅、②Officeビュー＋ツアー、③通常ビュー⇄Office詳細ページの重複台帳）＋メイン実測（全43タブ`renderVals`例外0・高さ計測 viewport900px）。正本記録は `ai-office-de-seo-screen-connection-map_v1.md` **§9（v1.7）**。
- **新規発見7件・全修正**（#33〜#39）: 最重要は`relatedOf()`が記事リンクIDを`'ar_'+slug`連結で作りoverride記事（tp_*）への関連/クロス導線が全滅していたデッドリンク（→`articlePageId()`経由に統一・40本全解決を実機確認）。ほかにgsIndexへ`GS_TABS`43エントリ＋`kind:'tab'`新設（support/knowledge/content＋全タブが検索到達可能に）、`TOUR_STEPS_OFFICE`に「詳細ページ（深掘り）」ステップ（`room`/`page`フィールド＋`tourGo`のofficeモード分岐＝officeNav→officePageの順序依存遵守・endTourクリア）、dpStats死にデータ削除、廃止語「夜間チェック」残存の統一、「夜間実行の完了通知」→「おまかせ実行（夜間）の完了通知」、watch●ボタン統一。
- **ゆとり化Y1〜Y6**（通常ビュー=見やすさ優先／Office詳細ページ=詳しさ優先。機能は削除せずモーダル・詳細パネル・Office詳細ページへ委譲）:
  - Y1 S1承認待ち: 大型カード→2行コンパクト行＋「一括処理 →」。承認/差し戻しは差分モーダルに集約（実クリック確認）。S1ピックアップ 1173→**900=1画面完結**。
  - Y2 S2キーワード詳細パネル: SERP/競合構造/関連グループを削り「記事に入れる要素」1行＋Office誘導noteに（kwDetSerp等のvalsは孤児化・無害）。1228→1080。
  - Y3 S2クラスター図: 候補の収集元→対策候補タブへ移設（2カラム化）、ギャップ×網羅率を「カバレッジ」1セクション横並びに。1818→1179。
  - Y4 S3生成履歴: 行内ボタン6個→状態依存（再送/停止）のみ＋「選択して詳細・操作 ›」。詳細パネルに操作フッター新設（`ctOutPanelCopy/Dl/Wp/WpOn/QaGo/Rewrite`）。1401→1219。
  - Y5 S7タブ分割: 「お支払い・通知」→「お支払い」＋新タブ「通知」（`seTab:'notif'`・6タブ構成）。WB_MAP・ROOM_DATA.settings（options 6項目化）・GS_SETTINGS・GS_TABS追随。1577→1299＋通知900。
  - Y6 S6学んだ勝ちパターン: 3行→1行（根拠・推移・確信度はkn_*ページが正本、●title明記）。1105→976。
  - 据え置き: W7通知1350（受信箱の性質）・+17〜26%帯の5タブ・S7その他（同意系1目的群）。判断根拠は§9.2。
- **検証**: 44タブ`renderVals`例外0・consoleエラー0・タグバランス `sc-if 408/408・sc-for 190/190・div 1745/1745・button 476/476・section 95/95・span 1058/1058・svg 95/95`・TXT.en重複11件のまま（「通知設定」重複を追加時に検出し即削除）・EN切替スポット確認・S1差分モーダル/gsGo3系統/ツアー往復の実クリック確認。
- **教訓**: ①deep-mergeでidをoverrideする設計では、リンクIDを文字列連結で再構築してはならない（必ず解決ヘルパー経由。kw側は正しくarticle側だけ非対称という「片側だけ直っている」パターンは横断grepで潰す）。②タブ分割・ラベル改名時はWB_MAP/ROOM_DATA/GS_SETTINGS/GS_TABS/TXT.enの5点セットを同一コミットで追随（今回Y5で実施）。③縮約系のリデザインは「機能を削る」のではなく「操作の置き場所を移す」（行→詳細パネル、カード→モーダル）＋「深掘りデータの正本をOffice詳細ページに一本化」で行う。④サイドバー幅の狭いカラムで1行flexに詰めるとタイトルが数文字に潰れる——スクショ目視で発見（2行構成に修正）。構造検証だけでなくスクショの目視をワンセットに。

## 2026-07-10 モード切替ヴェール（ユーザー相談「OfficeビューとカラートーンPを近づけるべき？遷移がストレスでは」→「トーンは分けたまま遷移だけ緩和」で合意・案1採用）

- **設計判断**: 通常=ライト業務面／Office=ダークネオンのトーン対比は役割シグナル＋コンセプトの中核なので**統一しない**。両ビューでヘッダー・サイドメニューの濃紺クロームは既に共有されており、ストレスの実体はコンテンツ領域の輝度反転（白⇄濃紺）の瞬間のみ。そこだけ暗色ヴェールのフェードで緩和する。
- **実装**: `setMode()`でモード切替は**即時**（後続の`officeNav`/`officePage`設定と競合しない＝ディレイ方式を避けた理由）、同時に`modeVeil`をセット→固定オーバーレイ（`#0A1224`・z-index 390・pointer-events none・`aosModeVeil .38s ease forwards`＝opacity 1→0）がフリップを覆ってフェードアウト→430msの`setTimeout`（`this._veilT`・連打はclearTimeoutで再スタート）でアンマウント。`rmOn()`（prefers-reduced-motion）時と同一モードへの再クリック時はヴェールを出さない。keyframesはhelmetの`aosToast`直後、テンプレはトースト直前（グローバル領域＝両モードで描画）、valは`modeVeilOn`（`toastOn`の隣）。
- **検証**: 両方向の切替でヴェールのマウント→opacity中間値（0.11実測）→アンマウント・state自動クリアをrAFサンプリングで確認。renderVals例外0・consoleエラー0・タグバランス sc-if 409/409・div 1747/1747。
- **同日改訂（ユーザー「エレベーターみたいなアクションのほうがいい」）**: フェードヴェールを**エレベーター扉演出**に差し替え。`scene_elevator.webp`は床数「2」焼き込み＋アスペクト固定のため全画面演出には使わず、素材のルック（濃紺パネル＋青ネオンシーム）をCSSで再現——左右2枚の扉div（`linear-gradient`＋`border`のシーム発光＋内側ネオンライン）＋上部中央のフロアインジケータchip（`modeVeilLabel`: officeへ=「▲ AGENT OFFICE」／通常へ=「▼ 通常ビュー」）。keyframes `aosDoorL/R`（0〜24%は閉で保持→100%で`translateX(±103%)`・0.62s・`cubic-bezier(.66,0,.34,1)`）＋`aosDoorLabel`（フェードアウト）。アンマウントは680ms。構造は前と同じ「切替は即時・閉じた扉が新ビューへ開く」なのでofficeNav/officePageとの競合なし・reduced-motionガードも同じ。
- **同日再改訂（ユーザー「エレベーターとは違う感じで」→AskUserQuestionで「ホログラム走査ワイプ」を選択）**: 扉演出を**ホログラム走査ワイプ**に差し替え（最終形）。単一の暗色シールド（`rgba(6,12,26,.97)`＋`repeating-linear-gradient`の微細スキャンライン）が keyframes `aosScanWipe`（0〜10%保持→100%で`translateY(101%)`・0.58s・`cubic-bezier(.45,.05,.55,.95)`）で上→下へ抜け、上端の発光走査線（青グラデ＋二重glow）＋残光帯＋右上の行き先タグ（`modeVeilLabel`）が付随。旧`aosDoorL/R/Label`は削除済み。**注意: helmetの`aosDoorInL/InR/OutL/OutR`（`elevPhase`）はOfficeビューの既存フロア移動演出で別物＝触らないこと**。state機構（`modeVeil`・680msアンマウント・rmOnガード・即時切替）は不変。検証=中間凍結スクショ（走査線上=新ビュー/下=シールド）・Web Animations APIで完走（finished）・往復・アンマウント・console 0・タグバランス一致。
- **検証の注意（今回のハマり）**: プレビューのパネルが背面だと `document.hidden=true` になり、`setInterval`/`rAF`のサンプリングが極端に間引かれ、`getComputedStyle`のtransform読みが「アニメーションが進んでいない」ように見える偽陰性を起こす。**CSSアニメーションの進行検証は `document.getAnimations()`（Web Animations API）の `playState`/`currentTime` で行うのが確実**（今回 `finished`/progress 1.21 で完走を確認）。閉状態の目視はアニメーションを`animationPlayState='paused'`で凍結してからスクリーンショット（凍結スクショは撮れたが、`preview_screenshot`が古いフレームを返すことがあるので直後にDOMプローブで裏取りしてから撮り直す）。

## 2026-07-10 Office詳細ページに「元のページへ戻る」を追加（ユーザー指摘「元ページに戻る遷移がないわ」）

- **問題**: 通常ビュー各所の「オフィスで詳しく見る」で詳細ページへ飛んだあと、**発リンク元の通常ビューのページへ直接戻る手段がなかった**。詳細ページのNEXT ACTIONは固定の行き先（例: S3から飛んだtp_*ページのCTAはanalytics/traffic）で、来た場所には戻れない。既存の戻り経路は「部屋へ戻る→俯瞰→HUBボタン」の3段（screenは保持されるがタブまでは戻らない）。
- **実装**: `setMode('office')`で通常→Officeに入る瞬間、現在位置のスナップショット `this._stdReturn`（screen＋全タブキー: s1Tab/kwTab/ctTab/auTab/anTab/anRwKind/knTab/seTab、undefinedは除外）を自動記録。**32箇所ある個別導線には一切手を入れず、setModeの一点で捕捉**。詳細ページヘッダーの「部屋へ戻る」の隣に「↩ 元のページへ戻る」（`dpBackStdOn`/`dpBackStd`＝setMode('standard')＋スナップショット復元。シルバー系ボーダーで部屋へ戻ると区別）。Office直入り（スナップショット無し）ではボタン非表示。EN: "Back to previous page"。
- **検証**: S6成功学習→kn_structure→戻る=knowledge/winner復帰、S5リライト/CV改善→tp_*→戻る=anTab:'rewrite'+anRwKind:'cv'まで復元（実DOMクリック）、S2キーワード詳細→kw_mitsumoriでヘッダー2ボタン並置をスクショ目視。renderVals例外0・console 0・タグバランス sc-if 410/410 他一致・重複キー11のまま。
- **調査メモ**: `kwPageId()`が未知キーワードで返す`'kw_rowx'`は「実在しないID→dpOpen=false→部屋止まり」の**意図的センチネル**（コメント記載どおり導線を出さない設計）。検証でkwSel未設定のままgoOfficeKwDetailを直接呼ぶと踏むが、実UIではボタンがkwSel選択時のみ表示されるため実害なし＝バグではない。

## 2026-07-10 通知センターに段階表示（ユーザー「表示件数の調整は？」）

- **現状把握**: 「もっと見る」型の段階表示・表示件数セレクタは全アプリで未実装だった（全リスト固定長。内部sliceはWORK LOG等の演出用のみ）。実利がある最長リスト＝通知・アラートセンター（フィルタ後最大17行・1350px）に段階表示を実装。
- **実装**: `ncVals()`で既定8件（`NC_LIMIT`）にslice、`st.ncShowAll`で全件。リスト末尾に「すべて表示（残りn件）」ボタン（`ncMoreOn`/`ncMoreLabel`=locale分岐のJS計算値/`ncShowAll`val）。フィルタ切替（すべて/要対応/運営お知らせ）で`ncShowAll:false`にリセット。valsの関数キー`ncShowAll`とstateキー`ncShowAll`は名前空間が別で衝突しない。
- **検証**: 既定8行=946px（1画面化、旧1350px）→ボタン「すべて表示（残り9件）」→17行→フィルタ切替でリセット＋各フィルタでも8件超なら再表示。renderVals例外0・console 0・タグバランス sc-if 411/411 他一致。
- **未対応（要判断）**: テーブル系（S2キーワード一覧・S5ページ一覧・W6履歴）の表示件数セレクタ（10/25/50）は、プロトのデモデータが10件前後のため「見せかけのセレクタ」になる。本実装のページネーション要件（要求書側）に委ねるか、プロトにもダミーで置くかは未決。

## 2026-07-10 移行案内文言の除去（ユーザー指摘「移動しました、は新規リリースでいらなくない？」）

- S5リライト＞CV改善タブの案内バナー「CVポイントの登録・記事への割当は「ナレッジ管理」へ移動しました」を、現在形のガイダンス「このタブは、公開済み記事のコンバージョン改善（差し替え提案）を扱います／CVポイントの登録・記事への割当は「ナレッジ管理」で行います。」に書き換え（**ナレッジ管理へのクロスリンク「CVポイントの管理へ →」は温存**＝案内の機能は残し、移行の物語だけ除去）。EN辞書2キー追加。
- 両DCを「移動しました/移設/以前は/従来の/リニューアル/旧タブ/旧画面」で掃引し、ユーザー可視の移行案内はこの1件のみと確認（コードコメントの「旧:」等は対象外）。**原則: IA再編時の「〜へ移動しました」系バナーは開発中の暫定文言。リリース観点では「今どこで何をするか」の現在形ガイダンスに直す**（新規ユーザーは旧配置を知らない）。

## 2026-07-10 Office導線の視認性＋詳細ページの直接アクション化（ユーザー指示「Office導線と詳細ページからそのままアクションできる導線の見直し。特にOfficeへのボタンが分かりにくい」）

- **①Office導線ボタンの明示化**: 無ラベルの「●」円ボタン14箇所（18px×13＋26px×1）を、**発光ドット＋「オフィスで見る」ラベル付きの濃紺ピル**（10px・title温存・hover青リング）へ一括置換（Pythonで完全一致replace＋件数assert）。追随したレイアウト調整2件: S2流入クエリのグリッド末列 30px→104px（2箇所）、順位モニタリングの操作列 96px→minmax(210px,auto)（2箇所・リライト+解除+ピルの3ボタン行が96pxに収まらず見切れていた）。パネルヘッダーの大型「オフィスで詳しく見る」ボタン・promoタブ等のテキストリンクは既にラベル明示のため不変。EN: 「オフィスで見る」="View in office"。
- **②詳細ページCTAの直接アクション化**: NEXT ACTIONを「一覧を開く」ナビから**状態依存の行動**へ:
  - 生成kwページ: 未割当=「この語で記事を作成」（content/create・新規・今すぐレーン・kwMakeNew同型）／割当済み=「この記事をリライトへ」（content/create・rewrite）
  - 生成記事ページ: 好調/学習元バッジ=「成功学習でこの記事の学びを見る」（knowledge/winner・保護記事をリライトへ誘導しない）／通常公開=「この記事をリライトへ」／未公開=「成果物（未公開の記事）を確認」（content/output）
  - override 4件も同方針で更新（kw_mitsumori=リライトへ・kw_danball=成果物確認・tp_souba/tp_yarukoto=成功学習）。ap_*の「差分を承認」は元から直接アクションのため不変。roGuard・showToast付与。一覧への到達はサイドバー「通常ビューで一覧」と「元のページへ戻る」が担うため機能減なし。旧「通常ビューでこのキーワードを開く/流入ページを開く」の辞書キーは孤児化（無害）。
- **検証**: CTA全5種の着地をstate直接確認（未割当→create/new/now、割当→rewrite、souba→knowledge/winner、danball→content/output、通常記事→rewrite）。ピル=対象10タブで表示・ページ横はみ出し0・順位モニタリング表はスクロールなしで全列表示（917/917）・実クリックでkw_mitsumori詳細ページ着地・EN「View in office」表示。renderVals例外0・console 0・タグバランス sc-if 411/411 他一致・重複キー11のまま。

## 2026-07-10 一般的なユーザーコントロールの網羅監査＋補完（ユーザー「入力やテーブル表示の量とか一般的にユーザーがコントロールや入力するもので漏れはない？」）

- **監査**（独立エージェント・テーブル18種×8観点＋フォーム13種×6観点の○×マトリクス）: 主な欠落= ①列内テキスト検索が全テーブルで皆無 ②S5流入ページ/W6のソート欠如 ③主要モーダルの未保存ガード不在 ④文字数上限ゼロ ⑤サイト追加のURL形式チェック無し ⑥S4承認の種別フィルタ無し ⑦CSVが3箇所のみ。
- **実装（バッチA=テーブル系）**: S2キーワード一覧・S5ページ一覧・W6タスク履歴に**テキスト検索ボックス**（`kwQuery`/`anPageQuery`/`whQuery`。kwCsv/whCsvは検索適用後を維持・0件時は空状態）。S5流入ページに**列ソート**（`anTrafficSort`・クリック/前期比/掲載順位/CV・ソート時`anPageSel`リセット）。S4承認に**種別フィルタチップ**（`apFilter`: すべて/リライト（タイトル改善含む）/新規/リンク/CV。`apRows`=S4専用フィルタ済みval・**S1ダイジェストの`approvals`には非適用**・「すべて選択」`apToggleAll`/`apAllOn`はフィルタ後の可視行のみ対象・フィルタ切替で選択リセット）。※ワーカー委任だが尻切れで項目3-5が未実装＝メインが補完（毎回の教訓どおりgrep監査で検出）。ワーカー起因の`kwMappingEmpty`誤参照はワーカー自身が修正済みを確認。
- **実装（バッチB=入力系）**: サイト追加に**URL形式チェック**（`LABELS.msg.siteAddUrlInvalid`）。サンプル記事モーダルに**下書き保持**（`knSampleDraft`＝閉じても入力温存・保存でクリア。※キーワード一括貼付`kwBulk`は元から閉じても残る構造＝対応不要と確認）。watchキーワード/CV名称に`maxlength="60"`。**CSV追加2箇所**（S5ページ一覧`kwMappingCsv`・S7消費履歴`seLedgerCsv`＝`this._seLedgerRows`で表とCSVを単一ソース化）。W6検索は`kind`も対象（「リライト」で種別検索可）。
- **意図的に見送り（記録）**: テーブルの表示件数セレクタ（デモデータ10件前後で見せかけになる・方針5）／期間切替の全表展開（データが28日固定デモのため同上）／goalModal・cvPtの下書き保持（2項目の短文で価値薄）／サンプル本文のmaxlength（長文貼付が正当用途）。
- **検証**: 実機で検索3種（DOM実入力含む）・ソート昇降・承認フィルタ（link→1件・S1側5件不変・すべて選択がフィルタ内のみ）・URL拒否/受理・下書き復元・CSV vals存在、renderVals例外0・console 0・タグバランス sc-if 412/412 他一致・重複キー11のまま。

## 2026-07-10 要求定義書群との全量突合＋書面追随（ユーザー「品質改善はできそう？要求系とか漏らしてない？」）

- **突合**（独立2系統: ①台帳⇄実装ドリフト ②L1要求の逆引き）。結果:
  - **実装側の能力欠落は実質ゼロ**（据え置き済みの screen-connection-map §3 #18/#23・ADM-S12一覧行の死にコード・PT-Q「日本語リテラル皆無」＝テンプレ静的文言のインライン残存=既知の意図的トレードオフ、のみ）。
  - **書面ドリフト D1〜D12 を検出→追随済み**: screen-inventory §5のS1/S2/S3/S6/S7行（S7=6タブ・クラスター図再構成・生成履歴スリム化・承認待ちコンパクト化・成功学習1行化）＋§5末尾の旧タブ列挙＋2026-07-10追記ブロック（段階表示・テーブルコントロール・Office導線見直し・元のページへ戻る・走査ワイプ）を反映。admin-screen-inventory にもタブ化/グローバル検索/テナントスコープの追記。両ファイル updated_at=2026-07-10。
  - **要求の本物の漏れ=REQ-KGA-22 ローカルSEOの後半（P1・未実装）**: ①ローカル順位観測・地域スコープ設定は実装済みだが、②地域ページ/サービスエリアページの記事タイプ＋地域固有情報（対応エリア・NAP等）の入力フォーム、③NAP（名称・住所・電話）一貫性チェック＋LocalBusiness/Service構造化データ妥当性（テクニカル分析に項目なし）、④ローカル競合・レビュー信号の戦略入力、が欠落（REQ-NAV-04のコンテンツ作成画面責務にも明記あり）。**ユーザー判断待ち＝未実装**。
  - **意味論ズレ（P2・記録のみ）**: REQ-WPA-04 公開前承認とプロトの自動公開トグル（2026-07-08のユーザー決定による意図的変更・L1本文が未改訂のまま＝L1更新時に反映すべき既知事項の再確認）／REQ-PRODUCT-09 の版UP時差分再同意ゲートが未実装（同意書表示のみ）。
  - **P3（バックエンド寄り・軽微）**: PRODUCT-19 導出事実ストア/施策台帳の一覧UI・KGA-22④。
- **品質面の実機確認**: 本日追加分のEN翻訳漏れゼロ（7画面×8プローブ・placeholder含む）・新設コントロールは全てnative要素＝キーボード到達・focus-visible適用・新演出はreduced-motionガード済み（PT-Z維持）。PT-C（5類型）は新設テーブルの空状態を実装済みだが、`state.disp`検証トグルの対象一覧への組み込みは未追認（残タスク・軽微）。

## 2026-07-10 REQ-KGA-22（ローカルSEO後半）＋REQ-PRODUCT-09（再同意ゲート）を実装（要求突合の検出漏れ→ユーザー指示「全部実装」）

- **経緯**: 要求逆引き監査で「観測（ローカル順位・地域スコープ）だけ実装され、地域ページ生成・NAP一貫性チェック・ローカル競合入力・再同意ゲートが未実装」と検出。AskUserQuestionで全部実装の指示。ワーカー委任は辞書キー＋定数（BIZ_INFO/BIZ_AREAS/BIZ_RIVALS/CT_LOCAL_ORG）のみで停止（尻切れ・実装本体ゼロ）→メインが直接実装。
- **KGA-22②④**: S6戦略入力に「事業者情報（ローカルSEO）」（bizInfo編集モーダル=必須＋電話`/^[0-9-]+$/`検証・bizAreas chips=Undo削除/重複チェック付き追加・onEscにbizEdit登録）と「ローカル競合・レビュー信号」（bizRivals行=Undo削除・インライン追加=評価/レビューは'—'）。S3起点に'local'（cands分岐・ctOrigins 4本目・ctOriginNote分岐・記事タイプチップ`ctTypeChip`動的化＋`ctLocalNoteOn`注記と`ctGoBizInfo`導線）。
- **KGA-22③**: S5テクニカル分析`idxIssues`にtype:'local'2行（NAP不一致=detailが`(st.bizInfo||BIZ_INFO).phone`とライブ連動・LocalBusiness=openingHours未記載warn）＋フィルタ「ローカル」＋`idxTechNote`改訂（旧文の辞書キーは新文へ差し替え）。※local行のOffice着地は既存`technicalPageId`のフォールバック（tk_crawl）＝専用ページは未作成（拡張点）。
- **PRODUCT-09**: `consentVer2`/`reconsentDone` state。S7利用同意書に版チップ＋「版の更新を再現（検証用）」トグル。更新中は差分3点提示の再同意モーダル（同意して続ける/あとで）＋通常ビュー常設バナー（office時は非表示）。**`ctStart`にゲート**（再同意まで生成開始をブロック・LABELS.msg.reconsentBlocked。閲覧・承認・設定は非制限＝要求の「機能制限」を生成開始に限定する解釈）。
- **検証**: 実機で電話形式の拒否/受理・エリア追加/重複・競合追加・地域起点の候補/チップ/注記・localフィルタ2行・NAP detailのbizInfoライブ連動・再同意ゲート（sim→modal→later→banner→ctStartブロック（ctCreated不変・ctSeq不変）→agree→開始成功→「v1.1・再同意済み」）を全通り確認。renderVals例外0・console 0・タグバランス sc-if 416/416 他一致・重複キー11のまま。EN辞書は banner/modal/差分3点/チップ等を登録（データ値は対象外）。
- 書面: screen-inventory末尾に追記済み。ローカルSEOのOffice詳細ページ（technical_seoにローカル専用ページ）・地域ページのlive生成デモは拡張点として未実装。

## セッション記録（2026-07-10・残課題解消: tk_local新設＋ADM-S12掃除＋SVGバインディング描画バグ）

- **tk_local**: technical_seoの4枚目の詳細ページ（型E）。`technicalPageId()`が`type:'local'`をtk_crawlに丸めていたフォールバックを解消。修正候補・分析文は`st.bizInfo || BIZ_INFO`とライブ連動（`cur`合成部のid分岐で上書き。st_creditと同じパターン）。CTAは`idxFilter:'local'`を積んでS5に着地。
- **ADM-S12**: 一覧行mapの未配線ハンドラ4種（selOn/onReply/onImpersonate/onFaq）を削除。`sel`解決を先に移動し、選択中行ハイライト`rowBg`を新規配線（テンプレ側 `background: {{ t.rowBg }}`）。
- **support.js（dc-runtime成果物）の系統バグ**: `walkText`が`{{ }}`を常にHTML `span.sc-interp`で包むため、**SVG `<text>`内のバインディングが全く描画されなかった**（ゲージ数値83%・ラベル・リンクグラフのノードラベル・チャート軸すべて不可視。textContentには存在するためテキスト検証では検出不可＝getBBox().width===0で発覚）。親が`namespaceURI === svg`なら`tspan`で包むよう修正。生成元`dc-runtime/src`はリポジトリに無いためビルド成果物を直接編集（ファイル内にコメントで明記）。
- **教訓**: ①SVG内のテキストは innerText/textContent 検証をすり抜けて視覚的に消える。SVG絡みの検収は `getBBox().width === 0 && textContent.trim()` の全数スキャンを入れる。②スクショが「％だけ表示」に見えても、まずDOM実測（今回の初回スクショはstaleではなく本物のバグだった）。③support.jsはブラウザキャッシュが強い: 編集→reloadだけでは旧版が残ることがある。`fetch(url,{cache:'reload'})`でキャッシュ更新→再reloadで確実に反映。
- PT-C（5類型トグル）は代表テーブル限定のデモ設計として追認。S6設定系小型リストは対象外のまま。
